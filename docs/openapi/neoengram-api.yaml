openapi: 3.1.0
jsonSchemaDialect: https://json-schema.org/draft/2020-12/schema

info:
  title: NeoEngram Public API
  version: 1.0.0
  summary: NeoEngram 用户、CLI 和 UI 使用的非 REST JSON API 契约
  description: |
    这是 NeoEngram 中心服务的首版公开 API 设计契约。`neoengram-central` 已注册 System、Tenant、
    StorageVolume、StorageEnrollment、Artifact、Playground、Snapshot、S3 Access Point、资源生命周期、
    Managed Add Job 和 Gateway Registry 的当前 action；其余 operation 仍是目标契约，不表示已经可以访问。Gateway Registry
    当前管理 Pool/Replica 资源和显式 activation action；运行时 mTLS 与最多一跳 Replica forwarding 已
    接入。外部生产 issuer/证书自动切换、完整业务 E2E、真实集群切换和跨集群 payload 仍待验收；
    S3 只读 Access Point 控制面 action 已在本契约中公开。

    业务方法使用“模块 / 子域 / 动作”路径，并通过普通 JSON 请求和响应调用；
    路径不包含版本号。除版本查询和健康检查外，调用方必须发送
    `NeoEngram-API-Version: 1` 和 Bearer JWT。

    Agent API 不属于本 OpenAPI。Gateway edge listener 已实现 enrollment action，以及由 Agent 主动发起、
    逐帧 Ed25519 签名的 HTTP/2 全双工 `application/x-ndjson` control channel；session open、heartbeat、
    Assignment/Decision 下发和 report 上报均通过 Gateway 转发该 channel，MetadataBatch 与 object 使用
    独立 action。Gateway/Agent 运行时 mTLS 身份校验已接入；外部生产 issuer/provisioner、证书轮换和
    一次性切换仍是部署验收门槛。Storage enrollment 仅公开 Tenant 管理员创建一次性 bootstrap token
    和审批 Agent 主动提交的脱敏申请。

    本契约只公开 P0 用户资源和脱敏逻辑元数据。Dashboard 聚合、容量与底层诊断、用途与保留
    策略、Lease/Mount、Agent/assignment、fencing、Manifest/Chunk、文件内容 digest、对象位置和
    物理路径不属于本 OpenAPI；不得由客户端扩展字段模拟为生产能力。
  license:
    name: MIT OR Apache-2.0
    identifier: MIT OR Apache-2.0

servers:
  - url: /
    description: 与当前 neoengram-central 用户 API origin 相同

tags:
  - name: System
    description: API 版本和兼容能力查询
  - name: Tenant
    description: 当前 principal 可见的租户与平台级租户创建方法
  - name: Storage
    description: 租户登记的 StorageVolume 查询与创建方法
  - name: StorageEnrollment
    description: Tenant 管理员冻结待接入 PVC descriptor、创建 bootstrap token 并审核脱敏 enrollment 记录
  - name: Project
    description: 租户内 Project 创建与查询方法
  - name: Artifact
    description: 租户内 Artifact 创建、查询，以及 Commit 图和文件 Diff 查询方法
  - name: Playground
    description: 租户内 Playground 创建、查询和 Commit 方法
  - name: Snapshot
    description: 绑定 Artifact Commit 的只读 Snapshot 创建与查询方法
  - name: Placement
    description: Commit 对象副本、数据健康和目标 Volume Workspace 操作
  - name: S3
    description: 将 Ready Snapshot 通过 Gateway 暴露为只读 S3 Access Point，并管理短期凭证与对象浏览
  - name: ResourceLifecycle
    description: Tenant 管理员查询安全删除影响、管理回收站任务与 Retention Hold
  - name: Job
    description: 用户发起和查询的中心权威 Job 方法
  - name: Gateway
    description: 平台管理员管理 EdgeCluster GatewayPool 与 GatewayReplica 的全局方法
  - name: Health
    description: 供进程管理器和负载均衡器调用的标准健康探针

paths:
  /api/system/version/query:
    post:
      tags: [System]
      operationId: queryApiVersion
      summary: 查询当前 API 和 Agent wire 版本
      security: []
      description: |
        返回当前服务使用的公开 API 版本和 Agent wire protocol 版本。版本不匹配直接拒绝，
        不提供版本数组协商，因此不要求 `NeoEngram-API-Version` 或认证。
      parameters:
        - $ref: "#/components/parameters/RequestId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EmptyRequest"
            example: {}
      responses:
        "200":
          description: 版本与能力信息
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiVersionResponse"
              example:
                api_version: 1
                agent_wire_version: 1
                capabilities:
                  - artifact_catalog
                  - artifact_commit_graph
                  - artifact_commit_diff
                  - artifact_commit_replication
                  - managed_add
                  - sqlite_authority
                  - commit_layout_selection_v2
                  - snapshot_delivery_fuse_v2
                  - snapshot_delivery_copy_v2
                  - snapshot_delivery_hardlink_v2
                  - s3_readonly_access_point
                  - resource_lifecycle_v1
        "413":
          $ref: "#/components/responses/PayloadTooLargeProblem"
        "422":
          $ref: "#/components/responses/ValidationProblem"
        "429":
          $ref: "#/components/responses/OverloadedProblem"
        "500":
          $ref: "#/components/responses/InternalProblem"
        "503":
          $ref: "#/components/responses/ServiceUnavailableProblem"
        "504":
          $ref: "#/components/responses/RequestTimeoutProblem"

  /api/tenant/list/query:
    post:
      tags: [Tenant]
      operationId: queryTenantList
      summary: 查询当前用户可见租户
      description: |
        分页返回认证 principal 已获授权的租户。列表不会返回 principal 无权访问的租户，
        `can_create_tenant` 只用于界面能力提示，服务端仍必须在创建时重新授权。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QueryTenantListRequest"
            example:
              page_size: 50
              query: research
      responses:
        "200":
          description: 当前 principal 可见的租户页
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueryTenantListResponse"
              example:
                items:
                  - tenant_id: tenant-a
                    display_name: 研究数据平台
                    description: 研究团队的受管数据空间
                    resource_version: "3"
                    created_at_unix_ms: "1785167000000"
                    updated_at_unix_ms: "1785167600000"
                    permissions: [tenant.read, artifact.read, job.create]
                next_cursor: tenant-page-2
                can_create_tenant: true
        "401":
          $ref: "#/components/responses/AuthenticationProblem"
        "403":
          $ref: "#/components/responses/AuthorizationProblem"
        "413":
          $ref: "#/components/responses/PayloadTooLargeProblem"
        "422":
          $ref: "#/components/responses/ValidationProblem"
        "500":
          $ref: "#/components/responses/InternalProblem"
        "503":
          $ref: "#/components/responses/ServiceUnavailableProblem"

  /api/tenant/query:
    post:
      tags: [Tenant]
      operationId: queryTenant
      summary: 查询单个租户
      description: 查询当前 principal 可见的租户视图；不可见与不存在统一返回 404。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QueryTenantRequest"
            example:
              tenant_id: tenant-a
      responses:
        "200":
          description: 可见租户的当前视图
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueryTenantResponse"
              example:
                tenant:
                  tenant_id: tenant-a
                  display_name: 研究数据平台
                  description: 研究团队的受管数据空间
                  resource_version: "3"
                  created_at_unix_ms: "1785167000000"
                  updated_at_unix_ms: "1785167600000"
                  permissions: [tenant.read, artifact.read, job.create]
        "401":
          $ref: "#/components/responses/AuthenticationProblem"
        "403":
          $ref: "#/components/responses/AuthorizationProblem"
        "404":
          $ref: "#/components/responses/ResourceNotFoundProblem"
        "413":
          $ref: "#/components/responses/PayloadTooLargeProblem"
        "422":
          $ref: "#/components/responses/ValidationProblem"
        "500":
          $ref: "#/components/responses/InternalProblem"
        "503":
          $ref: "#/components/responses/ServiceUnavailableProblem"

  /api/tenant/create:
    post:
      tags: [Tenant]
      operationId: createTenant
      summary: 创建租户
      description: |
        仅平台管理员可调用。相同 tenant ID 和相同 canonical 创建内容返回既有租户并标记重放；
        相同 tenant ID 对应不同内容时返回 `TENANT_ID_REUSED`。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateTenantRequest"
            example:
              tenant_id: tenant-lab
              display_name: 算法实验室
              description: 算法团队的受管数据空间
      responses:
        "200":
          description: 租户已创建或相同创建请求被重放
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateTenantResponse"
              examples:
                created:
                  value:
                    tenant:
                      tenant_id: tenant-lab
                      display_name: 算法实验室
                      description: 算法团队的受管数据空间
                      resource_version: "1"
                      created_at_unix_ms: "1785168000000"
                      updated_at_unix_ms: "1785168000000"
                      permissions:
                        [tenant.admin, tenant.read, artifact.read, job.create]
                    replayed: false
                replayed:
                  value:
                    tenant:
                      tenant_id: tenant-lab
                      display_name: 算法实验室
                      description: 算法团队的受管数据空间
                      resource_version: "1"
                      created_at_unix_ms: "1785168000000"
                      updated_at_unix_ms: "1785168000000"
                      permissions:
                        [tenant.admin, tenant.read, artifact.read, job.create]
                    replayed: true
        "401":
          $ref: "#/components/responses/AuthenticationProblem"
        "403":
          $ref: "#/components/responses/AuthorizationProblem"
        "409":
          $ref: "#/components/responses/TenantConflictProblem"
        "413":
          $ref: "#/components/responses/PayloadTooLargeProblem"
        "422":
          $ref: "#/components/responses/ValidationProblem"
        "500":
          $ref: "#/components/responses/InternalProblem"
        "503":
          $ref: "#/components/responses/ServiceUnavailableProblem"

  /api/storage/volume/list/query:
    post:
      tags: [Storage]
      operationId: queryStorageVolumeList
      summary: 查询租户内 StorageVolume
      description: |
        分页返回租户登记的逻辑存储卷。返回值不包含挂载路径、凭据、Agent ownership
        或 fencing 信息；region 和 EdgeCluster 用于创建资源时选择放置位置。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QueryStorageVolumeListRequest"
            example:
              tenant_id: tenant-a
              page_size: 50
      responses:
        "200":
          description: StorageVolume 页
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueryStorageVolumeListResponse"
        "401":
          $ref: "#/components/responses/AuthenticationProblem"
        "403":
          $ref: "#/components/responses/AuthorizationProblem"
        "404":
          $ref: "#/components/responses/ResourceNotFoundProblem"
        "409":
          $ref: "#/components/responses/CursorConflictProblem"
        "413":
          $ref: "#/components/responses/PayloadTooLargeProblem"
        "422":
          $ref: "#/components/responses/ValidationProblem"
        "500":
          $ref: "#/components/responses/InternalProblem"
        "503":
          $ref: "#/components/responses/ServiceUnavailableProblem"

  /api/storage/volume/query:
    post:
      tags: [Storage]
      operationId: queryStorageVolume
      summary: 查询单个 StorageVolume
      description: 按 tenant 和稳定逻辑存储 ID 返回可用于资源放置选择的公开视图。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QueryStorageVolumeRequest"
      responses:
        "200":
          description: StorageVolume 当前公开视图
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueryStorageVolumeResponse"
        "401":
          $ref: "#/components/responses/AuthenticationProblem"
        "403":
          $ref: "#/components/responses/AuthorizationProblem"
        "404":
          $ref: "#/components/responses/ResourceNotFoundProblem"
        "413":
          $ref: "#/components/responses/PayloadTooLargeProblem"
        "422":
          $ref: "#/components/responses/ValidationProblem"
        "500":
          $ref: "#/components/responses/InternalProblem"
        "503":
          $ref: "#/components/responses/ServiceUnavailableProblem"

  /api/storage/volume/create:
    post:
      tags: [Storage]
      operationId: createStorageVolume
      summary: 登记 StorageVolume
      description: |
        在租户内登记已有 PVC 或 NFS 后端，不创建 Kubernetes PV/PVC 或 NFS 资源。
        region 由 StorageVolume 固定，后续资源创建只提交 storage_volume_id。
        首次登记的 Volume 响应必须为 `state=unavailable`；未经受信 Agent session 和健康挂载观测，
        不得自动成为 `ready`。相同请求重放只返回当前权威视图，不改变或提升 Volume 状态。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateStorageVolumeRequest"
            example:
              tenant_id: tenant-a
              storage_volume_id: volume-vision
              display_name: 视觉数据 PVC
              edge_cluster_id: cluster-cn-east-1
              region: cn-shanghai
              backend_type: pvc
              access_mode: read_write_many
              pvc_reference:
                namespace: neoengram-data
                claim_name: vision-data
      responses:
        "200":
          description: 首次登记返回 unavailable；相同请求重放返回当前权威状态且不触发状态提升
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateStorageVolumeResponse"
        "401":
          $ref: "#/components/responses/AuthenticationProblem"
        "403":
          $ref: "#/components/responses/AuthorizationProblem"
        "404":
          $ref: "#/components/responses/ResourceNotFoundProblem"
        "409":
          $ref: "#/components/responses/MutationConflictProblem"
        "413":
          $ref: "#/components/responses/PayloadTooLargeProblem"
        "422":
          $ref: "#/components/responses/ValidationProblem"
        "500":
          $ref: "#/components/responses/InternalProblem"
        "503":
          $ref: "#/components/responses/ServiceUnavailableProblem"

  /api/storage/enrollment/token/create:
    post:
      tags: [StorageEnrollment]
      operationId: createStorageEnrollmentToken
      summary: 创建 Storage enrollment bootstrap token
      description: |
        要求 `storage.enrollment.create` 权限。为尚未接入或需要替换 Agent 的 PVC 创建 15 分钟有效、
        只能被内部 Agent bootstrap 成功消费一次的 opaque token；无需预先登记 StorageVolume。token
        冻结并绑定 tenant、StorageVolume ID、display name、EdgeCluster、Region、access mode 和 PVC
        descriptor；Agent 提交的 descriptor 或 probe 不匹配时审批不得通过。服务端只存储受保护的
        token 材料。

        `token_request_id` 是稳定 mutation identity；相同 ID 和完全相同 payload 重放原响应并将
        `replayed` 设为 true，不同 payload 返回 409。原始 `bootstrap_token` 只允许出现在本方法的
        成功响应中，不能进入 enrollment 列表、查询、审批、审计或日志。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateStorageEnrollmentTokenRequest"
            example:
              tenant_id: tenant-a
              token_request_id: storage-enrollment-token-vision-01
              storage_volume_id: volume-vision
              display_name: Vision dataset PVC
              edge_cluster_id: cluster-cn-east-1
              region: cn-east-1
              access_mode: read_write_many
              pvc_reference:
                namespace: neoengram-data
                claim_name: vision-data
      responses:
        "200":
          description: bootstrap token 已创建或相同请求被重放
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateStorageEnrollmentTokenResponse"
              examples:
                created:
                  value:
                    token_id: storage-enrollment-token-01
                    bootstrap_token: ngenr_v1_hQ7Bv2jL0pK9Xz4cN6mR8sT1wY3aD5fG
                    volume_descriptor_digest: aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
                    expires_at_unix_ms: "1785168900000"
                    replayed: false
                replayed:
                  value:
                    token_id: storage-enrollment-token-01
                    bootstrap_token: ngenr_v1_hQ7Bv2jL0pK9Xz4cN6mR8sT1wY3aD5fG
                    volume_descriptor_digest: aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
                    expires_at_unix_ms: "1785168900000"
                    replayed: true
        "401":
          $ref: "#/components/responses/AuthenticationProblem"
        "403":
          $ref: "#/components/responses/AuthorizationProblem"
        "404":
          $ref: "#/components/responses/ResourceNotFoundProblem"
        "409":
          $ref: "#/components/responses/StorageEnrollmentConflictProblem"
        "413":
          $ref: "#/components/responses/PayloadTooLargeProblem"
        "422":
          $ref: "#/components/responses/ValidationProblem"
        "429":
          $ref: "#/components/responses/OverloadedProblem"
        "500":
          $ref: "#/components/responses/InternalProblem"
        "503":
          $ref: "#/components/responses/ServiceUnavailableProblem"
        "504":
          $ref: "#/components/responses/RequestTimeoutProblem"

  /api/storage/enrollment/list/query:
    post:
      tags: [StorageEnrollment]
      operationId: queryStorageEnrollmentList
      summary: 查询 Storage enrollment 列表
      description: |
        要求 `storage.enrollment.read` 权限。分页返回当前 Tenant 内 Agent 已主动提交的脱敏 enrollment
        记录；不可见 Tenant 与不存在 Tenant 使用相同的 404 语义。响应不包含 bootstrap token、CSR、
        证书、PVC UID、mount path、AgentId/MountId、session、lease 或 fencing 信息。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QueryStorageEnrollmentListRequest"
            example:
              tenant_id: tenant-a
              state: pending_approval
              page_size: 50
      responses:
        "200":
          description: Storage enrollment 页
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueryStorageEnrollmentListResponse"
              example:
                items:
                  - tenant_id: tenant-a
                    storage_enrollment_id: storage-enrollment-vision-01
                    storage_volume_id: volume-vision
                    display_name: Vision dataset PVC
                    edge_cluster_id: cluster-cn-east-1
                    region: cn-east-1
                    access_mode: read_write_many
                    pvc_reference:
                      namespace: neoengram-data
                      claim_name: vision-data
                    registration_kind: initial
                    state: pending_approval
                    agent_version: 0.0.1
                    identity_fingerprint: aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
                    proof_of_possession_status: verified
                    probe:
                      observed_access_mode: read_write
                      descriptor_matches: true
                      observed_at_unix_ms: "1785168060000"
                    resource_version: "1"
                    created_at_unix_ms: "1785168060000"
                    expires_at_unix_ms: "1785254460000"
                    updated_at_unix_ms: "1785168060000"
        "401":
          $ref: "#/components/responses/AuthenticationProblem"
        "403":
          $ref: "#/components/responses/AuthorizationProblem"
        "404":
          $ref: "#/components/responses/ResourceNotFoundProblem"
        "409":
          $ref: "#/components/responses/CursorConflictProblem"
        "413":
          $ref: "#/components/responses/PayloadTooLargeProblem"
        "422":
          $ref: "#/components/responses/ValidationProblem"
        "429":
          $ref: "#/components/responses/OverloadedProblem"
        "500":
          $ref: "#/components/responses/InternalProblem"
        "503":
          $ref: "#/components/responses/ServiceUnavailableProblem"
        "504":
          $ref: "#/components/responses/RequestTimeoutProblem"

  /api/storage/enrollment/query:
    post:
      tags: [StorageEnrollment]
      operationId: queryStorageEnrollment
      summary: 查询单个 Storage enrollment
      description: |
        要求 `storage.enrollment.read` 权限。按 Tenant 和 enrollment ID 返回脱敏审批视图；不存在与
        跨 Tenant 不可见统一返回 404。该方法不返回任何 bootstrap 或 Agent credential 材料。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QueryStorageEnrollmentRequest"
            example:
              tenant_id: tenant-a
              storage_enrollment_id: storage-enrollment-vision-01
      responses:
        "200":
          description: Storage enrollment 当前脱敏视图
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueryStorageEnrollmentResponse"
        "401":
          $ref: "#/components/responses/AuthenticationProblem"
        "403":
          $ref: "#/components/responses/AuthorizationProblem"
        "404":
          $ref: "#/components/responses/StorageEnrollmentNotFoundProblem"
        "413":
          $ref: "#/components/responses/PayloadTooLargeProblem"
        "422":
          $ref: "#/components/responses/ValidationProblem"
        "429":
          $ref: "#/components/responses/OverloadedProblem"
        "500":
          $ref: "#/components/responses/InternalProblem"
        "503":
          $ref: "#/components/responses/ServiceUnavailableProblem"
        "504":
          $ref: "#/components/responses/RequestTimeoutProblem"

  /api/storage/enrollment/approve:
    post:
      tags: [StorageEnrollment]
      operationId: approveStorageEnrollment
      summary: 批准 Storage enrollment
      description: |
        要求 `storage.enrollment.review` 权限。仅允许批准未过期的 `pending_approval` enrollment；服务端
        必须在一个事务中执行 resource version CAS，校验冻结的 Tenant/Volume/EdgeCluster/Region/
        access mode/PVC descriptor、probe 和一 PVC 一活动 Agent 唯一约束。initial 审批
        创建缺失的 StorageVolume，或精确绑定 descriptor 完全一致且 `unavailable`、无活动 Owner 的
        既有 PVC Volume；replacement 则精确绑定同一 Tenant 下由旧 Owner 占用的既有 Volume。
        `registration_kind=replacement` 时必须显式传 `confirm_replacement=true`，否则返回 409。

        审批响应中的 StorageVolume 固定为 `state=unavailable`；审批本身不会把 Volume 标为 ready。
        Enrollment 先进入 `approved`，只有后续认证 Agent session 和健康 probe 才能进入 `enrolled` 并
        使 Volume 成为 ready。公开响应不包含 bootstrap token、证书、AgentId 或 session identity。

        `approval_request_id` 是稳定 mutation identity，并与 `rejection_request_id` 共享 Tenant 级 decision
        request identity 命名空间。相同 ID 和 payload 幂等重放；跨审核动作复用、不同 payload、stale
        `expected_resource_version`、非 pending_approval 状态或 PVC 已绑定另一活动 Agent 均返回 409。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ApproveStorageEnrollmentRequest"
            example:
              tenant_id: tenant-a
              storage_enrollment_id: storage-enrollment-vision-01
              approval_request_id: approve-storage-enrollment-vision-01
              expected_resource_version: "1"
              confirm_replacement: false
      responses:
        "200":
          description: enrollment 已批准或相同审批被重放
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApproveStorageEnrollmentResponse"
              example:
                enrollment:
                  tenant_id: tenant-a
                  storage_enrollment_id: storage-enrollment-vision-01
                  storage_volume_id: volume-vision
                  display_name: Vision dataset PVC
                  edge_cluster_id: cluster-cn-east-1
                  region: cn-east-1
                  access_mode: read_write_many
                  pvc_reference:
                    namespace: neoengram-data
                    claim_name: vision-data
                  registration_kind: initial
                  state: approved
                  agent_version: 0.0.1
                  identity_fingerprint: aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
                  proof_of_possession_status: verified
                  probe:
                    observed_access_mode: read_write
                    descriptor_matches: true
                    observed_at_unix_ms: "1785168060000"
                  resource_version: "2"
                  created_at_unix_ms: "1785168060000"
                  expires_at_unix_ms: "1785254460000"
                  reviewed_at_unix_ms: "1785168120000"
                  updated_at_unix_ms: "1785168120000"
                storage_volume:
                  tenant_id: tenant-a
                  storage_volume_id: volume-vision
                  display_name: Vision dataset PVC
                  edge_cluster_id: cluster-cn-east-1
                  region: cn-east-1
                  backend_type: pvc
                  access_mode: read_write_many
                  allowed_delivery_modes: [fuse, copy]
                  hardlink_policy: disabled
                  max_whole_file_bytes: "107374182400"
                  copy_reserve_bytes: "10737418240"
                  pvc_reference:
                    namespace: neoengram-data
                    claim_name: vision-data
                  state: unavailable
                  resource_version: "1"
                  lifecycle:
                    state: active
                    generation: "1"
                  created_at_unix_ms: "1785168120000"
                  updated_at_unix_ms: "1785168120000"
                replayed: false
        "401":
          $ref: "#/components/responses/AuthenticationProblem"
        "403":
          $ref: "#/components/responses/AuthorizationProblem"
        "404":
          $ref: "#/components/responses/StorageEnrollmentNotFoundProblem"
        "409":
          $ref: "#/components/responses/StorageEnrollmentConflictProblem"
        "413":
          $ref: "#/components/responses/PayloadTooLargeProblem"
        "422":
          $ref: "#/components/responses/ValidationProblem"
        "429":
          $ref: "#/components/responses/OverloadedProblem"
        "500":
          $ref: "#/components/responses/InternalProblem"
        "503":
          $ref: "#/components/responses/ServiceUnavailableProblem"
        "504":
          $ref: "#/components/responses/RequestTimeoutProblem"

  /api/storage/enrollment/recovery/complete:
    post:
      tags: [StorageEnrollment]
      operationId: completeStorageRecovery
      summary: 完成 replacement Storage recovery
      description: |
        要求 `storage.enrollment.review` 权限。仅用于 replacement Agent 已建立认证 session 并报告
        Ready mount 后，显式完成 Volume recovery fence。服务端同时校验 enrollment resource version
        和当前 owner generation；旧 Owner、旧 generation、非 recovery 状态或尚未 Ready 的 mount
        均返回 409。该动作不会签发凭证，也不接受客户端路径或挂载信息。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CompleteStorageRecoveryRequest"
            example:
              tenant_id: tenant-a
              storage_enrollment_id: storage-enrollment-vision-replacement-01
              expected_resource_version: "4"
              owner_generation: "2"
      responses:
        "200":
          description: recovery fence 已由当前 Ready Owner 完成
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CompleteStorageRecoveryResponse"
        "401":
          $ref: "#/components/responses/AuthenticationProblem"
        "403":
          $ref: "#/components/responses/AuthorizationProblem"
        "404":
          $ref: "#/components/responses/StorageEnrollmentNotFoundProblem"
        "409":
          $ref: "#/components/responses/StorageEnrollmentConflictProblem"
        "413":
          $ref: "#/components/responses/PayloadTooLargeProblem"
        "422":
          $ref: "#/components/responses/ValidationProblem"
        "429":
          $ref: "#/components/responses/OverloadedProblem"
        "500":
          $ref: "#/components/responses/InternalProblem"
        "503":
          $ref: "#/components/responses/ServiceUnavailableProblem"
        "504":
          $ref: "#/components/responses/RequestTimeoutProblem"

  /api/storage/enrollment/reject:
    post:
      tags: [StorageEnrollment]
      operationId: rejectStorageEnrollment
      summary: 拒绝 Storage enrollment
      description: |
        要求 `storage.enrollment.review` 权限。仅允许拒绝未过期的 `pending_approval` enrollment；服务端使用
        `expected_resource_version` 执行 CAS 并记录认证 actor 的审计事件。拒绝是终态，不签发 Agent
        证书；0.0.1 永久退休该 candidate 的 installation identity 与公钥，再次申请需要初始化新的
        Agent 状态身份、密钥和 bootstrap token。

        可选 `reason` 仅写入认证 actor 的受控审计记录，不进入 StorageEnrollmentView、公开响应或日志。
        `rejection_request_id` 是稳定 mutation identity，并与 `approval_request_id` 共享 Tenant 级 decision
        request identity 命名空间。相同 ID 和 payload 幂等重放；跨审核动作复用、不同 payload、stale
        resource version 或非 pending_approval 状态返回 409。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RejectStorageEnrollmentRequest"
            example:
              tenant_id: tenant-a
              storage_enrollment_id: storage-enrollment-vision-01
              rejection_request_id: reject-storage-enrollment-vision-01
              expected_resource_version: "1"
              reason: PVC descriptor 与本次部署变更窗口不匹配
      responses:
        "200":
          description: enrollment 已拒绝或相同拒绝请求被重放
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RejectStorageEnrollmentResponse"
              example:
                enrollment:
                  tenant_id: tenant-a
                  storage_enrollment_id: storage-enrollment-vision-01
                  storage_volume_id: volume-vision
                  display_name: Vision dataset PVC
                  edge_cluster_id: cluster-cn-east-1
                  region: cn-east-1
                  access_mode: read_write_many
                  pvc_reference:
                    namespace: neoengram-data
                    claim_name: vision-data
                  registration_kind: initial
                  state: rejected
                  agent_version: 0.0.1
                  identity_fingerprint: aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
                  proof_of_possession_status: verified
                  probe:
                    observed_access_mode: read_write
                    descriptor_matches: false
                    observed_at_unix_ms: "1785168060000"
                  resource_version: "2"
                  created_at_unix_ms: "1785168060000"
                  expires_at_unix_ms: "1785254460000"
                  reviewed_at_unix_ms: "1785168120000"
                  updated_at_unix_ms: "1785168120000"
                replayed: false
        "401":
          $ref: "#/components/responses/AuthenticationProblem"
        "403":
          $ref: "#/components/responses/AuthorizationProblem"
        "404":
          $ref: "#/components/responses/StorageEnrollmentNotFoundProblem"
        "409":
          $ref: "#/components/responses/StorageEnrollmentConflictProblem"
        "413":
          $ref: "#/components/responses/PayloadTooLargeProblem"
        "422":
          $ref: "#/components/responses/ValidationProblem"
        "429":
          $ref: "#/components/responses/OverloadedProblem"
        "500":
          $ref: "#/components/responses/InternalProblem"
        "503":
          $ref: "#/components/responses/ServiceUnavailableProblem"
        "504":
          $ref: "#/components/responses/RequestTimeoutProblem"

  /api/project/create:
    post:
      tags: [Project]
      operationId: createProject
      x-neoengram-central-route: true
      summary: 创建 Project
      description: |
        要求 Tenant 内的 `project.create` 权限。相同 Tenant、Project ID 和 canonical 创建内容返回
        既有 Project 并标记 `replayed=true`；相同 Project ID 对应不同内容时返回冲突。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateProjectRequest"
            example:
              tenant_id: tenant-a
              project_id: project-lab
              display_name: 算法实验室
              description: 实验数据和结果
      responses:
        "200":
          description: Project 已创建或相同创建请求被重放
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateProjectResponse"
        "401":
          $ref: "#/components/responses/AuthenticationProblem"
        "403":
          $ref: "#/components/responses/AuthorizationProblem"
        "404":
          $ref: "#/components/responses/ResourceNotFoundProblem"
        "409":
          $ref: "#/components/responses/MutationConflictProblem"
        "413":
          $ref: "#/components/responses/PayloadTooLargeProblem"
        "422":
          $ref: "#/components/responses/ValidationProblem"
        "500":
          $ref: "#/components/responses/InternalProblem"
        "503":
          $ref: "#/components/responses/ServiceUnavailableProblem"

  /api/project/list/query:
    post:
      tags: [Project]
      operationId: queryProjectList
      x-neoengram-central-route: true
      summary: 查询租户内 Project
      description: 分页返回租户内当前 principal 可见的 Project，用于资源筛选。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QueryProjectListRequest"
            example:
              tenant_id: tenant-a
              page_size: 50
      responses:
        "200":
          description: Project 页
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueryProjectListResponse"
              example:
                items:
                  - tenant_id: tenant-a
                    project_id: project-vision
                    display_name: 视觉数据
                    resource_version: "2"
                    created_at_unix_ms: "1785167000000"
                    updated_at_unix_ms: "1785167600000"
        "401":
          $ref: "#/components/responses/AuthenticationProblem"
        "403":
          $ref: "#/components/responses/AuthorizationProblem"
        "404":
          $ref: "#/components/responses/ResourceNotFoundProblem"
        "413":
          $ref: "#/components/responses/PayloadTooLargeProblem"
        "422":
          $ref: "#/components/responses/ValidationProblem"
        "500":
          $ref: "#/components/responses/InternalProblem"
        "503":
          $ref: "#/components/responses/ServiceUnavailableProblem"

  /api/artifact/list/query:
    post:
      tags: [Artifact]
      operationId: queryArtifactList
      summary: 查询租户内 Artifact
      description: 按可选 Project 和关键字过滤，分页返回脱敏 Artifact 视图。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QueryArtifactListRequest"
            example:
              tenant_id: tenant-a
              project_id: project-vision
              page_size: 50
      responses:
        "200":
          description: Artifact 页
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueryArtifactListResponse"
              example:
                items:
                  - tenant_id: tenant-a
                    project_id: project-vision
                    artifact_id: road-scenes
                    display_name: 道路场景数据集
                    description: 训练与回归评估数据
                    initialization:
                      mode: empty
                    head_commit_id: aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
                    resource_version: "18"
                    lifecycle:
                      state: active
                      generation: "1"
                    created_at_unix_ms: "1785167000000"
                    updated_at_unix_ms: "1785167600000"
        "401":
          $ref: "#/components/responses/AuthenticationProblem"
        "403":
          $ref: "#/components/responses/AuthorizationProblem"
        "404":
          $ref: "#/components/responses/ResourceNotFoundProblem"
        "413":
          $ref: "#/components/responses/PayloadTooLargeProblem"
        "422":
          $ref: "#/components/responses/ValidationProblem"
        "500":
          $ref: "#/components/responses/InternalProblem"
        "503":
          $ref: "#/components/responses/ServiceUnavailableProblem"

  /api/artifact/query:
    post:
      tags: [Artifact]
      operationId: queryArtifact
      summary: 查询 Artifact 详情
      description: 按完整 tenant、project、artifact scope 查询公开 Artifact 元数据。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QueryArtifactRequest"
            example:
              tenant_id: tenant-a
              project_id: project-vision
              artifact_id: road-scenes
      responses:
        "200":
          description: Artifact 当前视图
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueryArtifactResponse"
              example:
                artifact:
                  tenant_id: tenant-a
                  project_id: project-vision
                  artifact_id: road-scenes
                  display_name: 道路场景数据集
                  description: 训练与回归评估数据
                  initialization:
                    mode: empty
                  head_commit_id: aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
                  resource_version: "18"
                  lifecycle:
                    state: active
                    generation: "1"
                  created_at_unix_ms: "1785167000000"
                  updated_at_unix_ms: "1785167600000"
        "401":
          $ref: "#/components/responses/AuthenticationProblem"
        "403":
          $ref: "#/components/responses/AuthorizationProblem"
        "404":
          $ref: "#/components/responses/ResourceNotFoundProblem"
        "413":
          $ref: "#/components/responses/PayloadTooLargeProblem"
        "422":
          $ref: "#/components/responses/ValidationProblem"
        "500":
          $ref: "#/components/responses/InternalProblem"
        "503":
          $ref: "#/components/responses/ServiceUnavailableProblem"

  /api/artifact/create:
    post:
      tags: [Artifact]
      operationId: createArtifact
      summary: 创建 Artifact
      description: |
        在指定 Tenant/Project 下创建空 Artifact。Artifact 本身不选择 StorageVolume 或 Region；
        `artifact_id` 在 Tenant 内唯一，Project 是其唯一归属。资源身份使相同请求可幂等重放。
        `derived` 初始化保留给 Commit authority 闭环，当前服务无法验证来源 Commit 归属时以
        `ARTIFACT_DERIVED_INITIALIZATION_UNSUPPORTED` 返回 409。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateArtifactRequest"
            example:
              tenant_id: tenant-a
              project_id: project-vision
              artifact_id: evaluation-set
              display_name: 评测数据集
              initialization:
                mode: empty
      responses:
        "200":
          description: 已创建或幂等返回的 Artifact
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateArtifactResponse"
        "401":
          $ref: "#/components/responses/AuthenticationProblem"
        "403":
          $ref: "#/components/responses/AuthorizationProblem"
        "404":
          $ref: "#/components/responses/ResourceNotFoundProblem"
        "409":
          $ref: "#/components/responses/MutationConflictProblem"
        "413":
          $ref: "#/components/responses/PayloadTooLargeProblem"
        "422":
          $ref: "#/components/responses/ValidationProblem"
        "500":
          $ref: "#/components/responses/InternalProblem"
        "503":
          $ref: "#/components/responses/ServiceUnavailableProblem"

  /api/commit/replicate:
    post:
      tags: [Placement]
      operationId: replicateCommit
      summary: 将 Commit 复制到目标 Volume
      description: 创建显式对象级复制任务；目标 PlacementSet 完整校验并发布前不可用于 Delivery。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateCommitReplicationRequest"
      responses:
        "200":
          description: 复制任务已排队或幂等重放
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateCommitReplicationResponse"
        "401": { $ref: "#/components/responses/AuthenticationProblem" }
        "403": { $ref: "#/components/responses/AuthorizationProblem" }
        "404": { $ref: "#/components/responses/ResourceNotFoundProblem" }
        "409": { $ref: "#/components/responses/MutationConflictProblem" }
        "422": { $ref: "#/components/responses/ValidationProblem" }
        "500": { $ref: "#/components/responses/InternalProblem" }
        "503": { $ref: "#/components/responses/ServiceUnavailableProblem" }

  /api/commit/replication/query:
    post:
      tags: [Placement]
      operationId: queryCommitReplication
      summary: 查询复制任务
      description: 查询单个 Commit 复制任务及其当前对象和字节进度。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QueryCommitReplicationRequest"
      responses:
        "200":
          description: 复制任务
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueryCommitReplicationResponse"
        "401": { $ref: "#/components/responses/AuthenticationProblem" }
        "403": { $ref: "#/components/responses/AuthorizationProblem" }
        "404": { $ref: "#/components/responses/ResourceNotFoundProblem" }
        "422": { $ref: "#/components/responses/ValidationProblem" }
        "500": { $ref: "#/components/responses/InternalProblem" }

  /api/commit/replication/ticket/query:
    post:
      tags: [Placement]
      operationId: queryCommitReplicationTicket
      summary: 查询复制传输票据
      description: 返回绑定 Tenant、Commit、ObjectSet、源/目标 Placement 和路由代次的短期传输票据。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QueryCommitReplicationTicketRequest"
      responses:
        "200":
          description: 短期传输票据
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueryCommitReplicationTicketResponse"
        "401": { $ref: "#/components/responses/AuthenticationProblem" }
        "403": { $ref: "#/components/responses/AuthorizationProblem" }
        "404": { $ref: "#/components/responses/ResourceNotFoundProblem" }
        "409": { $ref: "#/components/responses/MutationConflictProblem" }
        "422": { $ref: "#/components/responses/ValidationProblem" }
        "500": { $ref: "#/components/responses/InternalProblem" }
        "503": { $ref: "#/components/responses/ServiceUnavailableProblem" }

  /api/commit/replication/list/query:
    post:
      tags: [Placement]
      operationId: queryCommitReplicationList
      summary: 查询 Commit 的复制任务
      description: 查询指定 Commit 的全部复制任务及其目标副本状态。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QueryCommitReplicationListRequest"
      responses:
        "200":
          description: 复制任务列表
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueryCommitReplicationListResponse"
        "401": { $ref: "#/components/responses/AuthenticationProblem" }
        "403": { $ref: "#/components/responses/AuthorizationProblem" }
        "422": { $ref: "#/components/responses/ValidationProblem" }
        "500": { $ref: "#/components/responses/InternalProblem" }

  /api/commit/replication/retry:
    post:
      tags: [Placement]
      operationId: retryCommitReplication
      summary: 重试失败的 Commit 复制
      description: 使用当前 attempt fence 重新排队失败或取消的 Commit 复制任务。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RetryCommitReplicationRequest"
      responses:
        "200":
          description: 重试任务
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RetryCommitReplicationResponse"
        "401": { $ref: "#/components/responses/AuthenticationProblem" }
        "403": { $ref: "#/components/responses/AuthorizationProblem" }
        "409": { $ref: "#/components/responses/MutationConflictProblem" }
        "422": { $ref: "#/components/responses/ValidationProblem" }
        "500": { $ref: "#/components/responses/InternalProblem" }

  /api/commit/replication/cancel:
    post:
      tags: [Placement]
      operationId: cancelCommitReplication
      summary: 取消 Commit 复制
      description: 取消仍在进行中的 Commit 复制任务并保留临时数据供后续清理。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CancelCommitReplicationRequest"
      responses:
        "200":
          description: 取消任务
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CancelCommitReplicationResponse"
        "401": { $ref: "#/components/responses/AuthenticationProblem" }
        "403": { $ref: "#/components/responses/AuthorizationProblem" }
        "409": { $ref: "#/components/responses/MutationConflictProblem" }
        "422": { $ref: "#/components/responses/ValidationProblem" }
        "500": { $ref: "#/components/responses/InternalProblem" }

  /api/commit/placements/query:
    post:
      tags: [Placement]
      operationId: queryCommitPlacementList
      summary: 查询 Commit Placement 列表
      description: 查询 Commit 已发布、可用于读取的物理 PlacementSet 列表。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QueryCommitPlacementListRequest"
      responses:
        "200":
          description: Placement 列表
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueryCommitPlacementListResponse"
        "401": { $ref: "#/components/responses/AuthenticationProblem" }
        "403": { $ref: "#/components/responses/AuthorizationProblem" }
        "422": { $ref: "#/components/responses/ValidationProblem" }
        "500": { $ref: "#/components/responses/InternalProblem" }

  /api/commit/availability/query:
    post:
      tags: [Placement]
      operationId: queryCommitAvailability
      summary: 查询 Commit 数据健康
      description: 根据已发布 PlacementSet 计算 Commit 当前对象可用性。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QueryCommitAvailabilityRequest"
      responses:
        "200":
          description: Commit 健康状态
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueryCommitAvailabilityResponse"
        "401": { $ref: "#/components/responses/AuthenticationProblem" }
        "403": { $ref: "#/components/responses/AuthorizationProblem" }
        "404": { $ref: "#/components/responses/ResourceNotFoundProblem" }
        "422": { $ref: "#/components/responses/ValidationProblem" }
        "500": { $ref: "#/components/responses/InternalProblem" }

  /api/workspace/create:
    post:
      tags: [Placement]
      operationId: createWorkspace
      summary: 在目标 Volume 创建 Workspace
      description: Workspace 是唯一可写数据面；Central 自动从已发布 Placement 选择可用源。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateWorkspaceRequest"
      responses:
        "200":
          description: Workspace 已创建或幂等重放
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateWorkspaceResponse"
        "401": { $ref: "#/components/responses/AuthenticationProblem" }
        "403": { $ref: "#/components/responses/AuthorizationProblem" }
        "404": { $ref: "#/components/responses/ResourceNotFoundProblem" }
        "409": { $ref: "#/components/responses/MutationConflictProblem" }
        "422": { $ref: "#/components/responses/ValidationProblem" }
        "500": { $ref: "#/components/responses/InternalProblem" }
        "503": { $ref: "#/components/responses/ServiceUnavailableProblem" }

  /api/artifact/commit/graph/query:
    post:
      tags: [Artifact]
      operationId: queryArtifactCommitGraph
      summary: 查询 Artifact Commit 图
      description: |
        分页返回该 Artifact 的全部已发布单 parent Commit 节点和公开 Tags；客户端依据
        `parent_commit_id` 重建可分叉的版本树。`head_commit_id` 是默认基线的便捷指针，不限制
        图中可见的分支。Cursor 绑定 Artifact resource version、Head 与完整 scope；任一 Commit
        发布并推进版本后，旧 Cursor 返回 409。公开节点不包含 root directory、Manifest、Chunk
        或存储信息。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QueryArtifactCommitGraphRequest"
            example:
              tenant_id: tenant-a
              project_id: project-vision
              artifact_id: road-scenes
              page_size: 50
      responses:
        "200":
          description: Commit 图的一页
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueryArtifactCommitGraphResponse"
              example:
                graph:
                  graph_version: "18"
                  head_commit_id: aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
                  nodes:
                    - commit_id: aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
                      parent_commit_id: bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb
                      message: 补充夜间道路场景
                      tag_names: [dataset/v4, release-candidate]
                      data_layout: fast_cdc
                      created_at_unix_ms: "1785167600000"
                    - commit_id: bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb
                      parent_commit_id: cccccccccccccccccccccccccccccccccccccccccccccccccccccccccccccccc
                      message: 完成首轮质量复核
                      tag_names: [v1.0]
                      data_layout: whole_file
                      created_at_unix_ms: "1785067400000"
        "401":
          $ref: "#/components/responses/AuthenticationProblem"
        "403":
          $ref: "#/components/responses/AuthorizationProblem"
        "404":
          $ref: "#/components/responses/ResourceNotFoundProblem"
        "409":
          $ref: "#/components/responses/CursorConflictProblem"
        "413":
          $ref: "#/components/responses/PayloadTooLargeProblem"
        "422":
          $ref: "#/components/responses/ValidationProblem"
        "500":
          $ref: "#/components/responses/InternalProblem"
        "503":
          $ref: "#/components/responses/ServiceUnavailableProblem"

  /api/artifact/commit/diff/query:
    post:
      tags: [Artifact]
      operationId: queryArtifactCommitDiff
      x-neoengram-central-route: true
      summary: 查询 Commit 文件差异
      description: |
        比较目标 Commit 与指定 base Commit；未提供 `base_commit_id` 时使用目标 Commit 的单一
        parent，根 Commit 则与空基线比较。结果只返回公开路径、变更类型和逻辑大小统计，
        不暴露 Directory、Manifest、Chunk、digest 或物理对象位置。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QueryArtifactCommitDiffRequest"
            example:
              tenant_id: tenant-a
              project_id: project-vision
              artifact_id: road-scenes
              commit_id: aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
      responses:
        "200":
          description: 目标 Commit 与 base/parent Commit 的文件级差异
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueryArtifactCommitDiffResponse"
              example:
                diff:
                  base_commit:
                    commit_id: bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb
                    parent_commit_id: cccccccccccccccccccccccccccccccccccccccccccccccccccccccccccccccc
                    message: 完成首轮质量复核
                    description: 完成白天场景的质量抽检和标签修订。
                    tag_names: [v1.0]
                    data_layout: whole_file
                    created_at_unix_ms: "1785067400000"
                  target_commit:
                    commit_id: aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
                    parent_commit_id: bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb
                    message: 补充夜间道路场景
                    description: 增加夜间和低照度样本，并更新场景索引。
                    tag_names: [dataset/v4, release-candidate]
                    data_layout: fast_cdc
                    created_at_unix_ms: "1785167600000"
                  summary:
                    files_added: "1"
                    files_modified: "1"
                    files_deleted: "0"
                    files_renamed: "0"
                    bytes_added: "6144"
                    bytes_removed: "2048"
                  changes:
                    - change_type: modified
                      path: dataset/index.json
                      old_size_bytes: "2048"
                      new_size_bytes: "3072"
                    - change_type: added
                      path: dataset/night/manifest.jsonl
                      new_size_bytes: "5120"
        "401":
          $ref: "#/components/responses/AuthenticationProblem"
        "403":
          $ref: "#/components/responses/AuthorizationProblem"
        "404":
          $ref: "#/components/responses/ResourceNotFoundProblem"
        "413":
          $ref: "#/components/responses/PayloadTooLargeProblem"
        "422":
          $ref: "#/components/responses/ValidationProblem"
        "500":
          $ref: "#/components/responses/InternalProblem"
        "503":
          $ref: "#/components/responses/ServiceUnavailableProblem"

  /api/playground/list/query:
    post:
      tags: [Playground]
      operationId: queryPlaygroundList
      summary: 查询租户内 Playground
      description: |
        按 Project、Artifact、Region、主状态或关键字分页查询 Playground。Artifact 筛选必须同时
        提供 Project scope；opaque cursor 与完整筛选条件绑定。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QueryPlaygroundListRequest"
            example:
              tenant_id: tenant-a
              project_id: project-vision
              artifact_id: road-scenes
              state: ready
              page_size: 50
      responses:
        "200":
          description: Playground 页
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueryPlaygroundListResponse"
              example:
                items:
                  - tenant_id: tenant-a
                    project_id: project-vision
                    artifact_id: road-scenes
                    playground_id: labeling
                    storage_volume_id: volume-shanghai-vision
                    region: cn-shanghai
                    display_name: 标注工作区
                    base_commit_id: aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
                    head_commit_id: bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb
                    index_version:
                      revision: "31"
                      digest: aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
                    state: ready
                    storage_availability: ready
                    resource_version: "1"
                    lifecycle:
                      state: active
                      generation: "1"
                    created_at_unix_ms: "1785167000000"
                    updated_at_unix_ms: "1785167600000"
        "401":
          $ref: "#/components/responses/AuthenticationProblem"
        "403":
          $ref: "#/components/responses/AuthorizationProblem"
        "404":
          $ref: "#/components/responses/ResourceNotFoundProblem"
        "409":
          $ref: "#/components/responses/CursorConflictProblem"
        "413":
          $ref: "#/components/responses/PayloadTooLargeProblem"
        "422":
          $ref: "#/components/responses/ValidationProblem"
        "500":
          $ref: "#/components/responses/InternalProblem"
        "503":
          $ref: "#/components/responses/ServiceUnavailableProblem"

  /api/playground/query:
    post:
      tags: [Playground]
      operationId: queryPlayground
      summary: 查询 Playground 详情
      description: 按完整资源 scope 返回 Playground 的只读权威元数据。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QueryPlaygroundRequest"
            example:
              tenant_id: tenant-a
              project_id: project-vision
              artifact_id: road-scenes
              playground_id: labeling
      responses:
        "200":
          description: Playground 当前视图
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueryPlaygroundResponse"
              example:
                playground:
                  tenant_id: tenant-a
                  project_id: project-vision
                  artifact_id: road-scenes
                  playground_id: labeling
                  storage_volume_id: volume-shanghai-vision
                  region: cn-shanghai
                  display_name: 标注工作区
                  base_commit_id: aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
                  head_commit_id: bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb
                  index_version:
                    revision: "31"
                    digest: aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
                  state: ready
                  storage_availability: ready
                  resource_version: "1"
                  lifecycle:
                    state: active
                    generation: "1"
                  created_at_unix_ms: "1785167000000"
                  updated_at_unix_ms: "1785167600000"
        "401":
          $ref: "#/components/responses/AuthenticationProblem"
        "403":
          $ref: "#/components/responses/AuthorizationProblem"
        "404":
          $ref: "#/components/responses/ResourceNotFoundProblem"
        "413":
          $ref: "#/components/responses/PayloadTooLargeProblem"
        "422":
          $ref: "#/components/responses/ValidationProblem"
        "500":
          $ref: "#/components/responses/InternalProblem"
        "503":
          $ref: "#/components/responses/ServiceUnavailableProblem"

  /api/playground/create:
    post:
      tags: [Playground]
      operationId: createPlayground
      summary: 创建 Playground
      description: |
        在已存在的权威 Artifact 下创建受管可写 Playground。显式 `base_commit_id` 可以选择当前
        Head 或该 Artifact 已发布历史中的任一 Commit；未被任何 Head 可达、也未确认的 Commit 返回
        404，省略时服务端冻结创建瞬间的当前 Head。所选 Commit 的权威 Index 会先初始化为
        Playground Index，再由目标 StorageVolume 的 Agent 异步物化；完成前 Playground 保持
        `state=creating`。所选 StorageVolume 必须属于同一 Tenant 且 `state=ready`；`degraded` 或
        `unavailable` 均拒绝新放置并返回 409。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreatePlaygroundRequest"
            example:
              tenant_id: tenant-a
              project_id: project-vision
              artifact_id: road-scenes
              playground_id: review-july
              storage_volume_id: volume-shanghai-vision
              display_name: 七月复核
              base_commit_id: bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb
      responses:
        "200":
          description: 已创建或幂等返回的 Playground
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreatePlaygroundResponse"
        "401":
          $ref: "#/components/responses/AuthenticationProblem"
        "403":
          $ref: "#/components/responses/AuthorizationProblem"
        "404":
          $ref: "#/components/responses/ResourceNotFoundProblem"
        "409":
          $ref: "#/components/responses/MutationConflictProblem"
        "413":
          $ref: "#/components/responses/PayloadTooLargeProblem"
        "422":
          $ref: "#/components/responses/ValidationProblem"
        "500":
          $ref: "#/components/responses/InternalProblem"
        "503":
          $ref: "#/components/responses/ServiceUnavailableProblem"

  /api/playground/precommit/start:
    post:
      tags: [Playground]
      operationId: startPlaygroundPreCommit
      summary: 发起 Playground Pre-commit
      description: |
        为 Ready Playground 的当前 IndexVersion 创建新的冻结候选会话。服务端生成新的
        `precommit_id`，并在会话内部冻结当前 Head 供 Commit CAS 使用；Head 不作为公开请求或响应
        字段。已有 running/ready active Pre-commit 时必须先 cancel，不得用 start 覆盖。相同
        `precommit_request_id` 和 payload 返回原结果，不同 payload 返回 409。200 返回时 Pre-commit
        已持久化且可查询，扫描、哈希、上传和校验通过其状态、阶段与进度异步推进。页面查询或刷新
        不得隐式调用本方法。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StartPreCommitRequest"
            example:
              tenant_id: tenant-a
              project_id: project-vision
              artifact_id: road-scenes
              playground_id: labeling
              precommit_request_id: precommit-request-july-labels
              expected_index_version:
                revision: "31"
                digest: aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
              data_layout: fast_cdc
      responses:
        "200":
          description: 已创建或幂等返回的 Pre-commit
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StartPreCommitResponse"
              example:
                precommit:
                  tenant_id: tenant-a
                  project_id: project-vision
                  artifact_id: road-scenes
                  playground_id: labeling
                  precommit_id: precommit-july-labels-01
                  precommit_request_id: precommit-request-july-labels
                  data_layout: fast_cdc
                  attempt: 1
                  state: running
                  phase: queued
                  progress:
                    percent: 0
                    files_completed: "0"
                    bytes_completed: "0"
                  checks: []
                  warnings: []
                  blockers: []
                  source_index_version:
                    revision: "31"
                    digest: aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
                  created_at_unix_ms: "1785167600000"
                  updated_at_unix_ms: "1785167600000"
                playground:
                  tenant_id: tenant-a
                  project_id: project-vision
                  artifact_id: road-scenes
                  playground_id: labeling
                  storage_volume_id: volume-shanghai-vision
                  region: cn-shanghai
                  display_name: 标注工作区
                  base_commit_id: aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
                  head_commit_id: bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb
                  index_version:
                    revision: "31"
                    digest: aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
                  state: ready
                  storage_availability: ready
                  active_precommit_id: precommit-july-labels-01
                  resource_version: "1"
                  lifecycle:
                    state: active
                    generation: "1"
                  created_at_unix_ms: "1785167000000"
                  updated_at_unix_ms: "1785167600000"
                replayed: false
        "401":
          $ref: "#/components/responses/AuthenticationProblem"
        "403":
          $ref: "#/components/responses/AuthorizationProblem"
        "404":
          $ref: "#/components/responses/ResourceNotFoundProblem"
        "409":
          $ref: "#/components/responses/MutationConflictProblem"
        "413":
          $ref: "#/components/responses/PayloadTooLargeProblem"
        "422":
          $ref: "#/components/responses/ValidationProblem"
        "500":
          $ref: "#/components/responses/InternalProblem"
        "503":
          $ref: "#/components/responses/ServiceUnavailableProblem"

  /api/playground/precommit/query:
    post:
      tags: [Playground]
      operationId: queryPlaygroundPreCommit
      summary: 查询 Playground Pre-commit
      description: |
        使用 Tenant 和服务端生成的 Pre-commit ID 查询冻结候选、检查和 Diff 摘要。完成且可提交的
        结果是 `state=ready, phase=idle`；有阻断项的结果是 `state=abnormal, phase=idle` 且
        `blockers` 非空。`ready` 是 state，不是 phase。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QueryPreCommitRequest"
            example:
              tenant_id: tenant-a
              precommit_id: precommit-july-labels-01
      responses:
        "200":
          description: Pre-commit 当前视图
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueryPreCommitResponse"
              example:
                precommit:
                  tenant_id: tenant-a
                  project_id: project-vision
                  artifact_id: road-scenes
                  playground_id: labeling
                  precommit_id: precommit-july-labels-01
                  precommit_request_id: precommit-request-july-labels
                  data_layout: fast_cdc
                  attempt: 1
                  state: ready
                  phase: idle
                  progress:
                    percent: 100
                    files_completed: "864"
                    files_total: "864"
                    bytes_completed: "12884901888"
                    bytes_total: "12884901888"
                  checks:
                    - check_id: metadata-shape
                      status: passed
                      summary: Metadata 和逻辑路径检查通过
                  warnings: []
                  blockers: []
                  source_index_version:
                    revision: "31"
                    digest: aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
                  candidate_index_version:
                    revision: "31"
                    digest: aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
                  diff_summary:
                    files_added: "1"
                    files_modified: "1"
                    files_deleted: "0"
                    files_renamed: "0"
                    bytes_added: "6144"
                    bytes_removed: "2048"
                  created_at_unix_ms: "1785167600000"
                  updated_at_unix_ms: "1785167700000"
        "401":
          $ref: "#/components/responses/AuthenticationProblem"
        "403":
          $ref: "#/components/responses/AuthorizationProblem"
        "404":
          $ref: "#/components/responses/ResourceNotFoundProblem"
        "413":
          $ref: "#/components/responses/PayloadTooLargeProblem"
        "422":
          $ref: "#/components/responses/ValidationProblem"
        "500":
          $ref: "#/components/responses/InternalProblem"
        "503":
          $ref: "#/components/responses/ServiceUnavailableProblem"

  /api/playground/precommit/restart:
    post:
      tags: [Playground]
      operationId: restartPlaygroundPreCommit
      summary: 重试失败或已取消的 Playground Pre-commit
      description: |
        仅允许对 `abnormal` 或 `cancelled` Pre-commit 在同一 ID 上创建新 attempt，使 `attempt + 1`，
        并冻结请求给定的当前 IndexVersion 与服务端当前 Head。Head 仍仅保存在内部。running/ready
        会话的“重新检测”必须先 cancel，再调用 start 创建新的 `precommit_id`，不能调用 restart。
        相同 `restart_request_id` 和 payload 幂等返回，状态或版本不匹配返回 409。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RestartPreCommitRequest"
            example:
              tenant_id: tenant-a
              precommit_id: precommit-july-labels-01
              restart_request_id: restart-july-labels-02
              expected_index_version:
                revision: "32"
                digest: bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb
      responses:
        "200":
          description: 已重新发起或幂等返回的 Pre-commit
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RestartPreCommitResponse"
        "401":
          $ref: "#/components/responses/AuthenticationProblem"
        "403":
          $ref: "#/components/responses/AuthorizationProblem"
        "404":
          $ref: "#/components/responses/ResourceNotFoundProblem"
        "409":
          $ref: "#/components/responses/MutationConflictProblem"
        "413":
          $ref: "#/components/responses/PayloadTooLargeProblem"
        "422":
          $ref: "#/components/responses/ValidationProblem"
        "500":
          $ref: "#/components/responses/InternalProblem"
        "503":
          $ref: "#/components/responses/ServiceUnavailableProblem"

  /api/playground/precommit/cancel:
    post:
      tags: [Playground]
      operationId: cancelPlaygroundPreCommit
      summary: 取消 Playground Pre-commit
      description: |
        取消尚未提交的 Pre-commit 并清除 Playground 的 active Pre-commit。相同
        `cancel_request_id` 和 payload 幂等返回；已提交或状态不允许取消时返回 409。取消后的会话
        为 `state=cancelled, phase=idle`，旧候选不能被 Commit 消费。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CancelPreCommitRequest"
            example:
              tenant_id: tenant-a
              precommit_id: precommit-july-labels-01
              cancel_request_id: cancel-july-labels-01
      responses:
        "200":
          description: 已取消或幂等返回的 Pre-commit
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CancelPreCommitResponse"
        "401":
          $ref: "#/components/responses/AuthenticationProblem"
        "403":
          $ref: "#/components/responses/AuthorizationProblem"
        "404":
          $ref: "#/components/responses/ResourceNotFoundProblem"
        "409":
          $ref: "#/components/responses/MutationConflictProblem"
        "413":
          $ref: "#/components/responses/PayloadTooLargeProblem"
        "422":
          $ref: "#/components/responses/ValidationProblem"
        "500":
          $ref: "#/components/responses/InternalProblem"
        "503":
          $ref: "#/components/responses/ServiceUnavailableProblem"

  /api/playground/file/list/query:
    post:
      tags: [Playground]
      operationId: queryPlaygroundFileList
      summary: 查询 Playground 文件列表
      description: |
        分页返回当前工作区 IndexVersion 的逻辑文件和目录。响应不包含 Manifest ID、内容摘要、
        对象位置、凭据或物理路径；opaque cursor 与资源 scope、IndexVersion 和筛选条件绑定。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QueryPlaygroundFileListRequest"
            example:
              tenant_id: tenant-a
              project_id: project-vision
              artifact_id: road-scenes
              playground_id: labeling
              path_prefix: dataset/night-rain
              format: parquet
              page_size: 50
      responses:
        "200":
          description: Playground 逻辑文件页
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueryPlaygroundFileListResponse"
              example:
                index_version:
                  revision: "31"
                  digest: aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
                items:
                  - path: dataset/night-rain/part-0042.parquet
                    entry_type: file
                    size_bytes: "19971597926"
                    format: parquet
                    row_count: "12842731"
                    updated_at_unix_ms: "1785167500000"
        "401":
          $ref: "#/components/responses/AuthenticationProblem"
        "403":
          $ref: "#/components/responses/AuthorizationProblem"
        "404":
          $ref: "#/components/responses/ResourceNotFoundProblem"
        "409":
          $ref: "#/components/responses/CursorConflictProblem"
        "413":
          $ref: "#/components/responses/PayloadTooLargeProblem"
        "422":
          $ref: "#/components/responses/ValidationProblem"
        "500":
          $ref: "#/components/responses/InternalProblem"
        "503":
          $ref: "#/components/responses/ServiceUnavailableProblem"

  /api/playground/change/list/query:
    post:
      tags: [Playground]
      operationId: queryPlaygroundChangeList
      summary: 查询 Playground 文件变更
      description: |
        未提供 `precommit_id` 时分页查询当前工作区相对父 Commit 的变更；提供时查询该
        Pre-commit 的冻结候选。响应只包含逻辑路径、变更类型、格式和大小统计。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QueryPlaygroundChangeListRequest"
            example:
              tenant_id: tenant-a
              project_id: project-vision
              artifact_id: road-scenes
              playground_id: labeling
              precommit_id: precommit-july-labels-01
              page_size: 50
      responses:
        "200":
          description: 当前工作区或冻结候选的文件变更页
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueryPlaygroundChangeListResponse"
              example:
                source: precommit
                precommit_id: precommit-july-labels-01
                index_version:
                  revision: "31"
                  digest: aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
                summary:
                  files_added: "1"
                  files_modified: "1"
                  files_deleted: "0"
                  files_renamed: "0"
                  bytes_added: "6144"
                  bytes_removed: "2048"
                items:
                  - change_type: modified
                    path: dataset/index.json
                    old_size_bytes: "2048"
                    new_size_bytes: "3072"
                    format: json
                  - change_type: added
                    path: dataset/night-rain/part-0042.parquet
                    new_size_bytes: "5120"
                    format: parquet
        "401":
          $ref: "#/components/responses/AuthenticationProblem"
        "403":
          $ref: "#/components/responses/AuthorizationProblem"
        "404":
          $ref: "#/components/responses/ResourceNotFoundProblem"
        "409":
          $ref: "#/components/responses/CursorConflictProblem"
        "413":
          $ref: "#/components/responses/PayloadTooLargeProblem"
        "422":
          $ref: "#/components/responses/ValidationProblem"
        "500":
          $ref: "#/components/responses/InternalProblem"
        "503":
          $ref: "#/components/responses/ServiceUnavailableProblem"

  /api/playground/file/metadata/query:
    post:
      tags: [Playground]
      operationId: queryPlaygroundFileMetadata
      summary: 查询 Playground 文件元数据
      description: |
        返回当前工作区中一个逻辑文件的大小、格式、Schema、统计、质量和 freshness；不返回
        Manifest、Chunk、对象分布、凭据或底层文件系统路径。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QueryPlaygroundFileMetadataRequest"
            example:
              tenant_id: tenant-a
              project_id: project-vision
              artifact_id: road-scenes
              playground_id: labeling
              path: dataset/night-rain/part-0042.parquet
      responses:
        "200":
          description: 脱敏逻辑文件元数据
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueryPlaygroundFileMetadataResponse"
              example:
                index_version:
                  revision: "31"
                  digest: aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
                metadata:
                  path: dataset/night-rain/part-0042.parquet
                  size_bytes: "19971597926"
                  format: parquet
                  media_type: application/vnd.apache.parquet
                  row_count: "12842731"
                  schema:
                    fields:
                      - name: image_id
                        data_type: string
                        nullable: false
                      - name: weather
                        data_type: string
                        nullable: true
                  statistics:
                    row_count: "12842731"
                    column_count: 18
                    null_value_count: "1024"
                  quality:
                    state: warning
                    checks_total: 12
                    checks_passed: 11
                    checks_failed: 1
                  freshness:
                    observed_at_unix_ms: "1785167600000"
                    source_updated_at_unix_ms: "1785167400000"
                    age_seconds: "200"
        "401":
          $ref: "#/components/responses/AuthenticationProblem"
        "403":
          $ref: "#/components/responses/AuthorizationProblem"
        "404":
          $ref: "#/components/responses/ResourceNotFoundProblem"
        "413":
          $ref: "#/components/responses/PayloadTooLargeProblem"
        "422":
          $ref: "#/components/responses/ValidationProblem"
        "500":
          $ref: "#/components/responses/InternalProblem"
        "503":
          $ref: "#/components/responses/ServiceUnavailableProblem"

  /api/playground/dataset/profile/query:
    post:
      tags: [Playground]
      operationId: queryPlaygroundDatasetProfile
      summary: 查询 Playground Dataset Profile
      description: 返回当前工作区 IndexVersion 的公开数据集画像、Schema、质量和 freshness 摘要。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QueryPlaygroundDatasetProfileRequest"
            example:
              tenant_id: tenant-a
              project_id: project-vision
              artifact_id: road-scenes
              playground_id: labeling
      responses:
        "200":
          description: Playground Dataset Profile
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueryPlaygroundDatasetProfileResponse"
              example:
                index_version:
                  revision: "31"
                  digest: aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
                profile:
                  state: ready
                  summary:
                    format_count: 4
                    logical_file_count: "864"
                    logical_size_bytes: "12884901888"
                    row_count: "18409216"
                    field_count: 18
                  quality:
                    state: warning
                    checks_total: 12
                    checks_passed: 11
                    checks_failed: 1
                  freshness:
                    observed_at_unix_ms: "1785167600000"
                    age_seconds: "200"
        "401":
          $ref: "#/components/responses/AuthenticationProblem"
        "403":
          $ref: "#/components/responses/AuthorizationProblem"
        "404":
          $ref: "#/components/responses/ResourceNotFoundProblem"
        "413":
          $ref: "#/components/responses/PayloadTooLargeProblem"
        "422":
          $ref: "#/components/responses/ValidationProblem"
        "500":
          $ref: "#/components/responses/InternalProblem"
        "503":
          $ref: "#/components/responses/ServiceUnavailableProblem"

  /api/playground/commit/create:
    post:
      tags: [Playground]
      operationId: commitPlayground
      summary: 提交 Playground
      description: |
        消费属于该 Playground、`state=ready, phase=idle`、无 blockers 且候选 IndexVersion 匹配的
        Pre-commit，创建不可变 Commit，并把 Pre-commit 标记为 Committed。服务端必须同时校验
        Playground 当前 Head 与该 Pre-commit attempt 内部冻结的 Head，并对内部版本指针执行 CAS；
        Head 变化返回 409，公开请求不增加 `source_head_commit_id`。`commit_request_id` 是稳定
        mutation identity；200 返回时 Commit、Playground 和已消费 Pre-commit 均已持久化且可查询。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CommitPlaygroundRequest"
            example:
              tenant_id: tenant-a
              project_id: project-vision
              artifact_id: road-scenes
              playground_id: labeling
              commit_request_id: commit-request-july-labels
              precommit_id: precommit-july-labels-01
              expected_candidate_index_version:
                revision: "31"
                digest: aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
              data_layout: fast_cdc
              message: 完成七月标注复核
              description: 完成夜间场景的人工复核并冻结训练候选。
              tag_names: [dataset/v5, reviewed]
      responses:
        "200":
          description: 已创建或幂等返回的 Commit
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CommitPlaygroundResponse"
        "401":
          $ref: "#/components/responses/AuthenticationProblem"
        "403":
          $ref: "#/components/responses/AuthorizationProblem"
        "404":
          $ref: "#/components/responses/ResourceNotFoundProblem"
        "409":
          $ref: "#/components/responses/MutationConflictProblem"
        "413":
          $ref: "#/components/responses/PayloadTooLargeProblem"
        "422":
          $ref: "#/components/responses/ValidationProblem"
        "500":
          $ref: "#/components/responses/InternalProblem"
        "503":
          $ref: "#/components/responses/ServiceUnavailableProblem"

  /api/snapshot/list/query:
    post:
      tags: [Snapshot]
      operationId: querySnapshotList
      summary: 查询租户内 Snapshot
      description: |
        按 Project、Artifact、Commit 或状态分页查询独立逻辑 Snapshot；物理副本由 Placement
        动态解析，不作为 Snapshot 筛选条件。
        opaque cursor 与全部筛选条件绑定。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QuerySnapshotListRequest"
            example:
              tenant_id: tenant-a
              project_id: project-vision
              artifact_id: road-scenes
              state: ready
              page_size: 50
      responses:
        "200":
          description: Snapshot 页
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuerySnapshotListResponse"
              example:
                items:
                  - snapshot_id: snap-road-main3-sha-01
                    tenant_id: tenant-a
                    project_id: project-vision
                    artifact_id: road-scenes
                    commit_id: aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
                    data_layout: fast_cdc
                    message: 补充夜间道路场景
                    tag_names: [dataset/v4, release-candidate]
                    state: ready
                    data_health: available
                    integrity:
                      state: verified
                      files_verified: "864"
                      bytes_verified: "12884901888"
                      verified_at_unix_ms: "1785167700000"
                    resource_version: "1"
                    lifecycle:
                      state: active
                      generation: "1"
                    created_at_unix_ms: "1785167600000"
                    updated_at_unix_ms: "1785167700000"
                    logical_file_count: "864"
                    logical_size_bytes: "12884901888"
        "401":
          $ref: "#/components/responses/AuthenticationProblem"
        "403":
          $ref: "#/components/responses/AuthorizationProblem"
        "404":
          $ref: "#/components/responses/ResourceNotFoundProblem"
        "409":
          $ref: "#/components/responses/CursorConflictProblem"
        "413":
          $ref: "#/components/responses/PayloadTooLargeProblem"
        "422":
          $ref: "#/components/responses/ValidationProblem"
        "500":
          $ref: "#/components/responses/InternalProblem"
        "503":
          $ref: "#/components/responses/ServiceUnavailableProblem"

  /api/snapshot/query:
    post:
      tags: [Snapshot]
      operationId: querySnapshot
      summary: 查询 Snapshot 详情
      description: 使用 Tenant 和服务端生成的独立 Snapshot ID 查询脱敏交付详情。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QuerySnapshotRequest"
            example:
              tenant_id: tenant-a
              snapshot_id: snap-road-main3-sha-01
      responses:
        "200":
          description: Snapshot 当前视图
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuerySnapshotResponse"
              example:
                snapshot:
                  snapshot_id: snap-road-main3-sha-01
                  tenant_id: tenant-a
                  project_id: project-vision
                  artifact_id: road-scenes
                  commit_id: aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
                  data_layout: fast_cdc
                  message: 补充夜间道路场景
                  tag_names: [dataset/v4, release-candidate]
                  state: ready
                  data_health: available
                  integrity:
                    state: verified
                    files_verified: "864"
                    bytes_verified: "12884901888"
                    verified_at_unix_ms: "1785167700000"
                  resource_version: "1"
                  lifecycle:
                    state: active
                    generation: "1"
                  created_at_unix_ms: "1785167600000"
                  updated_at_unix_ms: "1785167700000"
                  logical_file_count: "864"
                  logical_size_bytes: "12884901888"
        "401":
          $ref: "#/components/responses/AuthenticationProblem"
        "403":
          $ref: "#/components/responses/AuthorizationProblem"
        "404":
          $ref: "#/components/responses/ResourceNotFoundProblem"
        "413":
          $ref: "#/components/responses/PayloadTooLargeProblem"
        "422":
          $ref: "#/components/responses/ValidationProblem"
        "500":
          $ref: "#/components/responses/InternalProblem"
        "503":
          $ref: "#/components/responses/ServiceUnavailableProblem"

  /api/snapshot/create:
    post:
      tags: [Snapshot]
      operationId: createSnapshot
      summary: 创建 Snapshot
      description: |
        为指定 Artifact 的固定 Commit 创建逻辑 Snapshot。Snapshot 不绑定 StorageVolume 或 Region；
        对象副本由 Placement authority 管理，复制与只读交付是独立动作。相同 `request_id` 和 payload
        返回 `replayed: true`。200 返回时
        Snapshot 记录已持久化且可查询，具体物化进度由独立 SnapshotDelivery 表达。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateSnapshotRequest"
            example:
              tenant_id: tenant-a
              project_id: project-vision
              artifact_id: road-scenes
              commit_id: aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
              request_id: snapshot-request-main3-shanghai
      responses:
        "200":
          description: 已创建或幂等返回的 Snapshot
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateSnapshotResponse"
              example:
                snapshot:
                  snapshot_id: snap-road-main3-sha-01
                  tenant_id: tenant-a
                  project_id: project-vision
                  artifact_id: road-scenes
                  commit_id: aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
                  data_layout: fast_cdc
                  message: 补充夜间道路场景
                  tag_names: [dataset/v4, release-candidate]
                  state: creating
                  data_health: unavailable
                  integrity:
                    state: pending
                    files_verified: "0"
                    bytes_verified: "0"
                  resource_version: "1"
                  lifecycle:
                    state: active
                    generation: "1"
                  logical_file_count: "864"
                  logical_size_bytes: "12884901888"
                  created_at_unix_ms: "1785167600000"
                  updated_at_unix_ms: "1785167600000"
                replayed: false
        "401":
          $ref: "#/components/responses/AuthenticationProblem"
        "403":
          $ref: "#/components/responses/AuthorizationProblem"
        "404":
          $ref: "#/components/responses/ResourceNotFoundProblem"
        "409":
          $ref: "#/components/responses/MutationConflictProblem"
        "413":
          $ref: "#/components/responses/PayloadTooLargeProblem"
        "422":
          $ref: "#/components/responses/ValidationProblem"
        "500":
          $ref: "#/components/responses/InternalProblem"
        "503":
          $ref: "#/components/responses/ServiceUnavailableProblem"

  /api/snapshot/delivery/retry:
    post:
      tags: [Snapshot]
      operationId: retrySnapshotDelivery
      summary: 重试 Snapshot 交付
      description: |
        对 failed SnapshotDelivery 的同一固定 Snapshot/Commit/Volume 交付重新发起 attempt，
        使 Delivery 状态回到 requested，Snapshot 状态保持不变。相同 `request_id` 和 payload
        幂等返回；状态不允许重试时返回 409。200 返回时新的交付 attempt 已持久化，
        后续进度通过 SnapshotDelivery 查询。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RetrySnapshotDeliveryRequest"
            example:
              tenant_id: tenant-a
              delivery_id: delivery-road-main2-fuse-01
              request_id: retry-road-main2-fuse-02
      responses:
        "200":
          description: 已重新发起或幂等返回的 SnapshotDelivery
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RetrySnapshotDeliveryResponse"
              example:
                delivery:
                  delivery_id: delivery-road-main2-fuse-01
                  snapshot_id: snap-road-main2-sha-01
                  commit_id: bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb
                  storage_volume_id: volume-shanghai-vision
                  mode: fuse
                  target_relative_root: deliveries/delivery-road-main2-fuse-01
                  state: requested
                  source_index_digest: aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
                  delivery_generation: "2"
                  file_count: "820"
                  size_bytes: "11884901888"
                  object_set_digest: bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb
                  resource_version: "2"
                  created_at_unix_ms: "1785067400000"
                  updated_at_unix_ms: "1785167800000"
                replayed: false
        "401":
          $ref: "#/components/responses/AuthenticationProblem"
        "403":
          $ref: "#/components/responses/AuthorizationProblem"
        "404":
          $ref: "#/components/responses/ResourceNotFoundProblem"
        "409":
          $ref: "#/components/responses/MutationConflictProblem"
        "413":
          $ref: "#/components/responses/PayloadTooLargeProblem"
        "422":
          $ref: "#/components/responses/ValidationProblem"
        "500":
          $ref: "#/components/responses/InternalProblem"
        "503":
          $ref: "#/components/responses/ServiceUnavailableProblem"

  /api/snapshot/delivery/create:
    post:
      tags: [Snapshot]
      operationId: createSnapshotDelivery
      summary: 创建 Snapshot 只读交付
      description: 为固定 Snapshot 创建 FUSE、Copy 或 Hardlink 只读交付，目标路径由服务端推导。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateSnapshotDeliveryRequest"
      responses:
        "200":
          description: 已创建或幂等返回的交付
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateSnapshotDeliveryResponse"
        "401": { $ref: "#/components/responses/AuthenticationProblem" }
        "403": { $ref: "#/components/responses/AuthorizationProblem" }
        "404": { $ref: "#/components/responses/ResourceNotFoundProblem" }
        "409": { $ref: "#/components/responses/MutationConflictProblem" }
        "422": { $ref: "#/components/responses/ValidationProblem" }
        "503": { $ref: "#/components/responses/ServiceUnavailableProblem" }

  /api/snapshot/delivery/query:
    post:
      tags: [Snapshot]
      operationId: querySnapshotDelivery
      summary: 查询 Snapshot 只读交付
      description: 返回单个 SnapshotDelivery 的状态、交付模式和物化进度。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QuerySnapshotDeliveryRequest"
      responses:
        "200":
          description: 交付详情
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuerySnapshotDeliveryResponse"
        "401": { $ref: "#/components/responses/AuthenticationProblem" }
        "403": { $ref: "#/components/responses/AuthorizationProblem" }
        "404": { $ref: "#/components/responses/ResourceNotFoundProblem" }

  /api/snapshot/delivery/list/query:
    post:
      tags: [Snapshot]
      operationId: querySnapshotDeliveryList
      summary: 查询 Snapshot 只读交付列表
      description: 分页返回指定 Snapshot 的所有只读交付。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QuerySnapshotDeliveryListRequest"
      responses:
        "200":
          description: 交付列表
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuerySnapshotDeliveryListResponse"
        "401": { $ref: "#/components/responses/AuthenticationProblem" }
        "403": { $ref: "#/components/responses/AuthorizationProblem" }
        "404": { $ref: "#/components/responses/ResourceNotFoundProblem" }
        "409": { $ref: "#/components/responses/CursorConflictProblem" }

  /api/snapshot/delivery/delete:
    post:
      tags: [Snapshot]
      operationId: deleteSnapshotDelivery
      summary: 删除 Snapshot 只读交付
      description: |
        将指定 SnapshotDelivery 标记为 `deleting`，并异步卸载 FUSE 或清理 Copy/Hardlink 交付。
        200 只表示删除流程已持久化；最终 `deleted` 状态通过查询接口获取，相同 `request_id` 和 payload
        幂等返回同一结果。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DeleteSnapshotDeliveryRequest"
      responses:
        "200":
          description: 已进入异步删除流程或幂等返回的交付
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeleteSnapshotDeliveryResponse"
        "401": { $ref: "#/components/responses/AuthenticationProblem" }
        "403": { $ref: "#/components/responses/AuthorizationProblem" }
        "404": { $ref: "#/components/responses/ResourceNotFoundProblem" }
        "409": { $ref: "#/components/responses/MutationConflictProblem" }

  /api/snapshot/file/list/query:
    post:
      tags: [Snapshot]
      operationId: querySnapshotFileList
      x-neoengram-central-route: false
      summary: 查询 Snapshot 文件列表
      description: |
        分页返回 Ready Snapshot 的逻辑文件和目录。Creating 或 Abnormal 状态返回 409；响应不包含
        Manifest、内容摘要、对象位置、凭据或物理路径，opaque cursor 与筛选条件绑定。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QuerySnapshotFileListRequest"
            example:
              tenant_id: tenant-a
              snapshot_id: snap-road-main3-sha-01
              path_prefix: dataset/night-rain
              format: parquet
              page_size: 50
      responses:
        "200":
          description: Snapshot 逻辑文件页
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuerySnapshotFileListResponse"
              example:
                items:
                  - path: dataset/night-rain/part-0042.parquet
                    entry_type: file
                    size_bytes: "19971597926"
                    format: parquet
                    row_count: "12842731"
                    updated_at_unix_ms: "1785167500000"
        "401":
          $ref: "#/components/responses/AuthenticationProblem"
        "403":
          $ref: "#/components/responses/AuthorizationProblem"
        "404":
          $ref: "#/components/responses/ResourceNotFoundProblem"
        "409":
          $ref: "#/components/responses/MutationConflictProblem"
        "413":
          $ref: "#/components/responses/PayloadTooLargeProblem"
        "422":
          $ref: "#/components/responses/ValidationProblem"
        "500":
          $ref: "#/components/responses/InternalProblem"
        "503":
          $ref: "#/components/responses/ServiceUnavailableProblem"

  /api/snapshot/activity/list/query:
    post:
      tags: [Snapshot]
      operationId: querySnapshotActivityList
      x-neoengram-central-route: false
      summary: 查询 Snapshot 活动
      description: 分页返回 Snapshot 创建、状态变化、失败和就绪的脱敏活动记录；Delivery 重试不改变 Snapshot 活动。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QuerySnapshotActivityListRequest"
            example:
              tenant_id: tenant-a
              snapshot_id: snap-road-main3-sha-01
              page_size: 50
      responses:
        "200":
          description: Snapshot 活动页
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuerySnapshotActivityListResponse"
              example:
                items:
                  - activity_id: activity-snap-road-main3-ready
                    activity_type: ready
                    summary: Snapshot 已完成完整性校验并可读取
                    created_at_unix_ms: "1785167700000"
                  - activity_id: activity-snap-road-main3-created
                    activity_type: created
                    summary: Snapshot 记录已创建
                    created_at_unix_ms: "1785167600000"
        "401":
          $ref: "#/components/responses/AuthenticationProblem"
        "403":
          $ref: "#/components/responses/AuthorizationProblem"
        "404":
          $ref: "#/components/responses/ResourceNotFoundProblem"
        "409":
          $ref: "#/components/responses/CursorConflictProblem"
        "413":
          $ref: "#/components/responses/PayloadTooLargeProblem"
        "422":
          $ref: "#/components/responses/ValidationProblem"
        "500":
          $ref: "#/components/responses/InternalProblem"
        "503":
          $ref: "#/components/responses/ServiceUnavailableProblem"

  /api/snapshot/dataset/profile/query:
    post:
      tags: [Snapshot]
      operationId: querySnapshotDatasetProfile
      x-neoengram-central-route: false
      summary: 查询 Snapshot Dataset Profile
      description: 返回固定 Snapshot 的公开数据集画像、Schema、质量和 freshness 摘要。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QuerySnapshotDatasetProfileRequest"
            example:
              tenant_id: tenant-a
              snapshot_id: snap-road-main3-sha-01
      responses:
        "200":
          description: Snapshot Dataset Profile
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuerySnapshotDatasetProfileResponse"
              example:
                profile:
                  state: ready
                  summary:
                    format_count: 4
                    logical_file_count: "864"
                    logical_size_bytes: "12884901888"
                    row_count: "18409216"
                    field_count: 18
                  quality:
                    state: warning
                    checks_total: 12
                    checks_passed: 11
                    checks_failed: 1
                  freshness:
                    observed_at_unix_ms: "1785167600000"
                    age_seconds: "200"
        "401":
          $ref: "#/components/responses/AuthenticationProblem"
        "403":
          $ref: "#/components/responses/AuthorizationProblem"
        "404":
          $ref: "#/components/responses/ResourceNotFoundProblem"
        "413":
          $ref: "#/components/responses/PayloadTooLargeProblem"
        "422":
          $ref: "#/components/responses/ValidationProblem"
        "500":
          $ref: "#/components/responses/InternalProblem"
        "503":
          $ref: "#/components/responses/ServiceUnavailableProblem"

  /api/s3/access-point/create:
    post:
      tags: [S3]
      operationId: createS3AccessPoint
      summary: 为 Ready Snapshot 开启只读 S3 访问
      description: |
        要求 `s3.access.manage` 权限。为固定 Snapshot 创建唯一只读 Access Point，并在首次成功
        响应中返回一组短期凭证。Snapshot 必须为 `ready`，对应 GatewayPool 必须可服务；同一
        Snapshot 已有停用 Access Point 时必须显式调用 enable，不会通过本方法重新签发凭证。
        `secret_access_key` 只在首次创建凭证时返回，幂等重放不会再次返回原 Secret。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateS3AccessPointRequest"
      responses:
        "200":
          description: Access Point 与首次凭证
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateS3AccessPointResponse"
        "401": { $ref: "#/components/responses/AuthenticationProblem" }
        "403": { $ref: "#/components/responses/AuthorizationProblem" }
        "404": { $ref: "#/components/responses/ResourceNotFoundProblem" }
        "409": { $ref: "#/components/responses/MutationConflictProblem" }
        "413": { $ref: "#/components/responses/PayloadTooLargeProblem" }
        "422": { $ref: "#/components/responses/ValidationProblem" }
        "500": { $ref: "#/components/responses/InternalProblem" }
        "503": { $ref: "#/components/responses/ServiceUnavailableProblem" }

  /api/s3/access-point/list/query:
    post:
      tags: [S3]
      operationId: queryS3AccessPointList
      summary: 分页查询 S3 Access Point
      description: 要求 `s3.access.read` 权限；仅返回当前 Tenant 可见的 Access Point。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QueryS3AccessPointListRequest"
      responses:
        "200":
          description: Access Point 页
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueryS3AccessPointListResponse"
        "401": { $ref: "#/components/responses/AuthenticationProblem" }
        "403": { $ref: "#/components/responses/AuthorizationProblem" }
        "422": { $ref: "#/components/responses/ValidationProblem" }
        "500": { $ref: "#/components/responses/InternalProblem" }
        "503": { $ref: "#/components/responses/ServiceUnavailableProblem" }

  /api/s3/access-point/query:
    post:
      tags: [S3]
      operationId: queryS3AccessPoint
      summary: 查询 S3 Access Point
      description: 要求 `s3.access.read` 权限；不存在或不可见统一返回 404。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QueryS3AccessPointRequest"
      responses:
        "200":
          description: Access Point 当前视图
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueryS3AccessPointResponse"
        "401": { $ref: "#/components/responses/AuthenticationProblem" }
        "403": { $ref: "#/components/responses/AuthorizationProblem" }
        "404": { $ref: "#/components/responses/ResourceNotFoundProblem" }
        "422": { $ref: "#/components/responses/ValidationProblem" }
        "500": { $ref: "#/components/responses/InternalProblem" }
        "503": { $ref: "#/components/responses/ServiceUnavailableProblem" }

  /api/s3/access-point/enable:
    post:
      tags: [S3]
      operationId: enableS3AccessPoint
      summary: 启用 S3 Access Point
      description: |
        要求 `s3.access.manage` 权限。启用只恢复 Access Point 策略，不恢复停用时已撤销的凭证；
        调用方需要随后创建新凭证。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateS3AccessPointRequest"
      responses:
        "200":
          description: 已启用 Access Point
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UpdateS3AccessPointResponse"
        "401": { $ref: "#/components/responses/AuthenticationProblem" }
        "403": { $ref: "#/components/responses/AuthorizationProblem" }
        "404": { $ref: "#/components/responses/ResourceNotFoundProblem" }
        "409": { $ref: "#/components/responses/MutationConflictProblem" }
        "422": { $ref: "#/components/responses/ValidationProblem" }
        "500": { $ref: "#/components/responses/InternalProblem" }
        "503": { $ref: "#/components/responses/ServiceUnavailableProblem" }

  /api/s3/access-point/disable:
    post:
      tags: [S3]
      operationId: disableS3AccessPoint
      summary: 停用 S3 Access Point
      description: |
        要求 `s3.access.manage` 权限。停用后对象访问立即不可用，所有 active 凭证被撤销，
        policy generation 单调递增。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateS3AccessPointRequest"
      responses:
        "200":
          description: 已停用 Access Point
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UpdateS3AccessPointResponse"
        "401": { $ref: "#/components/responses/AuthenticationProblem" }
        "403": { $ref: "#/components/responses/AuthorizationProblem" }
        "404": { $ref: "#/components/responses/ResourceNotFoundProblem" }
        "409": { $ref: "#/components/responses/MutationConflictProblem" }
        "422": { $ref: "#/components/responses/ValidationProblem" }
        "500": { $ref: "#/components/responses/InternalProblem" }
        "503": { $ref: "#/components/responses/ServiceUnavailableProblem" }

  /api/s3/credential/create:
    post:
      tags: [S3]
      operationId: createS3Credential
      summary: 创建 S3 短期凭证
      description: |
        要求 `s3.access.manage` 权限。仅 active Access Point 可创建凭证；disabled 状态返回
        `S3_ACCESS_POINT_DISABLED`。每个 Access Point 最多同时存在两组 active 凭证，
        `secret_access_key` 只在首次成功响应中返回。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateS3CredentialRequest"
      responses:
        "200":
          description: 新凭证或幂等重放结果
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateS3CredentialResponse"
        "401": { $ref: "#/components/responses/AuthenticationProblem" }
        "403": { $ref: "#/components/responses/AuthorizationProblem" }
        "404": { $ref: "#/components/responses/ResourceNotFoundProblem" }
        "409": { $ref: "#/components/responses/MutationConflictProblem" }
        "422": { $ref: "#/components/responses/ValidationProblem" }
        "500": { $ref: "#/components/responses/InternalProblem" }
        "503": { $ref: "#/components/responses/ServiceUnavailableProblem" }

  /api/s3/credential/list/query:
    post:
      tags: [S3]
      operationId: queryS3CredentialList
      summary: 查询 Access Point 凭证
      description: 要求 `s3.access.manage` 权限；响应永不包含 Secret 或加密材料。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QueryS3CredentialListRequest"
      responses:
        "200":
          description: Access Point 凭证列表
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueryS3CredentialListResponse"
        "401": { $ref: "#/components/responses/AuthenticationProblem" }
        "403": { $ref: "#/components/responses/AuthorizationProblem" }
        "404": { $ref: "#/components/responses/ResourceNotFoundProblem" }
        "422": { $ref: "#/components/responses/ValidationProblem" }
        "500": { $ref: "#/components/responses/InternalProblem" }
        "503": { $ref: "#/components/responses/ServiceUnavailableProblem" }

  /api/s3/credential/revoke:
    post:
      tags: [S3]
      operationId: revokeS3Credential
      summary: 撤销 S3 凭证
      description: 要求 `s3.access.manage` 权限；重复撤销保持 revoked，并返回当前凭证列表。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RevokeS3CredentialRequest"
      responses:
        "200":
          description: 撤销后的凭证列表
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueryS3CredentialListResponse"
        "401": { $ref: "#/components/responses/AuthenticationProblem" }
        "403": { $ref: "#/components/responses/AuthorizationProblem" }
        "404": { $ref: "#/components/responses/ResourceNotFoundProblem" }
        "409": { $ref: "#/components/responses/MutationConflictProblem" }
        "422": { $ref: "#/components/responses/ValidationProblem" }
        "500": { $ref: "#/components/responses/InternalProblem" }
        "503": { $ref: "#/components/responses/ServiceUnavailableProblem" }

  /api/s3/object/list/query:
    post:
      tags: [S3]
      operationId: queryS3ObjectList
      summary: 浏览只读 S3 对象
      description: |
        要求 `s3.access.read` 权限。对象列表来自 Access Point 固定 Commit 的不可变索引；
        `prefix` 与 `delimiter` 使用 S3 ListObjectsV2 语义，公共目录前缀单独返回。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QueryS3ObjectListRequest"
      responses:
        "200":
          description: 对象与公共前缀页
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueryS3ObjectListResponse"
        "401": { $ref: "#/components/responses/AuthenticationProblem" }
        "403": { $ref: "#/components/responses/AuthorizationProblem" }
        "404": { $ref: "#/components/responses/ResourceNotFoundProblem" }
        "409": { $ref: "#/components/responses/MutationConflictProblem" }
        "422": { $ref: "#/components/responses/ValidationProblem" }
        "500": { $ref: "#/components/responses/InternalProblem" }
        "503": { $ref: "#/components/responses/ServiceUnavailableProblem" }

  /api/s3/object/download-url/create:
    post:
      tags: [S3]
      operationId: createS3DownloadUrl
      summary: 创建只读对象下载 URL
      description: |
        要求 `s3.access.read` 权限。为固定 Commit 中存在的对象创建短期、只读 presigned URL；
        URL 不授予列举、写入或删除权限，且不会绕过 disabled Access Point。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateS3DownloadUrlRequest"
      responses:
        "200":
          description: 短期下载 URL
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateS3DownloadUrlResponse"
        "401": { $ref: "#/components/responses/AuthenticationProblem" }
        "403": { $ref: "#/components/responses/AuthorizationProblem" }
        "404": { $ref: "#/components/responses/ResourceNotFoundProblem" }
        "409": { $ref: "#/components/responses/MutationConflictProblem" }
        "422": { $ref: "#/components/responses/ValidationProblem" }
        "500": { $ref: "#/components/responses/InternalProblem" }
        "503": { $ref: "#/components/responses/ServiceUnavailableProblem" }

  /api/resource/deletion/impact/query:
    post:
      tags: [ResourceLifecycle]
      operationId: queryResourceDeletionImpact
      summary: 查询资源安全删除影响
      description: |
        要求 `resource.lifecycle.read` 权限。基于当前 Catalog 依赖图返回待删除资源、活动任务、
        S3 凭证、唯一对象副本与预计清理规模，并签发五分钟有效的 `impact_digest`。该调用只做
        dry-run，不改变资源状态；Artifact 或 StorageVolume 的子资源仅在显式 `cascade=true` 时纳入。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QueryDeletionImpactRequest"
      responses:
        "200":
          description: 与当前资源版本和依赖图绑定的删除影响摘要
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueryDeletionImpactResponse"
        "401": { $ref: "#/components/responses/AuthenticationProblem" }
        "403": { $ref: "#/components/responses/AuthorizationProblem" }
        "404": { $ref: "#/components/responses/ResourceNotFoundProblem" }
        "409": { $ref: "#/components/responses/MutationConflictProblem" }
        "413": { $ref: "#/components/responses/PayloadTooLargeProblem" }
        "422": { $ref: "#/components/responses/ValidationProblem" }
        "500": { $ref: "#/components/responses/InternalProblem" }
        "503": { $ref: "#/components/responses/ServiceUnavailableProblem" }

  /api/resource/deletion/create:
    post:
      tags: [ResourceLifecycle]
      operationId: createResourceDeletion
      summary: 创建安全删除任务
      description: |
        要求 `resource.lifecycle.manage` 权限。服务端在 Catalog 事务中重新验证五分钟影响摘要、
        资源版本与依赖集合，然后原子停止整批资源访问、停用关联 S3 Access Point 并创建删除 Saga。
        相同 `request_id` 与请求摘要幂等重放原任务；进入 `purging` 后不再允许恢复。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateDeletionRequest"
      responses:
        "200":
          description: 已线性化的删除任务
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeletionMutationResponse"
        "401": { $ref: "#/components/responses/AuthenticationProblem" }
        "403": { $ref: "#/components/responses/AuthorizationProblem" }
        "404": { $ref: "#/components/responses/ResourceNotFoundProblem" }
        "409": { $ref: "#/components/responses/MutationConflictProblem" }
        "413": { $ref: "#/components/responses/PayloadTooLargeProblem" }
        "422": { $ref: "#/components/responses/ValidationProblem" }
        "500": { $ref: "#/components/responses/InternalProblem" }
        "503": { $ref: "#/components/responses/ServiceUnavailableProblem" }

  /api/resource/deletion/query:
    post:
      tags: [ResourceLifecycle]
      operationId: queryResourceDeletion
      summary: 查询删除任务详情
      description: |
        要求 `resource.lifecycle.read` 权限。返回删除 Saga 当前阶段、整批目标、错误和所有
        Retention Hold；永久删除后的资源只可通过本任务视图和保留 tombstone 审计查询。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QueryDeletionRequest"
      responses:
        "200":
          description: 删除任务与 Retention Hold
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueryDeletionResponse"
        "401": { $ref: "#/components/responses/AuthenticationProblem" }
        "403": { $ref: "#/components/responses/AuthorizationProblem" }
        "404": { $ref: "#/components/responses/ResourceNotFoundProblem" }
        "413": { $ref: "#/components/responses/PayloadTooLargeProblem" }
        "422": { $ref: "#/components/responses/ValidationProblem" }
        "500": { $ref: "#/components/responses/InternalProblem" }
        "503": { $ref: "#/components/responses/ServiceUnavailableProblem" }

  /api/resource/deletion/list/query:
    post:
      tags: [ResourceLifecycle]
      operationId: queryResourceDeletionList
      summary: 分页查询回收站任务
      description: |
        要求 `resource.lifecycle.read` 权限。按 Tenant 和可选任务状态分页返回删除批次；
        普通资源列表仍只返回 `active` 资源，本方法是管理员查看回收站和永久删除记录的入口。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QueryDeletionListRequest"
      responses:
        "200":
          description: 删除任务页
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueryDeletionListResponse"
        "401": { $ref: "#/components/responses/AuthenticationProblem" }
        "403": { $ref: "#/components/responses/AuthorizationProblem" }
        "413": { $ref: "#/components/responses/PayloadTooLargeProblem" }
        "422": { $ref: "#/components/responses/ValidationProblem" }
        "500": { $ref: "#/components/responses/InternalProblem" }
        "503": { $ref: "#/components/responses/ServiceUnavailableProblem" }

  /api/resource/deletion/restore:
    post:
      tags: [ResourceLifecycle]
      operationId: restoreResourceDeletion
      summary: 整批恢复回收站资源
      description: |
        要求 `resource.lifecycle.manage` 权限。仅在任务进入 `purging` 前接受恢复，并以固定依赖批次
        执行；所有目标保持 `restoring`，直到 Agent 物理恢复完成后由 Catalog 原子切回 `active`。
        已撤销的 S3 凭证、Job 与 Pre-commit 不会随资源自动恢复。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateDeletionRequest"
      responses:
        "200":
          description: 已进入恢复流程的删除任务
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeletionMutationResponse"
        "401": { $ref: "#/components/responses/AuthenticationProblem" }
        "403": { $ref: "#/components/responses/AuthorizationProblem" }
        "404": { $ref: "#/components/responses/ResourceNotFoundProblem" }
        "409": { $ref: "#/components/responses/MutationConflictProblem" }
        "413": { $ref: "#/components/responses/PayloadTooLargeProblem" }
        "422": { $ref: "#/components/responses/ValidationProblem" }
        "500": { $ref: "#/components/responses/InternalProblem" }
        "503": { $ref: "#/components/responses/ServiceUnavailableProblem" }

  /api/resource/deletion/retry:
    post:
      tags: [ResourceLifecycle]
      operationId: retryResourceDeletion
      summary: 重试阻塞或失败的删除任务
      description: |
        要求 `resource.lifecycle.manage` 权限。对 `blocked` 或 `failed` Saga 创建幂等重试，保留原删除
        ID、依赖批次和生命周期 generation；该方法不能绕过 Retention Hold、唯一副本或 Agent fencing。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateDeletionRequest"
      responses:
        "200":
          description: 已重新进入可推进状态的删除任务
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeletionMutationResponse"
        "401": { $ref: "#/components/responses/AuthenticationProblem" }
        "403": { $ref: "#/components/responses/AuthorizationProblem" }
        "404": { $ref: "#/components/responses/ResourceNotFoundProblem" }
        "409": { $ref: "#/components/responses/MutationConflictProblem" }
        "413": { $ref: "#/components/responses/PayloadTooLargeProblem" }
        "422": { $ref: "#/components/responses/ValidationProblem" }
        "500": { $ref: "#/components/responses/InternalProblem" }
        "503": { $ref: "#/components/responses/ServiceUnavailableProblem" }

  /api/resource/retention-hold/create:
    post:
      tags: [ResourceLifecycle]
      operationId: createResourceRetentionHold
      summary: 为删除任务创建 Retention Hold
      description: |
        要求 `retention.manage` 权限。Hold 必须记录原因，可永久有效或设置到期时间；它不会恢复
        资源访问，只阻止删除任务进入 `purging`。相同 `request_id` 和请求摘要幂等返回同一个 Hold。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateRetentionHoldRequest"
      responses:
        "200":
          description: 删除任务与新建 Hold
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateRetentionHoldResponse"
        "401": { $ref: "#/components/responses/AuthenticationProblem" }
        "403": { $ref: "#/components/responses/AuthorizationProblem" }
        "404": { $ref: "#/components/responses/ResourceNotFoundProblem" }
        "409": { $ref: "#/components/responses/MutationConflictProblem" }
        "413": { $ref: "#/components/responses/PayloadTooLargeProblem" }
        "422": { $ref: "#/components/responses/ValidationProblem" }
        "500": { $ref: "#/components/responses/InternalProblem" }
        "503": { $ref: "#/components/responses/ServiceUnavailableProblem" }

  /api/resource/retention-hold/release:
    post:
      tags: [ResourceLifecycle]
      operationId: releaseResourceRetentionHold
      summary: 释放删除任务的 Retention Hold
      description: |
        要求 `retention.manage` 权限。使用删除任务资源版本执行 CAS 并幂等释放指定 Hold；最后一个
        有效 Hold 释放后，已超过 `purge_after` 的任务立即重新具备进入物理清理阶段的资格。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReleaseRetentionHoldRequest"
      responses:
        "200":
          description: 删除任务与已释放 Hold
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReleaseRetentionHoldResponse"
        "401": { $ref: "#/components/responses/AuthenticationProblem" }
        "403": { $ref: "#/components/responses/AuthorizationProblem" }
        "404": { $ref: "#/components/responses/ResourceNotFoundProblem" }
        "409": { $ref: "#/components/responses/MutationConflictProblem" }
        "413": { $ref: "#/components/responses/PayloadTooLargeProblem" }
        "422": { $ref: "#/components/responses/ValidationProblem" }
        "500": { $ref: "#/components/responses/InternalProblem" }
        "503": { $ref: "#/components/responses/ServiceUnavailableProblem" }

  /api/gateway/pool/create:
    post:
      tags: [Gateway]
      operationId: createGatewayPool
      summary: 创建 GatewayPool
      description: 为一个 EdgeCluster 创建唯一逻辑 GatewayPool；要求全局 gateway.manage 权限。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateGatewayPoolRequest"
      responses:
        "200":
          description: GatewayPool 已创建或相同定义被重放
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GatewayPoolResponse"
        "401": { $ref: "#/components/responses/AuthenticationProblem" }
        "403": { $ref: "#/components/responses/AuthorizationProblem" }
        "409": { $ref: "#/components/responses/MutationConflictProblem" }
        "422": { $ref: "#/components/responses/ValidationProblem" }
        "503": { $ref: "#/components/responses/ServiceUnavailableProblem" }

  /api/gateway/pool/query:
    post:
      tags: [Gateway]
      operationId: queryGatewayPool
      summary: 查询 GatewayPool
      description: 按稳定 ID 查询 GatewayPool，不返回 Replica credential 或路由租约。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QueryGatewayPoolRequest"
      responses:
        "200":
          description: GatewayPool 当前视图
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GatewayPoolResponse"
        "401": { $ref: "#/components/responses/AuthenticationProblem" }
        "403": { $ref: "#/components/responses/AuthorizationProblem" }
        "404": { $ref: "#/components/responses/ResourceNotFoundProblem" }
        "422": { $ref: "#/components/responses/ValidationProblem" }
        "503": { $ref: "#/components/responses/ServiceUnavailableProblem" }

  /api/gateway/pool/list/query:
    post:
      tags: [Gateway]
      operationId: queryGatewayPoolList
      summary: 分页查询 GatewayPool
      description: 可按 EdgeCluster 和状态过滤；游标使用上一页最后一个 GatewayPool ID。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QueryGatewayPoolListRequest"
      responses:
        "200":
          description: GatewayPool 页
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GatewayPoolListResponse"
        "401": { $ref: "#/components/responses/AuthenticationProblem" }
        "403": { $ref: "#/components/responses/AuthorizationProblem" }
        "422": { $ref: "#/components/responses/ValidationProblem" }
        "503": { $ref: "#/components/responses/ServiceUnavailableProblem" }

  /api/gateway/pool/update:
    post:
      tags: [Gateway]
      operationId: updateGatewayPool
      summary: 更新 GatewayPool
      description: 使用 expected_resource_version 原子更新入口、副本目标或非 draining 状态。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateGatewayPoolRequest"
      responses:
        "200":
          description: 更新后的 GatewayPool
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GatewayPoolResponse"
        "401": { $ref: "#/components/responses/AuthenticationProblem" }
        "403": { $ref: "#/components/responses/AuthorizationProblem" }
        "404": { $ref: "#/components/responses/ResourceNotFoundProblem" }
        "409": { $ref: "#/components/responses/MutationConflictProblem" }
        "422": { $ref: "#/components/responses/ValidationProblem" }
        "503": { $ref: "#/components/responses/ServiceUnavailableProblem" }

  /api/gateway/pool/drain:
    post:
      tags: [Gateway]
      operationId: drainGatewayPool
      summary: 排空 GatewayPool
      description: 使用 expected_resource_version 将 Pool 单向切换为 draining。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DrainGatewayPoolRequest"
      responses:
        "200":
          description: Draining GatewayPool
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GatewayPoolResponse"
        "401": { $ref: "#/components/responses/AuthenticationProblem" }
        "403": { $ref: "#/components/responses/AuthorizationProblem" }
        "404": { $ref: "#/components/responses/ResourceNotFoundProblem" }
        "409": { $ref: "#/components/responses/MutationConflictProblem" }
        "422": { $ref: "#/components/responses/ValidationProblem" }
        "503": { $ref: "#/components/responses/ServiceUnavailableProblem" }

  /api/gateway/replica/create:
    post:
      tags: [Gateway]
      operationId: createGatewayReplica
      summary: 创建 GatewayReplica
      description: activation_token 只在首次成功响应中出现；Central 持久化层只保存摘要。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateGatewayReplicaRequest"
      responses:
        "200":
          description: Pending GatewayReplica；重放响应不再包含 activation_token
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateGatewayReplicaResponse"
        "401": { $ref: "#/components/responses/AuthenticationProblem" }
        "403": { $ref: "#/components/responses/AuthorizationProblem" }
        "404": { $ref: "#/components/responses/ResourceNotFoundProblem" }
        "409": { $ref: "#/components/responses/MutationConflictProblem" }
        "422": { $ref: "#/components/responses/ValidationProblem" }
        "503": { $ref: "#/components/responses/ServiceUnavailableProblem" }

  /api/gateway/replica/activate:
    post:
      tags: [Gateway]
      operationId: activateGatewayReplica
      summary: 激活 GatewayReplica
      description: |
        仅允许具有全局 `gateway.manage` 权限的 operator 调用。Central 从 Registry 读取
        持久化的 bootstrap_endpoint，主动完成 challenge/proof、证书投递和 Pending -> Active
        原子提交；请求体不得指定或覆盖 endpoint。activation_token 只用于本次动作，不会在
        Central 日志或持久化 payload 中出现。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ActivateGatewayReplicaRequest"
      responses:
        "200":
          description: 已完成证书投递并激活的 GatewayReplica
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GatewayReplicaResponse"
        "401": { $ref: "#/components/responses/AuthenticationProblem" }
        "403": { $ref: "#/components/responses/AuthorizationProblem" }
        "404": { $ref: "#/components/responses/ResourceNotFoundProblem" }
        "409": { $ref: "#/components/responses/MutationConflictProblem" }
        "422": { $ref: "#/components/responses/ValidationProblem" }
        "503": { $ref: "#/components/responses/ServiceUnavailableProblem" }

  /api/gateway/replica/list/query:
    post:
      tags: [Gateway]
      operationId: queryGatewayReplicaList
      summary: 分页查询 GatewayReplica
      description: 返回 endpoint、版本、capability 和证书状态，不返回 token 摘要或证书指纹。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QueryGatewayReplicaListRequest"
      responses:
        "200":
          description: GatewayReplica 页
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GatewayReplicaListResponse"
        "401": { $ref: "#/components/responses/AuthenticationProblem" }
        "403": { $ref: "#/components/responses/AuthorizationProblem" }
        "422": { $ref: "#/components/responses/ValidationProblem" }
        "503": { $ref: "#/components/responses/ServiceUnavailableProblem" }

  /api/gateway/replica/drain:
    post:
      tags: [Gateway]
      operationId: drainGatewayReplica
      summary: 排空 GatewayReplica
      description: 使用 expected_resource_version 将 Active Replica 单向切换为 draining。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MutateGatewayReplicaRequest"
      responses:
        "200":
          description: Draining GatewayReplica
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GatewayReplicaResponse"
        "401": { $ref: "#/components/responses/AuthenticationProblem" }
        "403": { $ref: "#/components/responses/AuthorizationProblem" }
        "404": { $ref: "#/components/responses/ResourceNotFoundProblem" }
        "409": { $ref: "#/components/responses/MutationConflictProblem" }
        "422": { $ref: "#/components/responses/ValidationProblem" }
        "503": { $ref: "#/components/responses/ServiceUnavailableProblem" }

  /api/gateway/replica/revoke:
    post:
      tags: [Gateway]
      operationId: revokeGatewayReplica
      summary: 撤销 GatewayReplica
      description: 原子标记 Replica 与 credential revoked，并提升已有 certificate generation。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MutateGatewayReplicaRequest"
      responses:
        "200":
          description: Revoked GatewayReplica
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GatewayReplicaResponse"
        "401": { $ref: "#/components/responses/AuthenticationProblem" }
        "403": { $ref: "#/components/responses/AuthorizationProblem" }
        "404": { $ref: "#/components/responses/ResourceNotFoundProblem" }
        "409": { $ref: "#/components/responses/MutationConflictProblem" }
        "422": { $ref: "#/components/responses/ValidationProblem" }
        "503": { $ref: "#/components/responses/ServiceUnavailableProblem" }

  /api/job/add/create:
    post:
      tags: [Job]
      operationId: createAddJob
      summary: 创建 Managed Add Job
      description: |
        创建一个 tenant-scoped Managed Add Job。`principal` 不由请求体声明，而是由
        Bearer JWT 的认证结果注入。服务端在完成认证和请求校验后，使用 canonical
        PrincipalRef 与完整 Add operation 计算 `request_digest`；客户端不提交该字段。

        相同 `(tenant_id, job_id, request_digest)` 的重试返回原 Job，并将 `replayed`
        设为 `true`。相同 Job ID 对应另一份服务端计算 digest 时返回 `JOB_ID_REUSED`。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateAddJobRequest"
            examples:
              selectedPaths:
                summary: 添加显式路径
                value:
                  tenant_id: tenant-a
                  project_id: project-a
                  artifact_id: artifact-a
                  playground_id: playground-a
                  job_id: job-20260727-001
                  expected_index_version:
                    revision: "0"
                    digest: aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
                  deadline_unix_ms: "1785168000000"
                  paths:
                    - dataset/images
                    - dataset/labels.csv
                  all: false
              allPaths:
                summary: 添加 Playground 全部变化
                value:
                  tenant_id: tenant-a
                  project_id: project-a
                  artifact_id: artifact-a
                  playground_id: playground-a
                  job_id: job-20260727-002
                  expected_index_version:
                    revision: "12"
                    digest: cccccccccccccccccccccccccccccccccccccccccccccccccccccccccccccccc
                  deadline_unix_ms: "1785168000000"
                  paths: []
                  all: true
      responses:
        "200":
          description: Job 已创建，或同一请求被幂等重放
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateAddJobResponse"
              examples:
                created:
                  summary: 首次创建
                  value:
                    job:
                      operation: add
                      tenant_id: tenant-a
                      project_id: project-a
                      artifact_id: artifact-a
                      playground_id: playground-a
                      job_id: job-20260727-001
                      state: queued
                      resource_version: "1"
                      deadline_unix_ms: "1785168000000"
                    replayed: false
                replayed:
                  summary: 响应丢失后的同请求重放
                  value:
                    job:
                      operation: add
                      tenant_id: tenant-a
                      project_id: project-a
                      artifact_id: artifact-a
                      playground_id: playground-a
                      job_id: job-20260727-001
                      state: queued
                      resource_version: "1"
                      deadline_unix_ms: "1785168000000"
                    replayed: true
        "401":
          $ref: "#/components/responses/AuthenticationProblem"
        "403":
          $ref: "#/components/responses/AuthorizationProblem"
        "408":
          $ref: "#/components/responses/DeadlineProblem"
        "409":
          $ref: "#/components/responses/CreateConflictProblem"
        "413":
          $ref: "#/components/responses/PayloadTooLargeProblem"
        "422":
          $ref: "#/components/responses/ValidationProblem"
        "429":
          $ref: "#/components/responses/OverloadedProblem"
        "500":
          $ref: "#/components/responses/InternalProblem"
        "503":
          $ref: "#/components/responses/ServiceUnavailableProblem"
        "504":
          $ref: "#/components/responses/RequestTimeoutProblem"

  /api/job/query:
    post:
      tags: [Job]
      operationId: queryJob
      summary: 查询 Job 权威状态
      description: |
        只读取中心 authority，不直接访问 Agent。跨租户或调用方不可见的 Job 与不存在的
        Job 一样返回 `404 JOB_NOT_FOUND`，不得泄漏资源存在性。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QueryJobRequest"
            example:
              tenant_id: tenant-a
              job_id: job-20260727-001
      responses:
        "200":
          description: 当前权威 Job 视图
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueryJobResponse"
              examples:
                current:
                  summary: 首次查询当前状态
                  value:
                    job:
                      operation: add
                      tenant_id: tenant-a
                      project_id: project-a
                      artifact_id: artifact-a
                      playground_id: playground-a
                      job_id: job-20260727-001
                      state: running
                      resource_version: "4"
                      deadline_unix_ms: "1785168000000"
                      progress:
                        state: running
                        phase: hashing
                        files_completed: "320"
                        bytes_completed: "4294967296"
                repeated:
                  summary: 重复查询同一权威版本
                  value:
                    job:
                      operation: add
                      tenant_id: tenant-a
                      project_id: project-a
                      artifact_id: artifact-a
                      playground_id: playground-a
                      job_id: job-20260727-001
                      state: running
                      resource_version: "4"
                      deadline_unix_ms: "1785168000000"
                      progress:
                        state: running
                        phase: hashing
                        files_completed: "320"
                        bytes_completed: "4294967296"
        "401":
          $ref: "#/components/responses/AuthenticationProblem"
        "403":
          $ref: "#/components/responses/AuthorizationProblem"
        "404":
          $ref: "#/components/responses/JobNotFoundProblem"
        "413":
          $ref: "#/components/responses/PayloadTooLargeProblem"
        "422":
          $ref: "#/components/responses/ValidationProblem"
        "429":
          $ref: "#/components/responses/OverloadedProblem"
        "500":
          $ref: "#/components/responses/InternalProblem"
        "503":
          $ref: "#/components/responses/ServiceUnavailableProblem"
        "504":
          $ref: "#/components/responses/RequestTimeoutProblem"

  /api/job/add/finalize:
    post:
      tags: [Job]
      operationId: finalizeAddJob
      summary: 校验并发布 Managed Add
      description: |
        对完整 MetadataBatch、中心 object durability 和 expected IndexVersion 执行最终校验与
        CAS。成功、冲突和拒绝都会形成稳定的 Job decision；重复调用返回同一终态。

        内部 `ResumePublication` 恢复方法不通过此接口暴露。用户调用始终重新执行授权，
        但不会为已持久化的 Publishing candidate 重新构造发布内容。
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ApiVersion"
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Traceparent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FinalizeAddJobRequest"
            example:
              tenant_id: tenant-a
              job_id: job-20260727-001
      responses:
        "200":
          description: Add 已形成稳定发布决定
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinalizeAddJobResponse"
              examples:
                published:
                  summary: Index CAS 发布成功
                  value:
                    job:
                      operation: add
                      tenant_id: tenant-a
                      project_id: project-a
                      artifact_id: artifact-a
                      playground_id: playground-a
                      job_id: job-20260727-001
                      state: succeeded
                      resource_version: "7"
                      deadline_unix_ms: "1785168000000"
                      decision:
                        outcome: publish
                        final_state: succeeded
                        published_index_version:
                          revision: "1"
                          digest: eeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeee
                      finalized_at_unix_ms: "1785167999000"
                    decision:
                      outcome: publish
                      final_state: succeeded
                      published_index_version:
                        revision: "1"
                        digest: eeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeee
                    finalized_at_unix_ms: "1785167999000"
                    replayed: false
                replayed:
                  summary: 终态重放
                  value:
                    job:
                      operation: add
                      tenant_id: tenant-a
                      project_id: project-a
                      artifact_id: artifact-a
                      playground_id: playground-a
                      job_id: job-20260727-001
                      state: succeeded
                      resource_version: "7"
                      deadline_unix_ms: "1785168000000"
                      decision:
                        outcome: publish
                        final_state: succeeded
                        published_index_version:
                          revision: "1"
                          digest: eeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeee
                      finalized_at_unix_ms: "1785167999000"
                    decision:
                      outcome: publish
                      final_state: succeeded
                      published_index_version:
                        revision: "1"
                        digest: eeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeee
                    finalized_at_unix_ms: "1785167999000"
                    replayed: true
        "401":
          $ref: "#/components/responses/AuthenticationProblem"
        "403":
          $ref: "#/components/responses/AuthorizationProblem"
        "404":
          $ref: "#/components/responses/JobNotFoundProblem"
        "408":
          $ref: "#/components/responses/DeadlineProblem"
        "409":
          $ref: "#/components/responses/FinalizeConflictProblem"
        "413":
          $ref: "#/components/responses/PayloadTooLargeProblem"
        "422":
          $ref: "#/components/responses/ValidationProblem"
        "429":
          $ref: "#/components/responses/OverloadedProblem"
        "500":
          $ref: "#/components/responses/InternalProblem"
        "503":
          $ref: "#/components/responses/ServiceUnavailableProblem"
        "504":
          $ref: "#/components/responses/RequestTimeoutProblem"

  /health/live:
    get:
      tags: [Health]
      operationId: liveProbe
      summary: 检查 neoengram-central 进程是否存活
      security: []
      description: 不访问 authority；仅表示进程可以处理 HTTP 请求。
      parameters:
        - $ref: "#/components/parameters/RequestId"
      responses:
        "200":
          description: 进程存活
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthResponse"
              example:
                status: ok
        "422":
          $ref: "#/components/responses/ValidationProblem"
        "429":
          $ref: "#/components/responses/OverloadedProblem"
        "500":
          $ref: "#/components/responses/InternalProblem"
        "503":
          $ref: "#/components/responses/ServiceUnavailableProblem"
        "504":
          $ref: "#/components/responses/RequestTimeoutProblem"

  /health/ready:
    get:
      tags: [Health]
      operationId: readyProbe
      summary: 检查 neoengram-central 是否可以接收业务请求
      security: []
      description: 至少验证 authority 已打开并通过其后端完整性检查。
      parameters:
        - $ref: "#/components/parameters/RequestId"
      responses:
        "200":
          description: 服务已就绪
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthResponse"
              example:
                status: ok
        "422":
          $ref: "#/components/responses/ValidationProblem"
        "429":
          $ref: "#/components/responses/OverloadedProblem"
        "500":
          $ref: "#/components/responses/InternalProblem"
        "503":
          $ref: "#/components/responses/ServiceUnavailableProblem"
        "504":
          $ref: "#/components/responses/RequestTimeoutProblem"

components:
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: |
        由部署配置的 OIDC issuer 签发。服务端从验证后的 issuer/sub 映射 PrincipalRef，
        tenant scope 只能由启动时加载的服务端 RBAC 策略授予。JWT 中的 tenant、role、group
        只能作为审计提示；token 或请求体中的身份提示都不能覆盖服务端授权结果。

  parameters:
    ApiVersion:
      name: NeoEngram-API-Version
      in: header
      required: true
      description: 公开 API 主版本；不兼容演进不改变 path。
      schema:
        type: string
        enum: ["1"]
      example: "1"
    RequestId:
      name: X-Request-ID
      in: header
      required: false
      description: |
        可选的调用方请求 ID。缺失时由服务端生成；无论来源如何，响应都必须回传最终 ID。
      schema:
        $ref: "#/components/schemas/RequestId"
      example: req-20260727-001
    Traceparent:
      name: traceparent
      in: header
      required: false
      description: W3C Trace Context traceparent。
      schema:
        type: string
        minLength: 55
        maxLength: 512
      example: 00-4bf92f3577b34da6a3ce929d0e0e4736-00f067aa0ba902b7-01

  headers:
    RequestId:
      required: true
      description: 服务端确认或生成的请求 ID。
      schema:
        $ref: "#/components/schemas/RequestId"
      example: req-20260727-001

  responses:
    AuthenticationProblem:
      description: Bearer token 缺失、无效或已过期
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ProblemDetails"
          example:
            type: urn:neoengram:problem:authentication-required
            title: Authentication required
            status: 401
            detail: A valid Bearer token is required
            instance: /api/job/query
            code: AUTHENTICATION_REQUIRED
            request_id: req-20260727-001
            retryable: false
    AuthorizationProblem:
      description: principal 无权执行当前方法
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ProblemDetails"
          example:
            type: urn:neoengram:problem:authorization-denied
            title: Authorization denied
            status: 403
            detail: The principal is not authorized for this operation
            instance: /api/job/add/finalize
            code: AUTHORIZATION_DENIED
            request_id: req-20260727-001
            retryable: false
    JobNotFoundProblem:
      description: Job 不存在或对当前 principal 不可见
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ProblemDetails"
          example:
            type: urn:neoengram:problem:job-not-found
            title: Job not found
            status: 404
            detail: The requested Job was not found
            instance: /api/job/query
            code: JOB_NOT_FOUND
            request_id: req-20260727-001
            retryable: false
    ResourceNotFoundProblem:
      description: 资源不存在，或对当前 principal 不可见
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ProblemDetails"
          examples:
            tenantNotFound:
              value:
                type: urn:neoengram:problem:tenant-not-found
                title: Tenant not found
                status: 404
                detail: The requested Tenant was not found
                instance: /api/tenant/query
                code: TENANT_NOT_FOUND
                request_id: req-20260727-001
                retryable: false
            artifactNotFound:
              value:
                type: urn:neoengram:problem:artifact-not-found
                title: Artifact not found
                status: 404
                detail: The requested Artifact was not found
                instance: /api/artifact/query
                code: ARTIFACT_NOT_FOUND
                request_id: req-20260727-001
                retryable: false
    StorageEnrollmentNotFoundProblem:
      description: Storage enrollment 不存在，或不属于当前认证 principal 可见的 Tenant
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ProblemDetails"
          example:
            type: urn:neoengram:problem:storage-enrollment-not-found
            title: Storage enrollment not found
            status: 404
            detail: The requested Storage enrollment was not found
            instance: /api/storage/enrollment/query
            code: STORAGE_ENROLLMENT_NOT_FOUND
            request_id: req-20260731-001
            retryable: false
    TenantConflictProblem:
      description: Tenant ID 已绑定另一份创建内容
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ProblemDetails"
          example:
            type: urn:neoengram:problem:tenant-id-reused
            title: Tenant ID reused
            status: 409
            detail: The Tenant ID already belongs to a different create request
            instance: /api/tenant/create
            code: TENANT_ID_REUSED
            request_id: req-20260727-001
            retryable: false
    MutationConflictProblem:
      description: 资源 identity 已绑定另一创建内容，或 expected version 不再匹配
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ProblemDetails"
          examples:
            resourceConflict:
              value:
                type: urn:neoengram:problem:resource-conflict
                title: Resource mutation conflict
                status: 409
                detail: The resource identity or expected version conflicts with current state
                instance: /api/playground/commit/create
                code: RESOURCE_CONFLICT
                request_id: req-20260727-001
                retryable: false
            hardlinkRequiresWholeFile:
              value:
                type: urn:neoengram:problem:hardlink-requires-whole-file
                title: Hardlink delivery requires WholeFile layout
                status: 409
                detail: Hardlink delivery is only available for a WholeFile Commit
                instance: /api/snapshot/delivery/create
                code: HARDLINK_REQUIRES_WHOLE_FILE
                request_id: req-20260727-002
                retryable: false
            hardlinkCrossFilesystem:
              value:
                type: urn:neoengram:problem:hardlink-cross-filesystem
                title: Hardlink delivery crosses filesystems
                status: 409
                detail: The CAS object and delivery target are on different filesystems
                instance: /api/snapshot/delivery/create
                code: HARDLINK_CROSS_FILESYSTEM
                request_id: req-20260727-003
                retryable: false
            hardlinkUnsafeVolume:
              value:
                type: urn:neoengram:problem:hardlink-unsafe-volume
                title: Hardlink delivery is unsafe on this volume
                status: 409
                detail: The StorageVolume policy does not provide a sealed read-only hardlink boundary
                instance: /api/snapshot/delivery/create
                code: HARDLINK_UNSAFE_VOLUME
                request_id: req-20260727-004
                retryable: false
            hardlinkObjectNotSealed:
              value:
                type: urn:neoengram:problem:hardlink-object-not-sealed
                title: Hardlink object is not sealed
                status: 409
                detail: The referenced CAS object is not sealed for hardlink delivery
                instance: /api/snapshot/delivery/create
                code: HARDLINK_OBJECT_NOT_SEALED
                request_id: req-20260727-005
                retryable: false
            deliveryTargetConflict:
              value:
                type: urn:neoengram:problem:delivery-target-conflict
                title: Delivery target conflict
                status: 409
                detail: The derived delivery target is already occupied by another resource
                instance: /api/snapshot/delivery/create
                code: DELIVERY_TARGET_CONFLICT
                request_id: req-20260727-006
                retryable: false
    StorageEnrollmentConflictProblem:
      description: enrollment identity、状态、绑定或 expected resource version 与当前权威状态冲突
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ProblemDetails"
          examples:
            staleResourceVersion:
              value:
                type: urn:neoengram:problem:storage-enrollment-version-conflict
                title: Storage enrollment version conflict
                status: 409
                detail: The Storage enrollment resource version changed before review
                instance: /api/storage/enrollment/approve
                code: STORAGE_ENROLLMENT_VERSION_CONFLICT
                request_id: req-20260731-001
                retryable: false
            tokenRequestIdentityReused:
              value:
                type: urn:neoengram:problem:storage-enrollment-token-request-id-reused
                title: Storage enrollment token request ID reused
                status: 409
                detail: The token request ID already belongs to a different payload
                instance: /api/storage/enrollment/token/create
                code: STORAGE_ENROLLMENT_TOKEN_REQUEST_ID_REUSED
                request_id: req-20260731-002
                retryable: false
            decisionRequestIdentityReused:
              value:
                type: urn:neoengram:problem:storage-enrollment-decision-id-reused
                title: Storage enrollment decision request ID reused
                status: 409
                detail: The decision request ID already belongs to another review action or payload
                instance: /api/storage/enrollment/reject
                code: STORAGE_ENROLLMENT_DECISION_ID_REUSED
                request_id: req-20260731-003
                retryable: false
            pvcAlreadyEnrolled:
              value:
                type: urn:neoengram:problem:pvc-already-enrolled
                title: PVC already enrolled
                status: 409
                detail: The PVC is already bound to another active Agent enrollment
                instance: /api/storage/enrollment/approve
                code: PVC_ALREADY_ENROLLED
                request_id: req-20260731-003
                retryable: false
            replacementConfirmationRequired:
              value:
                type: urn:neoengram:problem:storage-enrollment-replacement-confirmation-required
                title: Replacement confirmation required
                status: 409
                detail: Replacement enrollment approval requires explicit confirmation
                instance: /api/storage/enrollment/approve
                code: STORAGE_ENROLLMENT_REPLACEMENT_CONFIRMATION_REQUIRED
                request_id: req-20260731-004
                retryable: false
            descriptorMismatch:
              value:
                type: urn:neoengram:problem:storage-volume-descriptor-mismatch
                title: StorageVolume descriptor mismatch
                status: 409
                detail: The enrollment probe does not match the descriptor frozen by the bootstrap token
                instance: /api/storage/enrollment/approve
                code: STORAGE_VOLUME_DESCRIPTOR_MISMATCH
                request_id: req-20260731-005
                retryable: false
    CursorConflictProblem:
      description: Cursor 无效、已过期或与当前 scope/filter 不匹配
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ProblemDetails"
          example:
            type: urn:neoengram:problem:cursor-invalid
            title: Cursor is invalid
            status: 409
            detail: The cursor is invalid or does not match the current query
            instance: /api/artifact/commit/graph/query
            code: CURSOR_INVALID
            request_id: req-20260727-001
            retryable: false
    DeadlineProblem:
      description: Job deadline 已过期
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ProblemDetails"
          example:
            type: urn:neoengram:problem:job-deadline-exceeded
            title: Job deadline exceeded
            status: 408
            detail: The managed Add deadline has elapsed
            instance: /api/job/add/finalize
            code: JOB_DEADLINE_EXCEEDED
            request_id: req-20260727-001
            retryable: false
    CreateConflictProblem:
      description: Job ID 已绑定另一请求，或 Job 创建发生并发冲突
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ProblemDetails"
          examples:
            reusedJobId:
              summary: 相同 Job ID 携带另一 request digest
              value:
                type: urn:neoengram:problem:job-id-reused
                title: Job ID reused
                status: 409
                detail: The Job ID already belongs to a different managed Add request
                instance: /api/job/add/create
                code: JOB_ID_REUSED
                request_id: req-20260727-001
                retryable: false
            concurrentUpdate:
              summary: authority CAS 发生并发更新
              value:
                type: urn:neoengram:problem:concurrent-update
                title: Concurrent update
                status: 409
                detail: The Job resource version changed during the request
                instance: /api/job/add/create
                code: CONCURRENT_UPDATE
                request_id: req-20260727-001
                retryable: true
                retry_after_ms: "100"
    FinalizeConflictProblem:
      description: Job 状态、MetadataBatch、durability 或 CAS 前置条件尚不满足
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ProblemDetails"
          examples:
            invalidState:
              summary: Job 尚未进入 Prepared
              value:
                type: urn:neoengram:problem:job-invalid-state
                title: Job state does not allow finalization
                status: 409
                detail: Managed Add can only be finalized from Prepared or a stable terminal state
                instance: /api/job/add/finalize
                code: JOB_INVALID_STATE
                request_id: req-20260727-001
                retryable: false
            objectNotDurable:
              summary: 引用对象尚无有效的 Volume placement evidence
              value:
                type: urn:neoengram:problem:object-not-durable
                title: Object is not durable
                status: 409
                detail: A referenced object has no evidence on the assigned Volume placement generation
                instance: /api/job/add/finalize
                code: OBJECT_NOT_DURABLE
                request_id: req-20260727-001
                retryable: true
                retry_after_ms: "1000"
    PayloadTooLargeProblem:
      description: 请求编码字节数超过当前方法上限
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ProblemDetails"
          example:
            type: urn:neoengram:problem:payload-too-large
            title: Payload too large
            status: 413
            detail: The encoded request exceeds the method limit
            instance: /api/job/add/create
            code: PROTOCOL_LIMIT_EXCEEDED
            request_id: req-20260727-001
            retryable: false
    ValidationProblem:
      description: 请求 header、protocol 或 metadata 违反公开契约
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ProblemDetails"
          examples:
            unsupportedApiVersion:
              summary: API 主版本不受支持
              value:
                type: urn:neoengram:problem:api-version-unsupported
                title: API version is unsupported
                status: 422
                detail: NeoEngram-API-Version must be 1
                instance: /api/job/query
                code: API_VERSION_UNSUPPORTED
                request_id: req-20260727-001
                retryable: false
                violations:
                  - field: NeoEngram-API-Version
                    reason: unsupported value
            invalidPathSelection:
              summary: Add path 集合无效
              value:
                type: urn:neoengram:problem:protocol-invalid
                title: Request validation failed
                status: 422
                detail: paths must be non-empty unless all is true
                instance: /api/job/add/create
                code: PROTOCOL_INVALID
                request_id: req-20260727-001
                retryable: false
                violations:
                  - field: paths
                    reason: must be non-empty unless all is true
    OverloadedProblem:
      description: 服务端并发请求配额已耗尽
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ProblemDetails"
          example:
            type: urn:neoengram:problem:overloaded
            title: Resource exhausted
            status: 429
            detail: server request concurrency is exhausted
            instance: /api/job/query
            code: OVERLOADED
            request_id: req-20260727-001
            retryable: false
    RequestTimeoutProblem:
      description: 请求超过服务端处理时限
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ProblemDetails"
          example:
            type: urn:neoengram:problem:deadline-exceeded
            title: Request deadline exceeded
            status: 504
            detail: service invocation deadline elapsed
            instance: /api/job/query
            code: DEADLINE_EXCEEDED
            request_id: req-20260727-001
            retryable: false
    InternalProblem:
      description: 未分类的服务端内部错误
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ProblemDetails"
          example:
            type: urn:neoengram:problem:internal
            title: Internal error
            status: 500
            detail: The server could not complete the request
            instance: /api/job/query
            code: INTERNAL
            request_id: req-20260727-001
            retryable: true
    ServiceUnavailableProblem:
      description: authority 或必要依赖暂不可用
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ProblemDetails"
          example:
            type: urn:neoengram:problem:storage-failure
            title: Authority unavailable
            status: 503
            detail: The authority store is temporarily unavailable
            instance: /health/ready
            code: STORAGE_FAILURE
            request_id: req-20260727-001
            retryable: true
            retry_after_ms: "1000"

  schemas:
    EmptyRequest:
      type: object
      additionalProperties: false

    ApiVersionResponse:
      type: object
      additionalProperties: false
      required:
        - api_version
        - agent_wire_version
        - capabilities
      properties:
        api_version:
          type: integer
          enum: [1]
        agent_wire_version:
          type: integer
          enum: [1]
        capabilities:
          type: array
          uniqueItems: true
          items:
            type: string
            minLength: 1
            maxLength: 128
            pattern: "^[a-z][a-z0-9_]{0,127}$"

    CreateGatewayPoolRequest:
      type: object
      additionalProperties: false
      required:
        - gateway_pool_id
        - edge_cluster_id
        - display_name
        - agent_endpoint
        - desired_replicas
        - minimum_ready_replicas
      properties:
        gateway_pool_id:
          $ref: "#/components/schemas/GatewayPoolId"
        edge_cluster_id:
          $ref: "#/components/schemas/EdgeClusterId"
        display_name:
          type: string
          minLength: 1
          maxLength: 256
        agent_endpoint:
          $ref: "#/components/schemas/GatewayEndpoint"
        s3_endpoint:
          $ref: "#/components/schemas/GatewayEndpoint"
        desired_replicas:
          $ref: "#/components/schemas/GatewayReplicaCount"
        minimum_ready_replicas:
          $ref: "#/components/schemas/GatewayReplicaCount"

    QueryGatewayPoolRequest:
      type: object
      additionalProperties: false
      required: [gateway_pool_id]
      properties:
        gateway_pool_id:
          $ref: "#/components/schemas/GatewayPoolId"

    QueryGatewayPoolListRequest:
      type: object
      additionalProperties: false
      properties:
        edge_cluster_id:
          $ref: "#/components/schemas/EdgeClusterId"
        state:
          $ref: "#/components/schemas/GatewayPoolState"
        after:
          $ref: "#/components/schemas/GatewayPoolId"
        page_size:
          type: integer
          minimum: 1
          maximum: 255

    UpdateGatewayPoolRequest:
      type: object
      additionalProperties: false
      required: [gateway_pool_id, expected_resource_version]
      properties:
        gateway_pool_id:
          $ref: "#/components/schemas/GatewayPoolId"
        expected_resource_version:
          $ref: "#/components/schemas/CanonicalU64"
        display_name:
          type: string
          minLength: 1
          maxLength: 256
        agent_endpoint:
          $ref: "#/components/schemas/GatewayEndpoint"
        s3_endpoint:
          $ref: "#/components/schemas/GatewayEndpoint"
        clear_s3_endpoint:
          type: boolean
        desired_replicas:
          $ref: "#/components/schemas/GatewayReplicaCount"
        minimum_ready_replicas:
          $ref: "#/components/schemas/GatewayReplicaCount"
        state:
          type: string
          enum: [provisioning, ready, disabled]

    DrainGatewayPoolRequest:
      type: object
      additionalProperties: false
      required: [gateway_pool_id, expected_resource_version]
      properties:
        gateway_pool_id:
          $ref: "#/components/schemas/GatewayPoolId"
        expected_resource_version:
          $ref: "#/components/schemas/CanonicalU64"

    GatewayPoolResponse:
      type: object
      additionalProperties: false
      required: [gateway_pool, replayed]
      properties:
        gateway_pool:
          $ref: "#/components/schemas/GatewayPoolView"
        replayed:
          type: boolean

    GatewayPoolListResponse:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items:
          type: array
          maxItems: 255
          items:
            $ref: "#/components/schemas/GatewayPoolView"
        next_after:
          $ref: "#/components/schemas/GatewayPoolId"

    GatewayPoolView:
      type: object
      additionalProperties: false
      required:
        - gateway_pool_id
        - edge_cluster_id
        - display_name
        - agent_endpoint
        - desired_replicas
        - minimum_ready_replicas
        - state
        - config_generation
        - resource_version
        - created_at_unix_ms
        - updated_at_unix_ms
      properties:
        gateway_pool_id:
          $ref: "#/components/schemas/GatewayPoolId"
        edge_cluster_id:
          $ref: "#/components/schemas/EdgeClusterId"
        display_name:
          type: string
          minLength: 1
          maxLength: 256
        agent_endpoint:
          $ref: "#/components/schemas/GatewayEndpoint"
        s3_endpoint:
          $ref: "#/components/schemas/GatewayEndpoint"
        desired_replicas:
          $ref: "#/components/schemas/GatewayReplicaCount"
        minimum_ready_replicas:
          $ref: "#/components/schemas/GatewayReplicaCount"
        state:
          $ref: "#/components/schemas/GatewayPoolState"
        config_generation:
          $ref: "#/components/schemas/PositiveCanonicalU64"
        resource_version:
          $ref: "#/components/schemas/CanonicalU64"
        created_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"
        updated_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"

    CreateGatewayReplicaRequest:
      type: object
      additionalProperties: false
      required:
        - gateway_replica_id
        - gateway_pool_id
        - control_endpoint
        - peer_endpoint
        - bootstrap_endpoint
        - software_version
        - wire_version
      properties:
        gateway_replica_id:
          $ref: "#/components/schemas/GatewayReplicaId"
        gateway_pool_id:
          $ref: "#/components/schemas/GatewayPoolId"
        control_endpoint:
          $ref: "#/components/schemas/GatewayEndpoint"
        peer_endpoint:
          $ref: "#/components/schemas/GatewayEndpoint"
        bootstrap_endpoint:
          $ref: "#/components/schemas/GatewayEndpoint"
        software_version:
          type: string
          minLength: 1
          maxLength: 256
        wire_version:
          type: integer
          const: 1
        capabilities:
          type: array
          maxItems: 128
          uniqueItems: true
          items:
            type: string
            minLength: 1
            maxLength: 128

    QueryGatewayReplicaListRequest:
      type: object
      additionalProperties: false
      required: [gateway_pool_id]
      properties:
        gateway_pool_id:
          $ref: "#/components/schemas/GatewayPoolId"
        state:
          $ref: "#/components/schemas/GatewayReplicaState"
        after:
          $ref: "#/components/schemas/GatewayReplicaId"
        page_size:
          type: integer
          minimum: 1
          maximum: 255

    MutateGatewayReplicaRequest:
      type: object
      additionalProperties: false
      required: [gateway_replica_id, expected_resource_version]
      properties:
        gateway_replica_id:
          $ref: "#/components/schemas/GatewayReplicaId"
        expected_resource_version:
          $ref: "#/components/schemas/CanonicalU64"

    ActivateGatewayReplicaRequest:
      type: object
      additionalProperties: false
      required:
        [gateway_replica_id, expected_resource_version, activation_token]
      properties:
        gateway_replica_id:
          $ref: "#/components/schemas/GatewayReplicaId"
        expected_resource_version:
          $ref: "#/components/schemas/CanonicalU64"
        activation_token:
          type: string
          writeOnly: true
          minLength: 51
          maxLength: 51
          pattern: "^nggw_v1_[A-Za-z0-9_-]{43}$"

    CreateGatewayReplicaResponse:
      type: object
      additionalProperties: false
      required: [gateway_replica, replayed]
      properties:
        gateway_replica:
          $ref: "#/components/schemas/GatewayReplicaView"
        activation_token:
          type: string
          readOnly: true
          minLength: 51
          maxLength: 51
          pattern: "^nggw_v1_[A-Za-z0-9_-]{43}$"
        replayed:
          type: boolean

    GatewayReplicaResponse:
      type: object
      additionalProperties: false
      required: [gateway_replica, replayed]
      properties:
        gateway_replica:
          $ref: "#/components/schemas/GatewayReplicaView"
        replayed:
          type: boolean

    GatewayReplicaListResponse:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items:
          type: array
          maxItems: 255
          items:
            $ref: "#/components/schemas/GatewayReplicaView"
        next_after:
          $ref: "#/components/schemas/GatewayReplicaId"

    GatewayReplicaView:
      type: object
      additionalProperties: false
      required:
        - gateway_replica_id
        - gateway_pool_id
        - edge_cluster_id
        - control_endpoint
        - peer_endpoint
        - bootstrap_endpoint
        - software_version
        - wire_version
        - capabilities
        - state
        - credential_state
        - resource_version
        - created_at_unix_ms
        - updated_at_unix_ms
      properties:
        gateway_replica_id:
          $ref: "#/components/schemas/GatewayReplicaId"
        gateway_pool_id:
          $ref: "#/components/schemas/GatewayPoolId"
        edge_cluster_id:
          $ref: "#/components/schemas/EdgeClusterId"
        control_endpoint:
          $ref: "#/components/schemas/GatewayEndpoint"
        peer_endpoint:
          $ref: "#/components/schemas/GatewayEndpoint"
        bootstrap_endpoint:
          $ref: "#/components/schemas/GatewayEndpoint"
        software_version:
          type: string
          minLength: 1
          maxLength: 256
        wire_version:
          type: integer
          const: 1
        capabilities:
          type: array
          maxItems: 128
          uniqueItems: true
          items:
            type: string
            minLength: 1
            maxLength: 128
        last_heartbeat_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"
        state:
          $ref: "#/components/schemas/GatewayReplicaState"
        credential_state:
          $ref: "#/components/schemas/GatewayCredentialState"
        certificate_generation:
          $ref: "#/components/schemas/PositiveCanonicalU64"
        certificate_not_after_unix_ms:
          $ref: "#/components/schemas/UnixMillis"
        resource_version:
          $ref: "#/components/schemas/CanonicalU64"
        created_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"
        updated_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"

    GatewayPoolState:
      type: string
      enum: [provisioning, ready, draining, disabled]

    GatewayReplicaState:
      type: string
      enum: [pending, active, draining, revoked]

    GatewayCredentialState:
      type: string
      enum:
        [
          pending_activation,
          pending_certificate_delivery,
          active,
          expired,
          revoked,
        ]

    GatewayReplicaCount:
      type: integer
      minimum: 1
      maximum: 65535

    GatewayEndpoint:
      type: string
      description: canonical HTTPS origin；开发环境允许 literal loopback HTTP origin；不得包含 userinfo、path、query、fragment 或尾随斜杠。
      format: uri
      minLength: 9
      maxLength: 2048
      pattern: '^(?:https://(?:\[[0-9A-Fa-f:.]+\]|[^@/?#\s:]+)(?::[0-9]{1,5})?|http://(?:localhost|127(?:\.[0-9]{1,3}){3}|\[::1\])(?::[0-9]{1,5})?)$'

    QueryTenantListRequest:
      type: object
      additionalProperties: false
      properties:
        cursor:
          $ref: "#/components/schemas/PageCursor"
        page_size:
          $ref: "#/components/schemas/PageSize"
        query:
          $ref: "#/components/schemas/SearchQuery"

    QueryTenantListResponse:
      type: object
      additionalProperties: false
      required: [items, can_create_tenant]
      properties:
        items:
          type: array
          maxItems: 100
          items:
            $ref: "#/components/schemas/TenantView"
        next_cursor:
          $ref: "#/components/schemas/PageCursor"
        can_create_tenant:
          type: boolean

    QueryTenantRequest:
      type: object
      additionalProperties: false
      required: [tenant_id]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"

    QueryTenantResponse:
      type: object
      additionalProperties: false
      required: [tenant]
      properties:
        tenant:
          $ref: "#/components/schemas/TenantView"

    CreateTenantRequest:
      type: object
      additionalProperties: false
      required: [tenant_id, display_name]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        display_name:
          $ref: "#/components/schemas/DisplayName"
        description:
          $ref: "#/components/schemas/Description"

    CreateTenantResponse:
      type: object
      additionalProperties: false
      required: [tenant, replayed]
      properties:
        tenant:
          $ref: "#/components/schemas/TenantView"
        replayed:
          type: boolean

    TenantView:
      type: object
      description: 当前 principal 可见的脱敏租户视图，不包含认证策略或存储配置。
      additionalProperties: false
      required:
        - tenant_id
        - display_name
        - resource_version
        - created_at_unix_ms
        - updated_at_unix_ms
        - permissions
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        display_name:
          $ref: "#/components/schemas/DisplayName"
        description:
          $ref: "#/components/schemas/Description"
        resource_version:
          $ref: "#/components/schemas/CanonicalU64"
        created_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"
        updated_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"
        permissions:
          type: array
          maxItems: 128
          uniqueItems: true
          items:
            $ref: "#/components/schemas/PermissionName"

    QueryStorageVolumeListRequest:
      type: object
      additionalProperties: false
      required: [tenant_id]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        region:
          $ref: "#/components/schemas/RegionName"
        backend_type:
          $ref: "#/components/schemas/StorageBackendType"
        cursor:
          $ref: "#/components/schemas/PageCursor"
        page_size:
          $ref: "#/components/schemas/PageSize"
        query:
          $ref: "#/components/schemas/SearchQuery"

    QueryStorageVolumeListResponse:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items:
          type: array
          maxItems: 100
          items:
            $ref: "#/components/schemas/StorageVolumeView"
        next_cursor:
          $ref: "#/components/schemas/PageCursor"

    QueryStorageVolumeRequest:
      type: object
      additionalProperties: false
      required: [tenant_id, storage_volume_id]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        storage_volume_id:
          $ref: "#/components/schemas/StorageVolumeId"

    QueryStorageVolumeResponse:
      type: object
      additionalProperties: false
      required: [storage_volume]
      properties:
        storage_volume:
          $ref: "#/components/schemas/StorageVolumeView"

    CreateStorageVolumeRequest:
      type: object
      additionalProperties: false
      required:
        - tenant_id
        - storage_volume_id
        - display_name
        - edge_cluster_id
        - region
        - backend_type
        - access_mode
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        storage_volume_id:
          $ref: "#/components/schemas/StorageVolumeId"
        display_name:
          $ref: "#/components/schemas/DisplayName"
        edge_cluster_id:
          $ref: "#/components/schemas/EdgeClusterId"
        region:
          $ref: "#/components/schemas/RegionName"
        backend_type:
          $ref: "#/components/schemas/StorageBackendType"
        access_mode:
          $ref: "#/components/schemas/StorageAccessMode"
        allowed_delivery_modes:
          type: array
          minItems: 1
          uniqueItems: true
          items:
            $ref: "#/components/schemas/SnapshotDeliveryMode"
        hardlink_policy:
          $ref: "#/components/schemas/HardlinkPolicy"
        max_whole_file_bytes:
          $ref: "#/components/schemas/DecimalU64"
        copy_reserve_bytes:
          $ref: "#/components/schemas/DecimalU64"
        pvc_reference:
          $ref: "#/components/schemas/PvcReference"
        nfs_reference:
          $ref: "#/components/schemas/NfsReference"
      oneOf:
        - properties:
            backend_type:
              const: pvc
            nfs_reference: false
          required: [pvc_reference]
        - properties:
            backend_type:
              const: nfs
            pvc_reference: false
          required: [nfs_reference]

    CreateStorageVolumeResponse:
      type: object
      additionalProperties: false
      required: [storage_volume, replayed]
      properties:
        storage_volume:
          $ref: "#/components/schemas/StorageVolumeView"
        replayed:
          type: boolean

    StorageVolumeView:
      type: object
      description: |
        租户登记的稳定逻辑存储身份。PVC namespace/claim 可以公开用于运维识别；NFS
        server、export path、凭据、中心挂载路径和 Agent ownership 不属于公开视图。只有 `state=ready`
        可用于创建新的 Playground 或 Snapshot；`degraded` 与 `unavailable` 仍可查询，但禁止新放置。
      additionalProperties: false
      required:
        - tenant_id
        - storage_volume_id
        - display_name
        - edge_cluster_id
        - region
        - backend_type
        - access_mode
        - allowed_delivery_modes
        - hardlink_policy
        - max_whole_file_bytes
        - copy_reserve_bytes
        - state
        - resource_version
        - lifecycle
        - created_at_unix_ms
        - updated_at_unix_ms
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        storage_volume_id:
          $ref: "#/components/schemas/StorageVolumeId"
        display_name:
          $ref: "#/components/schemas/DisplayName"
        edge_cluster_id:
          $ref: "#/components/schemas/EdgeClusterId"
        region:
          $ref: "#/components/schemas/RegionName"
        backend_type:
          $ref: "#/components/schemas/StorageBackendType"
        access_mode:
          $ref: "#/components/schemas/StorageAccessMode"
        allowed_delivery_modes:
          type: array
          items:
            $ref: "#/components/schemas/SnapshotDeliveryMode"
        hardlink_policy:
          $ref: "#/components/schemas/HardlinkPolicy"
        max_whole_file_bytes:
          $ref: "#/components/schemas/DecimalU64"
        copy_reserve_bytes:
          $ref: "#/components/schemas/DecimalU64"
        pvc_reference:
          $ref: "#/components/schemas/PvcReference"
        state:
          $ref: "#/components/schemas/StorageVolumeState"
        resource_version:
          $ref: "#/components/schemas/CanonicalU64"
        lifecycle:
          $ref: "#/components/schemas/ResourceLifecycleView"
        created_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"
        updated_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"

    CreateStorageEnrollmentTokenRequest:
      type: object
      description: |
        为待接入 PVC 冻结完整公开 Volume descriptor 并创建 bootstrap token。该请求不要求
        StorageVolume 已存在，也不接受现有 Volume resource version。
      additionalProperties: false
      required:
        - tenant_id
        - token_request_id
        - storage_volume_id
        - display_name
        - edge_cluster_id
        - region
        - access_mode
        - pvc_reference
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        token_request_id:
          $ref: "#/components/schemas/RequestId"
        storage_volume_id:
          $ref: "#/components/schemas/StorageVolumeId"
        display_name:
          $ref: "#/components/schemas/DisplayName"
        edge_cluster_id:
          $ref: "#/components/schemas/EdgeClusterId"
        region:
          $ref: "#/components/schemas/RegionName"
        access_mode:
          $ref: "#/components/schemas/StorageEnrollmentAccessMode"
        pvc_reference:
          $ref: "#/components/schemas/PvcReference"

    CreateStorageEnrollmentTokenResponse:
      type: object
      description: |
        原始 bootstrap token 只在本响应中返回。相同 token_request_id 和完全相同 payload 重放时返回
        同一 token_id、bootstrap_token、volume_descriptor_digest 和 expires_at_unix_ms，并将
        replayed 设为 true。Agent 配置必须使用该服务端冻结的 descriptor digest。
      additionalProperties: false
      required:
        [
          token_id,
          bootstrap_token,
          volume_descriptor_digest,
          expires_at_unix_ms,
          replayed,
        ]
      properties:
        token_id:
          $ref: "#/components/schemas/StorageEnrollmentTokenId"
        bootstrap_token:
          $ref: "#/components/schemas/StorageEnrollmentBootstrapToken"
        volume_descriptor_digest:
          $ref: "#/components/schemas/ContentDigest"
        expires_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"
        replayed:
          type: boolean

    QueryStorageEnrollmentListRequest:
      type: object
      additionalProperties: false
      required: [tenant_id]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        state:
          $ref: "#/components/schemas/StorageEnrollmentState"
        registration_kind:
          $ref: "#/components/schemas/StorageEnrollmentRegistrationKind"
        cursor:
          $ref: "#/components/schemas/PageCursor"
        page_size:
          $ref: "#/components/schemas/PageSize"
        query:
          $ref: "#/components/schemas/SearchQuery"

    QueryStorageEnrollmentListResponse:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items:
          type: array
          maxItems: 100
          items:
            $ref: "#/components/schemas/StorageEnrollmentView"
        next_cursor:
          $ref: "#/components/schemas/PageCursor"

    QueryStorageEnrollmentRequest:
      type: object
      additionalProperties: false
      required: [tenant_id, storage_enrollment_id]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        storage_enrollment_id:
          $ref: "#/components/schemas/StorageEnrollmentId"

    QueryStorageEnrollmentResponse:
      type: object
      additionalProperties: false
      required: [enrollment]
      properties:
        enrollment:
          $ref: "#/components/schemas/StorageEnrollmentView"

    ApproveStorageEnrollmentRequest:
      type: object
      description: |
        使用 expected_resource_version 对 pending_approval enrollment 执行 CAS。replacement 必须显式
        confirm_replacement=true；approval_request_id 与完整 payload 构成稳定幂等 identity，并与
        rejection_request_id 共享 Tenant 级 decision request identity 命名空间。
      additionalProperties: false
      required:
        - tenant_id
        - storage_enrollment_id
        - approval_request_id
        - expected_resource_version
        - confirm_replacement
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        storage_enrollment_id:
          $ref: "#/components/schemas/StorageEnrollmentId"
        approval_request_id:
          $ref: "#/components/schemas/RequestId"
        expected_resource_version:
          $ref: "#/components/schemas/CanonicalU64"
        confirm_replacement:
          type: boolean

    ApproveStorageEnrollmentResponse:
      type: object
      description: |
        审批事务返回更新后的 Enrollment 和创建或精确绑定的 StorageVolumeView。返回时 Enrollment 为
        approved，StorageVolume 固定为 unavailable；后续认证 Agent session 和健康 probe 才能分别
        推进到 enrolled 与 ready。
      additionalProperties: false
      required: [enrollment, storage_volume, replayed]
      properties:
        enrollment:
          $ref: "#/components/schemas/StorageEnrollmentView"
        storage_volume:
          $ref: "#/components/schemas/StorageVolumeView"
        replayed:
          type: boolean

    CompleteStorageRecoveryRequest:
      type: object
      description: |
        使用 expected_resource_version 和当前 owner_generation 完成 replacement recovery fence。
        Tenant、Enrollment 与 Owner generation 必须全部匹配服务端当前状态。
      additionalProperties: false
      required:
        - tenant_id
        - storage_enrollment_id
        - expected_resource_version
        - owner_generation
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        storage_enrollment_id:
          $ref: "#/components/schemas/StorageEnrollmentId"
        expected_resource_version:
          $ref: "#/components/schemas/CanonicalU64"
        owner_generation:
          $ref: "#/components/schemas/CanonicalU64"

    CompleteStorageRecoveryResponse:
      type: object
      additionalProperties: false
      required: [enrollment, storage_volume]
      properties:
        enrollment:
          $ref: "#/components/schemas/StorageEnrollmentView"
        storage_volume:
          $ref: "#/components/schemas/StorageVolumeView"

    RejectStorageEnrollmentRequest:
      type: object
      description: |
        使用 expected_resource_version 对 pending_approval enrollment 执行 CAS；rejection_request_id
        与完整 payload 构成稳定幂等 identity，并与 approval_request_id 共享 Tenant 级 decision request
        identity 命名空间。
      additionalProperties: false
      required:
        - tenant_id
        - storage_enrollment_id
        - rejection_request_id
        - expected_resource_version
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        storage_enrollment_id:
          $ref: "#/components/schemas/StorageEnrollmentId"
        rejection_request_id:
          $ref: "#/components/schemas/RequestId"
        expected_resource_version:
          $ref: "#/components/schemas/CanonicalU64"
        reason:
          type: string
          minLength: 1
          maxLength: 2048

    RejectStorageEnrollmentResponse:
      type: object
      additionalProperties: false
      required: [enrollment, replayed]
      properties:
        enrollment:
          $ref: "#/components/schemas/StorageEnrollmentView"
        replayed:
          type: boolean

    StorageEnrollmentView:
      type: object
      description: |
        Agent 使用单次 bootstrap token 提交的脱敏 PVC enrollment。pending_approval 自 created_at 起
        24 小时未审核即进入 expired；终态仍保留 expires_at_unix_ms 作为原审批期限。approved 仅表示
        管理员已审核并建立 unavailable Volume；认证 Agent session 和健康 probe 建立后才进入 enrolled。
        本视图不包含 token、token key ID、SPKI、签名、CSR、证书、PVC UID、挂载详情、拒绝原因或
        内部 Agent/session identity。
      additionalProperties: false
      required:
        - tenant_id
        - storage_enrollment_id
        - storage_volume_id
        - display_name
        - edge_cluster_id
        - region
        - access_mode
        - pvc_reference
        - registration_kind
        - state
        - agent_version
        - identity_fingerprint
        - proof_of_possession_status
        - probe
        - resource_version
        - created_at_unix_ms
        - updated_at_unix_ms
        - expires_at_unix_ms
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        storage_enrollment_id:
          $ref: "#/components/schemas/StorageEnrollmentId"
        storage_volume_id:
          $ref: "#/components/schemas/StorageVolumeId"
        display_name:
          $ref: "#/components/schemas/DisplayName"
        edge_cluster_id:
          $ref: "#/components/schemas/EdgeClusterId"
        region:
          $ref: "#/components/schemas/RegionName"
        access_mode:
          $ref: "#/components/schemas/StorageEnrollmentAccessMode"
        pvc_reference:
          $ref: "#/components/schemas/PvcReference"
        registration_kind:
          $ref: "#/components/schemas/StorageEnrollmentRegistrationKind"
        state:
          $ref: "#/components/schemas/StorageEnrollmentState"
        agent_version:
          type: string
          minLength: 1
          maxLength: 128
        identity_fingerprint:
          type: string
          description: Agent installation 公钥 identity 的摘要，仅供审批比对；不是 mount/device fingerprint。
          minLength: 64
          maxLength: 64
          pattern: "^[0-9a-f]{64}$"
        proof_of_possession_status:
          $ref: "#/components/schemas/StorageEnrollmentProofOfPossessionStatus"
        probe:
          $ref: "#/components/schemas/StorageEnrollmentProbeSummary"
        resource_version:
          $ref: "#/components/schemas/CanonicalU64"
        created_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"
        updated_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"
        expires_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"
        reviewed_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"
    StorageEnrollmentProbeSummary:
      type: object
      description: Agent bootstrap 时提交的最小脱敏探测摘要，不包含挂载或设备 identity。
      additionalProperties: false
      required:
        - observed_access_mode
        - descriptor_matches
        - observed_at_unix_ms
      properties:
        observed_access_mode:
          type: string
          enum: [read_only, read_write]
        descriptor_matches:
          type: boolean
        observed_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"

    StorageEnrollmentProofOfPossessionStatus:
      type: string
      description: 服务端对 canonical bootstrap request 完成 Ed25519 验签后持久化的脱敏状态；不接受客户端声明。
      enum: [verified]

    StorageEnrollmentRegistrationKind:
      type: string
      description: |
        initial 建立首个 Owner，可创建缺失的 Volume，或绑定 descriptor 完全一致且 unavailable、无活动
        Owner 的既有 PVC Volume；replacement 接管既有 Owner，审批时要求 confirm_replacement=true。
      enum: [initial, replacement]

    StorageEnrollmentState:
      type: string
      description: |
        pending_approval 可被审核或在 24 小时后过期；approved 等待认证 Agent session 和健康 probe；
        enrolled 表示 enrollment 已生效。rejected 与 expired 为终态。
      enum: [pending_approval, approved, enrolled, rejected, expired]

    StorageEnrollmentAccessMode:
      type: string
      description: 0.0.1 Volume-bound Agent 必须以 RW 方式挂载业务 PVC；只读 PVC 不可进入 enrollment。
      enum: [read_write_many, read_write_once]

    PvcReference:
      type: object
      additionalProperties: false
      required: [namespace, claim_name]
      properties:
        namespace:
          $ref: "#/components/schemas/KubernetesNamespaceName"
        claim_name:
          $ref: "#/components/schemas/KubernetesPvcClaimName"

    NfsReference:
      type: object
      description: 仅在登记请求中使用；服务端不得在公开 StorageVolumeView 中回显。
      additionalProperties: false
      required: [server, export_path]
      properties:
        server:
          type: string
          minLength: 1
          maxLength: 253
        export_path:
          type: string
          minLength: 1
          maxLength: 1024
          pattern: "^/.*$"

    QueryProjectListRequest:
      type: object
      additionalProperties: false
      required: [tenant_id]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        cursor:
          $ref: "#/components/schemas/PageCursor"
        page_size:
          $ref: "#/components/schemas/PageSize"
        query:
          $ref: "#/components/schemas/SearchQuery"

    QueryProjectListResponse:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items:
          type: array
          maxItems: 100
          items:
            $ref: "#/components/schemas/ProjectView"
        next_cursor:
          $ref: "#/components/schemas/PageCursor"

    CreateProjectRequest:
      type: object
      additionalProperties: false
      required: [tenant_id, project_id, display_name]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        project_id:
          $ref: "#/components/schemas/ProjectId"
        display_name:
          $ref: "#/components/schemas/DisplayName"
        description:
          $ref: "#/components/schemas/Description"

    CreateProjectResponse:
      type: object
      additionalProperties: false
      required: [project, replayed]
      properties:
        project:
          $ref: "#/components/schemas/ProjectView"
        replayed:
          type: boolean

    ProjectView:
      type: object
      additionalProperties: false
      required:
        - tenant_id
        - project_id
        - display_name
        - resource_version
        - created_at_unix_ms
        - updated_at_unix_ms
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        project_id:
          $ref: "#/components/schemas/ProjectId"
        display_name:
          $ref: "#/components/schemas/DisplayName"
        description:
          $ref: "#/components/schemas/Description"
        resource_version:
          $ref: "#/components/schemas/CanonicalU64"
        created_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"
        updated_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"

    QueryArtifactListRequest:
      type: object
      additionalProperties: false
      required: [tenant_id]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        project_id:
          $ref: "#/components/schemas/ProjectId"
        cursor:
          $ref: "#/components/schemas/PageCursor"
        page_size:
          $ref: "#/components/schemas/PageSize"
        query:
          $ref: "#/components/schemas/SearchQuery"

    QueryArtifactListResponse:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items:
          type: array
          maxItems: 100
          items:
            $ref: "#/components/schemas/ArtifactView"
        next_cursor:
          $ref: "#/components/schemas/PageCursor"

    QueryArtifactRequest:
      type: object
      additionalProperties: false
      required: [tenant_id, project_id, artifact_id]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        project_id:
          $ref: "#/components/schemas/ProjectId"
        artifact_id:
          $ref: "#/components/schemas/ArtifactId"

    QueryArtifactResponse:
      type: object
      additionalProperties: false
      required: [artifact]
      properties:
        artifact:
          $ref: "#/components/schemas/ArtifactView"

    CreateArtifactRequest:
      type: object
      additionalProperties: false
      required:
        [tenant_id, project_id, artifact_id, display_name, initialization]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        project_id:
          $ref: "#/components/schemas/ProjectId"
        artifact_id:
          $ref: "#/components/schemas/ArtifactId"
        display_name:
          $ref: "#/components/schemas/DisplayName"
        description:
          $ref: "#/components/schemas/Description"
        initialization:
          $ref: "#/components/schemas/ArtifactInitialization"

    ArtifactInitialization:
      oneOf:
        - $ref: "#/components/schemas/EmptyArtifactInitialization"
        - $ref: "#/components/schemas/DerivedArtifactInitialization"
      discriminator:
        propertyName: mode
        mapping:
          empty: "#/components/schemas/EmptyArtifactInitialization"
          derived: "#/components/schemas/DerivedArtifactInitialization"

    EmptyArtifactInitialization:
      type: object
      additionalProperties: false
      required: [mode]
      properties:
        mode:
          type: string
          const: empty

    DerivedArtifactInitialization:
      type: object
      description: |
        保留的派生初始化形态。来源 Artifact 必须属于当前 Tenant，来源 Commit 必须属于显式来源
        Project/Artifact；当前 capability 无法验证 Commit 归属，因此 create 会稳定返回 409。
      additionalProperties: false
      required: [mode, source_project_id, source_artifact_id, source_commit_id]
      properties:
        mode:
          type: string
          const: derived
        source_project_id:
          $ref: "#/components/schemas/ProjectId"
        source_artifact_id:
          $ref: "#/components/schemas/ArtifactId"
        source_commit_id:
          $ref: "#/components/schemas/CommitId"

    CreateArtifactResponse:
      type: object
      additionalProperties: false
      required: [artifact, replayed]
      properties:
        artifact:
          $ref: "#/components/schemas/ArtifactView"
        replayed:
          type: boolean

    ArtifactView:
      type: object
      description: |
        Artifact 是不绑定 Region 或 StorageVolume 的版本化逻辑数据资产。初始化血缘只暴露
        同 Tenant 的逻辑来源，不暴露 ArtifactPlacement、Manifest 或物理对象位置。`artifact_id`
        在 Tenant 内唯一，`project_id` 表示其唯一业务归属。
      additionalProperties: false
      required:
        - tenant_id
        - project_id
        - artifact_id
        - display_name
        - initialization
        - resource_version
        - lifecycle
        - created_at_unix_ms
        - updated_at_unix_ms
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        project_id:
          $ref: "#/components/schemas/ProjectId"
        artifact_id:
          $ref: "#/components/schemas/ArtifactId"
        display_name:
          $ref: "#/components/schemas/DisplayName"
        description:
          $ref: "#/components/schemas/Description"
        initialization:
          $ref: "#/components/schemas/ArtifactInitialization"
        head_commit_id:
          $ref: "#/components/schemas/CommitId"
        resource_version:
          $ref: "#/components/schemas/CanonicalU64"
        lifecycle:
          $ref: "#/components/schemas/ResourceLifecycleView"
        created_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"
        updated_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"

    QueryArtifactCommitGraphRequest:
      type: object
      additionalProperties: false
      required: [tenant_id, project_id, artifact_id]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        project_id:
          $ref: "#/components/schemas/ProjectId"
        artifact_id:
          $ref: "#/components/schemas/ArtifactId"
        cursor:
          $ref: "#/components/schemas/PageCursor"
        page_size:
          $ref: "#/components/schemas/PageSize"

    QueryArtifactCommitGraphResponse:
      type: object
      additionalProperties: false
      required: [graph]
      properties:
        graph:
          $ref: "#/components/schemas/CommitGraphView"

    CommitGraphView:
      type: object
      additionalProperties: false
      required: [graph_version, nodes]
      properties:
        graph_version:
          $ref: "#/components/schemas/CanonicalU64"
        head_commit_id:
          $ref: "#/components/schemas/CommitId"
        nodes:
          type: array
          maxItems: 100
          items:
            $ref: "#/components/schemas/CommitNode"
        next_cursor:
          $ref: "#/components/schemas/PageCursor"

    CommitNode:
      type: object
      description: 单 parent 的公开 Commit 节点，不包含 Directory 或 Manifest identity。
      additionalProperties: false
      required: [commit_id, message, tag_names, data_layout, created_at_unix_ms]
      properties:
        commit_id:
          $ref: "#/components/schemas/CommitId"
        parent_commit_id:
          $ref: "#/components/schemas/CommitId"
        message:
          type: string
          minLength: 1
          maxLength: 4096
        description:
          $ref: "#/components/schemas/Description"
        tag_names:
          type: array
          maxItems: 20
          uniqueItems: true
          items:
            $ref: "#/components/schemas/TagName"
        data_layout:
          $ref: "#/components/schemas/DataLayout"
        created_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"

    DataLayout:
      type: string
      description: Commit 内所有文件使用的统一数据布局。
      enum: [fast_cdc, whole_file]

    QueryArtifactCommitDiffRequest:
      type: object
      additionalProperties: false
      required: [tenant_id, project_id, artifact_id, commit_id]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        project_id:
          $ref: "#/components/schemas/ProjectId"
        artifact_id:
          $ref: "#/components/schemas/ArtifactId"
        commit_id:
          $ref: "#/components/schemas/CommitId"
        base_commit_id:
          $ref: "#/components/schemas/CommitId"

    QueryArtifactCommitDiffResponse:
      type: object
      additionalProperties: false
      required: [diff]
      properties:
        diff:
          $ref: "#/components/schemas/CommitDiffView"

    CommitDiffView:
      type: object
      description: 目标 Commit 与显式 base 或其单一 parent 的公开文件级差异。
      additionalProperties: false
      required: [target_commit, summary, changes]
      properties:
        base_commit:
          $ref: "#/components/schemas/CommitNode"
        target_commit:
          $ref: "#/components/schemas/CommitNode"
        summary:
          $ref: "#/components/schemas/CommitDiffSummary"
        changes:
          type: array
          maxItems: 10000
          items:
            $ref: "#/components/schemas/CommitDiffEntry"

    CommitDiffSummary:
      type: object
      additionalProperties: false
      required:
        - files_added
        - files_modified
        - files_deleted
        - files_renamed
        - bytes_added
        - bytes_removed
      properties:
        files_added:
          $ref: "#/components/schemas/CanonicalU64"
        files_modified:
          $ref: "#/components/schemas/CanonicalU64"
        files_deleted:
          $ref: "#/components/schemas/CanonicalU64"
        files_renamed:
          $ref: "#/components/schemas/CanonicalU64"
        bytes_added:
          $ref: "#/components/schemas/CanonicalU64"
        bytes_removed:
          $ref: "#/components/schemas/CanonicalU64"

    CommitDiffEntry:
      type: object
      additionalProperties: false
      required: [change_type, path]
      properties:
        change_type:
          type: string
          enum: [added, modified, deleted, renamed]
        path:
          type: string
          minLength: 1
          maxLength: 4096
        previous_path:
          type: string
          minLength: 1
          maxLength: 4096
        old_size_bytes:
          $ref: "#/components/schemas/CanonicalU64"
        new_size_bytes:
          $ref: "#/components/schemas/CanonicalU64"

    QueryPlaygroundListRequest:
      type: object
      additionalProperties: false
      dependentRequired:
        artifact_id: [project_id]
      required: [tenant_id]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        project_id:
          $ref: "#/components/schemas/ProjectId"
        artifact_id:
          $ref: "#/components/schemas/ArtifactId"
        region:
          $ref: "#/components/schemas/RegionName"
        state:
          $ref: "#/components/schemas/PlaygroundState"
        cursor:
          $ref: "#/components/schemas/PageCursor"
        page_size:
          $ref: "#/components/schemas/PageSize"
        query:
          $ref: "#/components/schemas/SearchQuery"

    QueryPlaygroundListResponse:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items:
          type: array
          maxItems: 100
          items:
            $ref: "#/components/schemas/PlaygroundView"
        next_cursor:
          $ref: "#/components/schemas/PageCursor"

    QueryPlaygroundRequest:
      type: object
      additionalProperties: false
      required: [tenant_id, project_id, artifact_id, playground_id]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        project_id:
          $ref: "#/components/schemas/ProjectId"
        artifact_id:
          $ref: "#/components/schemas/ArtifactId"
        playground_id:
          $ref: "#/components/schemas/PlaygroundId"

    QueryPlaygroundResponse:
      type: object
      additionalProperties: false
      required: [playground]
      properties:
        playground:
          $ref: "#/components/schemas/PlaygroundView"

    CreatePlaygroundRequest:
      type: object
      description: |
        创建请求必须引用同 Tenant/Project 下已存在的 Artifact，并明确选择同 Tenant、`state=ready`
        的 StorageVolume。Playground 是该 Artifact 某个固定 Commit 的可写派生视图，不能创建或
        重定义 Artifact；Region 由 Volume 派生，不能由调用方另行指定。
      additionalProperties: false
      required:
        - tenant_id
        - project_id
        - artifact_id
        - playground_id
        - storage_volume_id
        - display_name
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        project_id:
          $ref: "#/components/schemas/ProjectId"
        artifact_id:
          $ref: "#/components/schemas/ArtifactId"
        playground_id:
          $ref: "#/components/schemas/PlaygroundId"
        storage_volume_id:
          $ref: "#/components/schemas/StorageVolumeId"
        display_name:
          $ref: "#/components/schemas/DisplayName"
        base_commit_id:
          $ref: "#/components/schemas/CommitId"
          description: |
            可选的 canonical Commit 内容摘要。显式值必须属于请求 scope 指定的 Artifact，可以是
            当前 Head 或历史 Commit；省略时服务端冻结创建瞬间的当前 Head。空 Artifact 必须省略，
            且服务端不会为其伪造 base Commit。

    CreatePlaygroundResponse:
      type: object
      additionalProperties: false
      required: [playground, replayed]
      properties:
        playground:
          $ref: "#/components/schemas/PlaygroundView"
        replayed:
          type: boolean

    PlaygroundState:
      type: string
      enum: [creating, ready, abnormal]

    PlaygroundStorageAvailability:
      type: string
      description: |
        Playground 所在 StorageVolume 的实时可达性。`unknown` 表示服务端当前无法确认存储状态；
        只有 `ready` 可以执行扫描、Pre-commit 检测等依赖 Agent 的实时操作。中心已持久化的
        Index、文件清单、变化和 Profile 不受该字段限制。
      enum: [ready, degraded, unavailable, unknown]

    PlaygroundView:
      type: object
      description: |
        Playground 的 `tenant_id + project_id + artifact_id` 是不可变来源 scope，Region 始终由所选
        StorageVolume 派生。`state` 只表达持久生命周期，`storage_availability` 表达所在存储的
        实时可达性；scanning、hashing、uploading 和 validating 仅属于独立 Pre-commit 流程。
      additionalProperties: false
      required:
        - tenant_id
        - project_id
        - artifact_id
        - playground_id
        - storage_volume_id
        - region
        - display_name
        - index_version
        - state
        - storage_availability
        - resource_version
        - lifecycle
        - created_at_unix_ms
        - updated_at_unix_ms
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        project_id:
          $ref: "#/components/schemas/ProjectId"
        artifact_id:
          $ref: "#/components/schemas/ArtifactId"
        playground_id:
          $ref: "#/components/schemas/PlaygroundId"
        storage_volume_id:
          $ref: "#/components/schemas/StorageVolumeId"
        region:
          $ref: "#/components/schemas/RegionName"
        display_name:
          $ref: "#/components/schemas/DisplayName"
        base_commit_id:
          $ref: "#/components/schemas/CommitId"
        head_commit_id:
          $ref: "#/components/schemas/CommitId"
        index_version:
          $ref: "#/components/schemas/IndexVersion"
        state:
          $ref: "#/components/schemas/PlaygroundState"
        storage_availability:
          $ref: "#/components/schemas/PlaygroundStorageAvailability"
        active_precommit_id:
          $ref: "#/components/schemas/PreCommitId"
        issue:
          $ref: "#/components/schemas/ResourceIssueSummary"
        resource_version:
          $ref: "#/components/schemas/CanonicalU64"
        lifecycle:
          $ref: "#/components/schemas/ResourceLifecycleView"
        created_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"
        updated_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"

    StartPreCommitRequest:
      type: object
      description: |
        创建新 Pre-commit 会话的请求。服务端内部冻结当前 Head；客户端不提交
        `source_head_commit_id`，也不能通过读取页面隐式发送本请求。
      additionalProperties: false
      required:
        - tenant_id
        - project_id
        - artifact_id
        - playground_id
        - precommit_request_id
        - expected_index_version
        - data_layout
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        project_id:
          $ref: "#/components/schemas/ProjectId"
        artifact_id:
          $ref: "#/components/schemas/ArtifactId"
        playground_id:
          $ref: "#/components/schemas/PlaygroundId"
        precommit_request_id:
          $ref: "#/components/schemas/ResourceId"
        expected_index_version:
          $ref: "#/components/schemas/IndexVersion"
        data_layout:
          $ref: "#/components/schemas/DataLayout"

    StartPreCommitResponse:
      type: object
      additionalProperties: false
      required: [precommit, playground, replayed]
      properties:
        precommit:
          $ref: "#/components/schemas/PreCommitView"
        playground:
          $ref: "#/components/schemas/PlaygroundView"
        replayed:
          type: boolean

    QueryPreCommitRequest:
      type: object
      additionalProperties: false
      required: [tenant_id, precommit_id]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        precommit_id:
          $ref: "#/components/schemas/PreCommitId"

    QueryPreCommitResponse:
      type: object
      additionalProperties: false
      required: [precommit]
      properties:
        precommit:
          $ref: "#/components/schemas/PreCommitView"

    RestartPreCommitRequest:
      type: object
      description: |
        仅为 abnormal/cancelled 会话创建下一 attempt，保持 `precommit_id`。running/ready 的重新检测
        使用 cancel 后 start，并获得新的会话 ID。
      additionalProperties: false
      required:
        [tenant_id, precommit_id, restart_request_id, expected_index_version]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        precommit_id:
          $ref: "#/components/schemas/PreCommitId"
        restart_request_id:
          $ref: "#/components/schemas/ResourceId"
        expected_index_version:
          $ref: "#/components/schemas/IndexVersion"

    RestartPreCommitResponse:
      type: object
      additionalProperties: false
      required: [precommit, playground, replayed]
      properties:
        precommit:
          $ref: "#/components/schemas/PreCommitView"
        playground:
          $ref: "#/components/schemas/PlaygroundView"
        replayed:
          type: boolean

    CancelPreCommitRequest:
      type: object
      additionalProperties: false
      required: [tenant_id, precommit_id, cancel_request_id]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        precommit_id:
          $ref: "#/components/schemas/PreCommitId"
        cancel_request_id:
          $ref: "#/components/schemas/ResourceId"

    CancelPreCommitResponse:
      type: object
      additionalProperties: false
      required: [precommit, playground, replayed]
      properties:
        precommit:
          $ref: "#/components/schemas/PreCommitView"
        playground:
          $ref: "#/components/schemas/PlaygroundView"
        replayed:
          type: boolean

    PreCommitState:
      type: string
      description: |
        会话结果状态。可提交结果为 `ready`；阻断或执行失败为 `abnormal`。所有非 running 状态的
        phase 均为 `idle`。
      enum: [running, ready, abnormal, cancelled, committed]

    PreCommitPhase:
      type: string
      description: |
        running 会话的执行阶段；`idle` 用于 ready、abnormal、cancelled 和 committed 终态。
        `ready` 不是 phase。
      enum: [queued, scanning, hashing, uploading, validating, idle]

    PreCommitView:
      type: object
      description: |
        Playground 某一 IndexVersion 的冻结候选及其脱敏检查结果。`ready/idle` 必须有候选且 blockers
        为空；`abnormal/idle` 且 blockers 非空在产品中显示为 Blocked。服务端冻结的 Head 不公开。
      additionalProperties: false
      required:
        - tenant_id
        - project_id
        - artifact_id
        - playground_id
        - precommit_id
        - precommit_request_id
        - attempt
        - state
        - phase
        - progress
        - checks
        - warnings
        - blockers
        - source_index_version
        - data_layout
        - created_at_unix_ms
        - updated_at_unix_ms
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        project_id:
          $ref: "#/components/schemas/ProjectId"
        artifact_id:
          $ref: "#/components/schemas/ArtifactId"
        playground_id:
          $ref: "#/components/schemas/PlaygroundId"
        precommit_id:
          $ref: "#/components/schemas/PreCommitId"
        precommit_request_id:
          $ref: "#/components/schemas/ResourceId"
        attempt:
          type: integer
          minimum: 1
          maximum: 2147483647
        state:
          $ref: "#/components/schemas/PreCommitState"
        phase:
          $ref: "#/components/schemas/PreCommitPhase"
        progress:
          $ref: "#/components/schemas/PreCommitProgress"
        checks:
          type: array
          maxItems: 100
          items:
            $ref: "#/components/schemas/PreCommitCheck"
        warnings:
          type: array
          maxItems: 100
          items:
            $ref: "#/components/schemas/PreCommitNotice"
        blockers:
          type: array
          maxItems: 100
          items:
            $ref: "#/components/schemas/PreCommitNotice"
        source_index_version:
          $ref: "#/components/schemas/IndexVersion"
        data_layout:
          $ref: "#/components/schemas/DataLayout"
        candidate_index_version:
          $ref: "#/components/schemas/IndexVersion"
        diff_summary:
          $ref: "#/components/schemas/CommitDiffSummary"
        issue:
          $ref: "#/components/schemas/ResourceIssueSummary"
        committed_commit_id:
          $ref: "#/components/schemas/CommitId"
        created_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"
        updated_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"

    PreCommitProgress:
      type: object
      additionalProperties: false
      required: [percent, files_completed, bytes_completed]
      properties:
        percent:
          type: integer
          minimum: 0
          maximum: 100
        files_completed:
          $ref: "#/components/schemas/CanonicalU64"
        files_total:
          $ref: "#/components/schemas/CanonicalU64"
        bytes_completed:
          $ref: "#/components/schemas/CanonicalU64"
        bytes_total:
          $ref: "#/components/schemas/CanonicalU64"

    PreCommitCheck:
      type: object
      additionalProperties: false
      required: [check_id, status, summary]
      properties:
        check_id:
          $ref: "#/components/schemas/ResourceId"
        status:
          type: string
          enum: [pending, passed, warning, failed]
        summary:
          type: string
          minLength: 1
          maxLength: 1024

    PreCommitNotice:
      type: object
      additionalProperties: false
      required: [code, message]
      properties:
        code:
          $ref: "#/components/schemas/ErrorCode"
        message:
          type: string
          minLength: 1
          maxLength: 2048
        path:
          $ref: "#/components/schemas/LogicalPath"

    CommitPlaygroundRequest:
      type: object
      description: |
        消费 ready/idle Pre-commit 候选。服务端用该 attempt 内部冻结的 Head 执行 CAS；客户端只提交
        expected candidate IndexVersion，不提交 expected/source Head。
      additionalProperties: false
      required:
        - tenant_id
        - project_id
        - artifact_id
        - playground_id
        - commit_request_id
        - precommit_id
        - expected_candidate_index_version
        - message
        - data_layout
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        project_id:
          $ref: "#/components/schemas/ProjectId"
        artifact_id:
          $ref: "#/components/schemas/ArtifactId"
        playground_id:
          $ref: "#/components/schemas/PlaygroundId"
        commit_request_id:
          $ref: "#/components/schemas/ResourceId"
        precommit_id:
          $ref: "#/components/schemas/PreCommitId"
        expected_candidate_index_version:
          $ref: "#/components/schemas/IndexVersion"
        data_layout:
          $ref: "#/components/schemas/DataLayout"
        message:
          type: string
          minLength: 1
          maxLength: 4096
        description:
          $ref: "#/components/schemas/Description"
        tag_names:
          type: array
          maxItems: 20
          uniqueItems: true
          items:
            $ref: "#/components/schemas/TagName"

    CommitPlaygroundResponse:
      type: object
      additionalProperties: false
      required: [commit, playground, consumed_precommit, replayed]
      properties:
        commit:
          $ref: "#/components/schemas/CommitNode"
        playground:
          $ref: "#/components/schemas/PlaygroundView"
        consumed_precommit:
          $ref: "#/components/schemas/PreCommitView"
        replayed:
          type: boolean

    QueryPlaygroundFileListRequest:
      type: object
      additionalProperties: false
      required: [tenant_id, project_id, artifact_id, playground_id]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        project_id:
          $ref: "#/components/schemas/ProjectId"
        artifact_id:
          $ref: "#/components/schemas/ArtifactId"
        playground_id:
          $ref: "#/components/schemas/PlaygroundId"
        path_prefix:
          $ref: "#/components/schemas/LogicalPath"
        format:
          $ref: "#/components/schemas/FileFormat"
        cursor:
          $ref: "#/components/schemas/PageCursor"
        page_size:
          $ref: "#/components/schemas/PageSize"

    QueryPlaygroundFileListResponse:
      type: object
      additionalProperties: false
      required: [index_version, items]
      properties:
        index_version:
          $ref: "#/components/schemas/IndexVersion"
        items:
          type: array
          maxItems: 100
          items:
            $ref: "#/components/schemas/LogicalFileEntry"
        next_cursor:
          $ref: "#/components/schemas/PageCursor"

    QueryPlaygroundChangeListRequest:
      type: object
      additionalProperties: false
      required: [tenant_id, project_id, artifact_id, playground_id]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        project_id:
          $ref: "#/components/schemas/ProjectId"
        artifact_id:
          $ref: "#/components/schemas/ArtifactId"
        playground_id:
          $ref: "#/components/schemas/PlaygroundId"
        precommit_id:
          $ref: "#/components/schemas/PreCommitId"
        change_type:
          $ref: "#/components/schemas/FileChangeType"
        path_prefix:
          $ref: "#/components/schemas/LogicalPath"
        cursor:
          $ref: "#/components/schemas/PageCursor"
        page_size:
          $ref: "#/components/schemas/PageSize"

    QueryPlaygroundChangeListResponse:
      type: object
      additionalProperties: false
      required: [source, index_version, summary, items]
      properties:
        source:
          type: string
          enum: [workspace, precommit]
        precommit_id:
          $ref: "#/components/schemas/PreCommitId"
        index_version:
          $ref: "#/components/schemas/IndexVersion"
        summary:
          $ref: "#/components/schemas/CommitDiffSummary"
        items:
          type: array
          maxItems: 100
          items:
            $ref: "#/components/schemas/PlaygroundChangeEntry"
        next_cursor:
          $ref: "#/components/schemas/PageCursor"

    PlaygroundChangeEntry:
      type: object
      additionalProperties: false
      required: [change_type, path]
      properties:
        change_type:
          $ref: "#/components/schemas/FileChangeType"
        path:
          $ref: "#/components/schemas/LogicalPath"
        previous_path:
          $ref: "#/components/schemas/LogicalPath"
        old_size_bytes:
          $ref: "#/components/schemas/CanonicalU64"
        new_size_bytes:
          $ref: "#/components/schemas/CanonicalU64"
        format:
          $ref: "#/components/schemas/FileFormat"

    QueryPlaygroundFileMetadataRequest:
      type: object
      additionalProperties: false
      required: [tenant_id, project_id, artifact_id, playground_id, path]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        project_id:
          $ref: "#/components/schemas/ProjectId"
        artifact_id:
          $ref: "#/components/schemas/ArtifactId"
        playground_id:
          $ref: "#/components/schemas/PlaygroundId"
        path:
          $ref: "#/components/schemas/LogicalPath"

    QueryPlaygroundFileMetadataResponse:
      type: object
      additionalProperties: false
      required: [index_version, metadata]
      properties:
        index_version:
          $ref: "#/components/schemas/IndexVersion"
        metadata:
          $ref: "#/components/schemas/FileMetadataView"

    QueryPlaygroundDatasetProfileRequest:
      type: object
      additionalProperties: false
      required: [tenant_id, project_id, artifact_id, playground_id]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        project_id:
          $ref: "#/components/schemas/ProjectId"
        artifact_id:
          $ref: "#/components/schemas/ArtifactId"
        playground_id:
          $ref: "#/components/schemas/PlaygroundId"

    QueryPlaygroundDatasetProfileResponse:
      type: object
      additionalProperties: false
      required: [index_version, profile]
      properties:
        index_version:
          $ref: "#/components/schemas/IndexVersion"
        profile:
          $ref: "#/components/schemas/DatasetProfileView"

    LogicalFileEntry:
      type: object
      additionalProperties: false
      required: [path, entry_type]
      properties:
        path:
          $ref: "#/components/schemas/LogicalPath"
        entry_type:
          type: string
          enum: [file, directory]
        size_bytes:
          $ref: "#/components/schemas/CanonicalU64"
        format:
          $ref: "#/components/schemas/FileFormat"
        row_count:
          $ref: "#/components/schemas/CanonicalU64"
        updated_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"

    FileMetadataView:
      type: object
      description: 只包含逻辑文件属性和数据统计，不包含 Manifest、对象或文件系统位置。
      additionalProperties: false
      required: [path, size_bytes, format]
      properties:
        path:
          $ref: "#/components/schemas/LogicalPath"
        size_bytes:
          $ref: "#/components/schemas/CanonicalU64"
        format:
          $ref: "#/components/schemas/FileFormat"
        media_type:
          type: string
          minLength: 1
          maxLength: 255
        row_count:
          $ref: "#/components/schemas/CanonicalU64"
        schema:
          $ref: "#/components/schemas/DatasetSchemaView"
        statistics:
          $ref: "#/components/schemas/DatasetStatisticsView"
        quality:
          $ref: "#/components/schemas/DataQualitySummary"
        freshness:
          $ref: "#/components/schemas/FreshnessSummary"

    DatasetProfileState:
      type: string
      enum: [not_declared, ready, rejected]

    DatasetProfileView:
      type: object
      description: Playground 或 Snapshot 派生的只读逻辑画像，不是 Snapshot 创建输入。
      additionalProperties: false
      required: [state]
      properties:
        state:
          $ref: "#/components/schemas/DatasetProfileState"
        summary:
          $ref: "#/components/schemas/DatasetProfileSummary"
        schema:
          $ref: "#/components/schemas/DatasetSchemaView"
        statistics:
          $ref: "#/components/schemas/DatasetStatisticsView"
        quality:
          $ref: "#/components/schemas/DataQualitySummary"
        freshness:
          $ref: "#/components/schemas/FreshnessSummary"
        issue:
          $ref: "#/components/schemas/ResourceIssueSummary"

    DatasetProfileSummary:
      type: object
      additionalProperties: false
      required: [format_count, logical_file_count, logical_size_bytes]
      properties:
        format_count:
          type: integer
          minimum: 0
          maximum: 2147483647
        logical_file_count:
          $ref: "#/components/schemas/CanonicalU64"
        logical_size_bytes:
          $ref: "#/components/schemas/CanonicalU64"
        row_count:
          $ref: "#/components/schemas/CanonicalU64"
        field_count:
          type: integer
          minimum: 0
          maximum: 2147483647

    DatasetSchemaView:
      type: object
      additionalProperties: false
      required: [fields]
      properties:
        fields:
          type: array
          maxItems: 10000
          items:
            $ref: "#/components/schemas/DatasetFieldView"

    DatasetFieldView:
      type: object
      additionalProperties: false
      required: [name, data_type, nullable]
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 1024
        data_type:
          type: string
          minLength: 1
          maxLength: 256
        nullable:
          type: boolean
        description:
          $ref: "#/components/schemas/Description"

    DatasetStatisticsView:
      type: object
      additionalProperties: false
      properties:
        row_count:
          $ref: "#/components/schemas/CanonicalU64"
        column_count:
          type: integer
          minimum: 0
          maximum: 2147483647
        null_value_count:
          $ref: "#/components/schemas/CanonicalU64"
        distinct_value_count:
          $ref: "#/components/schemas/CanonicalU64"

    DataQualitySummary:
      type: object
      additionalProperties: false
      required: [state, checks_total, checks_passed, checks_failed]
      properties:
        state:
          type: string
          enum: [not_evaluated, passed, warning, failed]
        checks_total:
          type: integer
          minimum: 0
          maximum: 2147483647
        checks_passed:
          type: integer
          minimum: 0
          maximum: 2147483647
        checks_failed:
          type: integer
          minimum: 0
          maximum: 2147483647

    FreshnessSummary:
      type: object
      additionalProperties: false
      required: [observed_at_unix_ms]
      properties:
        observed_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"
        source_updated_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"
        age_seconds:
          $ref: "#/components/schemas/CanonicalU64"

    ResourceIssueSummary:
      type: object
      description: 面向用户的脱敏异常摘要，不包含 Agent、挂载、凭据或物理位置。
      additionalProperties: false
      required: [code, message, retryable]
      properties:
        code:
          $ref: "#/components/schemas/ErrorCode"
        message:
          type: string
          minLength: 1
          maxLength: 2048
        retryable:
          type: boolean
        occurred_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"

    FileChangeType:
      type: string
      enum: [added, modified, deleted, renamed]

    FileFormat:
      type: string
      minLength: 1
      maxLength: 128

    QuerySnapshotListRequest:
      type: object
      additionalProperties: false
      dependentRequired:
        artifact_id: [project_id]
      required: [tenant_id]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        project_id:
          $ref: "#/components/schemas/ProjectId"
        artifact_id:
          $ref: "#/components/schemas/ArtifactId"
        commit_id:
          $ref: "#/components/schemas/CommitId"
        state:
          $ref: "#/components/schemas/SnapshotState"
        cursor:
          $ref: "#/components/schemas/PageCursor"
        page_size:
          $ref: "#/components/schemas/PageSize"

    QuerySnapshotListResponse:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items:
          type: array
          maxItems: 100
          items:
            $ref: "#/components/schemas/SnapshotView"
        next_cursor:
          $ref: "#/components/schemas/PageCursor"

    QuerySnapshotRequest:
      type: object
      additionalProperties: false
      required: [tenant_id, snapshot_id]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        snapshot_id:
          $ref: "#/components/schemas/SnapshotId"

    QuerySnapshotResponse:
      type: object
      additionalProperties: false
      required: [snapshot]
      properties:
        snapshot:
          $ref: "#/components/schemas/SnapshotView"

    CreateCommitReplicationRequest:
      type: object
      additionalProperties: false
      required: [tenant_id, project_id, artifact_id, commit_id, target_storage_volume_id, request_id]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        project_id:
          $ref: "#/components/schemas/ProjectId"
        artifact_id:
          $ref: "#/components/schemas/ArtifactId"
        commit_id:
          $ref: "#/components/schemas/CommitId"
        target_storage_volume_id:
          $ref: "#/components/schemas/StorageVolumeId"
        request_id:
          $ref: "#/components/schemas/RequestId"

    QueryCommitReplicationRequest:
      type: object
      additionalProperties: false
      required: [tenant_id, replication_id]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        replication_id:
          $ref: "#/components/schemas/ResourceId"

    QueryCommitReplicationTicketRequest:
      type: object
      additionalProperties: false
      required: [tenant_id, replication_id]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        replication_id:
          $ref: "#/components/schemas/ResourceId"

    QueryCommitReplicationListRequest:
      type: object
      additionalProperties: false
      required: [tenant_id, commit_id]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        commit_id:
          $ref: "#/components/schemas/CommitId"

    RetryCommitReplicationRequest:
      type: object
      additionalProperties: false
      required: [tenant_id, replication_id, expected_attempt]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        replication_id:
          $ref: "#/components/schemas/ResourceId"
        expected_attempt:
          $ref: "#/components/schemas/CanonicalU64"

    CancelCommitReplicationRequest:
      type: object
      additionalProperties: false
      required: [tenant_id, replication_id, expected_attempt]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        replication_id:
          $ref: "#/components/schemas/ResourceId"
        expected_attempt:
          $ref: "#/components/schemas/CanonicalU64"

    QueryCommitPlacementListRequest:
      type: object
      additionalProperties: false
      required: [tenant_id, commit_id]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        commit_id:
          $ref: "#/components/schemas/CommitId"

    QueryCommitAvailabilityRequest:
      type: object
      additionalProperties: false
      required: [tenant_id, commit_id]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        commit_id:
          $ref: "#/components/schemas/CommitId"

    CreateWorkspaceRequest:
      type: object
      additionalProperties: false
      required: [tenant_id, project_id, artifact_id, target_storage_volume_id, request_id]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        project_id:
          $ref: "#/components/schemas/ProjectId"
        artifact_id:
          $ref: "#/components/schemas/ArtifactId"
        base_commit_id:
          $ref: "#/components/schemas/CommitId"
        target_storage_volume_id:
          $ref: "#/components/schemas/StorageVolumeId"
        request_id:
          $ref: "#/components/schemas/RequestId"

    ReplicationView:
      type: object
      additionalProperties: false
      required: [replication_id, tenant_id, commit_id, target_storage_volume_id, attempt, state, object_set_digest, completed_objects, total_objects, completed_bytes, total_bytes]
      properties:
        replication_id:
          $ref: "#/components/schemas/ResourceId"
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        artifact_id:
          $ref: "#/components/schemas/ArtifactId"
        commit_id:
          $ref: "#/components/schemas/CommitId"
        target_storage_volume_id:
          $ref: "#/components/schemas/StorageVolumeId"
        attempt:
          $ref: "#/components/schemas/CanonicalU64"
        state:
          type: string
          enum: [queued, planning, transferring, verifying, published, failed, cancelled]
        object_set_digest:
          $ref: "#/components/schemas/ContentDigest"
        completed_objects:
          $ref: "#/components/schemas/CanonicalU64"
        total_objects:
          $ref: "#/components/schemas/CanonicalU64"
        source_placement_set_id:
          $ref: "#/components/schemas/ResourceId"
        source_storage_volume_id:
          $ref: "#/components/schemas/StorageVolumeId"
        source_edge_cluster_id:
          $ref: "#/components/schemas/ResourceId"
        source_gateway_pool_id:
          $ref: "#/components/schemas/ResourceId"
        source_agent_id:
          $ref: "#/components/schemas/ResourceId"
        source_session_generation:
          $ref: "#/components/schemas/CanonicalU64"
        source_mount_generation:
          $ref: "#/components/schemas/CanonicalU64"
        source_route_generation:
          $ref: "#/components/schemas/CanonicalU64"
        target_edge_cluster_id:
          $ref: "#/components/schemas/ResourceId"
        target_gateway_pool_id:
          $ref: "#/components/schemas/ResourceId"
        target_agent_id:
          $ref: "#/components/schemas/ResourceId"
        target_session_generation:
          $ref: "#/components/schemas/CanonicalU64"
        target_mount_generation:
          $ref: "#/components/schemas/CanonicalU64"
        target_route_generation:
          $ref: "#/components/schemas/CanonicalU64"
        transfer_route_id:
          $ref: "#/components/schemas/ResourceId"
        transfer_id:
          $ref: "#/components/schemas/ResourceId"
        target_placement_set_id:
          $ref: "#/components/schemas/ResourceId"
        staging_id:
          $ref: "#/components/schemas/ResourceId"
        completed_bytes:
          $ref: "#/components/schemas/CanonicalU64"
        total_bytes:
          $ref: "#/components/schemas/CanonicalU64"
        issue:
          $ref: "#/components/schemas/ResourceIssueSummary"

    CreateCommitReplicationResponse:
      type: object
      additionalProperties: false
      required: [replication, replayed]
      properties:
        replication:
          $ref: "#/components/schemas/ReplicationView"
        replayed:
          type: boolean

    QueryCommitReplicationResponse:
      type: object
      additionalProperties: false
      required: [replication]
      properties:
        replication:
          $ref: "#/components/schemas/ReplicationView"

    TransferEndpoint:
      type: object
      additionalProperties: false
      required: [placement_id, agent_id, gateway_pool_id, edge_cluster_id]
      properties:
        placement_id:
          $ref: "#/components/schemas/ResourceId"
        agent_id:
          $ref: "#/components/schemas/ResourceId"
        gateway_pool_id:
          $ref: "#/components/schemas/GatewayPoolId"
        edge_cluster_id:
          $ref: "#/components/schemas/EdgeClusterId"
        storage_volume_id:
          $ref: "#/components/schemas/StorageVolumeId"

    TransferTicket:
      type: object
      additionalProperties: false
      required: [transfer_id, tenant_id, artifact_id, commit_id, object_set_digest, source, target, session_generation, mount_generation, route_generation, deadline_unix_ms, max_bytes, allowed_objects]
      properties:
        transfer_id:
          $ref: "#/components/schemas/ResourceId"
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        artifact_id:
          $ref: "#/components/schemas/ArtifactId"
        commit_id:
          $ref: "#/components/schemas/CommitId"
        object_set_digest:
          $ref: "#/components/schemas/ContentDigest"
        source:
          $ref: "#/components/schemas/TransferEndpoint"
        target:
          $ref: "#/components/schemas/TransferEndpoint"
        source_session_generation:
          $ref: "#/components/schemas/CanonicalU64"
        source_mount_generation:
          $ref: "#/components/schemas/CanonicalU64"
        source_route_generation:
          $ref: "#/components/schemas/CanonicalU64"
        session_generation:
          $ref: "#/components/schemas/CanonicalU64"
        mount_generation:
          $ref: "#/components/schemas/CanonicalU64"
        route_generation:
          $ref: "#/components/schemas/CanonicalU64"
        deadline_unix_ms:
          $ref: "#/components/schemas/UnixMillis"
        max_bytes:
          $ref: "#/components/schemas/DecimalU64"
        allowed_objects:
          type: array
          maxItems: 65535
          items:
            $ref: "#/components/schemas/ContentDigest"

    CentralSignedPayload:
      type: object
      additionalProperties: false
      required: [key_id, certificate_generation, signed_at_unix_ms, expires_at_unix_ms, payload_digest, payload, signature]
      properties:
        key_id:
          type: string
          minLength: 1
          maxLength: 1024
        certificate_generation:
          $ref: "#/components/schemas/CanonicalU64"
        signed_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"
        expires_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"
        payload_digest:
          $ref: "#/components/schemas/ContentDigest"
        payload:
          type: string
          maxLength: 16777216
          pattern: "^[A-Za-z0-9_-]*$"
        signature:
          type: string
          minLength: 86
          maxLength: 86
          pattern: "^[A-Za-z0-9_-]+$"
        extensions:
          type: object
          additionalProperties: false

    SignedTransferTicket:
      type: object
      additionalProperties: false
      required: [ticket, central_signature]
      properties:
        ticket:
          $ref: "#/components/schemas/TransferTicket"
        central_signature:
          $ref: "#/components/schemas/CentralSignedPayload"

    QueryCommitReplicationTicketResponse:
      type: object
      additionalProperties: false
      required: [ticket]
      properties:
        ticket:
          $ref: "#/components/schemas/TransferTicket"
        signed_ticket:
          $ref: "#/components/schemas/SignedTransferTicket"

    RetryCommitReplicationResponse:
      type: object
      additionalProperties: false
      required: [replication]
      properties:
        replication:
          $ref: "#/components/schemas/ReplicationView"

    CancelCommitReplicationResponse:
      type: object
      additionalProperties: false
      required: [replication]
      properties:
        replication:
          $ref: "#/components/schemas/ReplicationView"

    QueryCommitReplicationListResponse:
      type: object
      additionalProperties: false
      required: [replications]
      properties:
        replications:
          type: array
          items:
            $ref: "#/components/schemas/ReplicationView"

    CommitPlacementView:
      type: object
      additionalProperties: false
      required: [placement_set_id, commit_id, backend_id, object_set_digest, object_count, verified_object_count, placement_generation, state]
      properties:
        placement_set_id:
          $ref: "#/components/schemas/ResourceId"
        commit_id:
          $ref: "#/components/schemas/CommitId"
        backend_id:
          $ref: "#/components/schemas/ResourceId"
        storage_volume_id:
          $ref: "#/components/schemas/StorageVolumeId"
        object_set_digest:
          $ref: "#/components/schemas/ContentDigest"
        object_count:
          $ref: "#/components/schemas/CanonicalU64"
        verified_object_count:
          $ref: "#/components/schemas/CanonicalU64"
        placement_generation:
          $ref: "#/components/schemas/CanonicalU64"
        state:
          type: string
          enum: [staged, published, retiring, deleted]

    QueryCommitPlacementListResponse:
      type: object
      additionalProperties: false
      required: [placements]
      properties:
        placements:
          type: array
          items:
            $ref: "#/components/schemas/CommitPlacementView"

    CommitAvailabilityView:
      type: object
      additionalProperties: false
      required: [commit_id, data_health, verified_placements, missing_objects, verified_storage_volume_ids]
      properties:
        commit_id:
          $ref: "#/components/schemas/CommitId"
        data_health:
          type: string
          enum: [available, degraded, unavailable]
        verified_placements:
          $ref: "#/components/schemas/CanonicalU64"
        missing_objects:
          $ref: "#/components/schemas/CanonicalU64"
        verified_storage_volume_ids:
          type: array
          items:
            $ref: "#/components/schemas/StorageVolumeId"

    QueryCommitAvailabilityResponse:
      type: object
      additionalProperties: false
      required: [availability]
      properties:
        availability:
          $ref: "#/components/schemas/CommitAvailabilityView"

    WorkspaceView:
      type: object
      additionalProperties: false
      required: [workspace_id, tenant_id, project_id, artifact_id, target_storage_volume_id, lifecycle]
      properties:
        workspace_id:
          $ref: "#/components/schemas/ResourceId"
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        project_id:
          $ref: "#/components/schemas/ProjectId"
        artifact_id:
          $ref: "#/components/schemas/ArtifactId"
        base_commit_id:
          $ref: "#/components/schemas/CommitId"
        target_storage_volume_id:
          $ref: "#/components/schemas/StorageVolumeId"
        lifecycle:
          type: string
          enum: [provisioning, active, unavailable, deleting, deleted]

    CreateWorkspaceResponse:
      type: object
      additionalProperties: false
      required: [workspace, replayed]
      properties:
        workspace:
          $ref: "#/components/schemas/WorkspaceView"
        replayed:
          type: boolean

    CreateSnapshotRequest:
      type: object
      description: |
        为指定 Artifact 的固定 Commit 创建逻辑 Snapshot。服务端必须验证 Project/Artifact 存在
        且 Commit 确属该 Artifact；Snapshot 不能创建 Artifact、改写 Artifact Head 或在创建后
        切换 Commit。Commit 的源 Placement 由服务端解析，客户端不能为 Snapshot 选择 Volume。
      additionalProperties: false
      required:
        - tenant_id
        - project_id
        - artifact_id
        - commit_id
        - request_id
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        project_id:
          $ref: "#/components/schemas/ProjectId"
        artifact_id:
          $ref: "#/components/schemas/ArtifactId"
        commit_id:
          $ref: "#/components/schemas/CommitId"
        request_id:
          $ref: "#/components/schemas/ResourceId"

    CreateSnapshotResponse:
      type: object
      additionalProperties: false
      required: [snapshot, replayed]
      properties:
        snapshot:
          $ref: "#/components/schemas/SnapshotView"
        replayed:
          type: boolean

    SnapshotDeliveryMode:
      type: string
      enum: [fuse, copy, hardlink]

    HardlinkPolicy:
      type: string
      enum: [disabled, sealed_acl, trusted_local]

    SnapshotDeliveryState:
      type: string
      enum: [requested, validating, materializing, ready, failed, deleting, deleted]

    SnapshotDeliveryView:
      type: object
      additionalProperties: false
      required:
        - delivery_id
        - snapshot_id
        - commit_id
        - storage_volume_id
        - mode
        - target_relative_root
        - state
        - source_index_digest
        - delivery_generation
        - file_count
        - size_bytes
        - object_set_digest
        - resource_version
        - created_at_unix_ms
        - updated_at_unix_ms
      properties:
        delivery_id:
          $ref: "#/components/schemas/ResourceId"
        snapshot_id:
          $ref: "#/components/schemas/SnapshotId"
        commit_id:
          $ref: "#/components/schemas/CommitId"
        storage_volume_id:
          $ref: "#/components/schemas/StorageVolumeId"
        mode:
          $ref: "#/components/schemas/SnapshotDeliveryMode"
        target_relative_root:
          type: string
          minLength: 1
          maxLength: 4096
        state:
          $ref: "#/components/schemas/SnapshotDeliveryState"
        source_index_digest:
          $ref: "#/components/schemas/ContentDigest"
        delivery_generation:
          $ref: "#/components/schemas/CanonicalU64"
        file_count:
          $ref: "#/components/schemas/CanonicalU64"
        size_bytes:
          $ref: "#/components/schemas/CanonicalU64"
        object_set_digest:
          $ref: "#/components/schemas/ContentDigest"
        resource_version:
          $ref: "#/components/schemas/CanonicalU64"
        issue:
          $ref: "#/components/schemas/ResourceIssueSummary"
        created_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"
        updated_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"

    CreateSnapshotDeliveryRequest:
      type: object
      additionalProperties: false
      required: [tenant_id, snapshot_id, target_storage_volume_id, mode, request_id]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        snapshot_id:
          $ref: "#/components/schemas/SnapshotId"
        target_storage_volume_id:
          $ref: "#/components/schemas/StorageVolumeId"
        mode:
          $ref: "#/components/schemas/SnapshotDeliveryMode"
        request_id:
          $ref: "#/components/schemas/ResourceId"

    CreateSnapshotDeliveryResponse:
      type: object
      additionalProperties: false
      required: [delivery, replayed]
      properties:
        delivery:
          $ref: "#/components/schemas/SnapshotDeliveryView"
        replayed:
          type: boolean

    QuerySnapshotDeliveryRequest:
      type: object
      additionalProperties: false
      required: [tenant_id, delivery_id]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        delivery_id:
          $ref: "#/components/schemas/ResourceId"

    QuerySnapshotDeliveryResponse:
      type: object
      additionalProperties: false
      required: [delivery]
      properties:
        delivery:
          $ref: "#/components/schemas/SnapshotDeliveryView"

    QuerySnapshotDeliveryListRequest:
      type: object
      additionalProperties: false
      required: [tenant_id, snapshot_id]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        snapshot_id:
          $ref: "#/components/schemas/SnapshotId"
        cursor:
          $ref: "#/components/schemas/PageCursor"
        page_size:
          $ref: "#/components/schemas/PageSize"

    QuerySnapshotDeliveryListResponse:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items:
          type: array
          maxItems: 100
          items:
            $ref: "#/components/schemas/SnapshotDeliveryView"
        next_cursor:
          $ref: "#/components/schemas/PageCursor"

    RetrySnapshotDeliveryRequest:
      type: object
      additionalProperties: false
      required: [tenant_id, delivery_id, request_id]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        delivery_id:
          $ref: "#/components/schemas/ResourceId"
        request_id:
          $ref: "#/components/schemas/ResourceId"

    RetrySnapshotDeliveryResponse:
      type: object
      additionalProperties: false
      required: [delivery, replayed]
      properties:
        delivery:
          $ref: "#/components/schemas/SnapshotDeliveryView"
        replayed:
          type: boolean

    DeleteSnapshotDeliveryRequest:
      type: object
      additionalProperties: false
      required: [tenant_id, delivery_id, request_id]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        delivery_id:
          $ref: "#/components/schemas/ResourceId"
        request_id:
          $ref: "#/components/schemas/ResourceId"

    DeleteSnapshotDeliveryResponse:
      type: object
      additionalProperties: false
      required: [delivery, replayed]
      properties:
        delivery:
          $ref: "#/components/schemas/SnapshotDeliveryView"
        replayed:
          type: boolean

    QuerySnapshotFileListRequest:
      type: object
      additionalProperties: false
      required: [tenant_id, snapshot_id]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        snapshot_id:
          $ref: "#/components/schemas/SnapshotId"
        path_prefix:
          $ref: "#/components/schemas/LogicalPath"
        format:
          $ref: "#/components/schemas/FileFormat"
        cursor:
          $ref: "#/components/schemas/PageCursor"
        page_size:
          $ref: "#/components/schemas/PageSize"

    QuerySnapshotFileListResponse:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items:
          type: array
          maxItems: 100
          items:
            $ref: "#/components/schemas/LogicalFileEntry"
        next_cursor:
          $ref: "#/components/schemas/PageCursor"

    QuerySnapshotActivityListRequest:
      type: object
      additionalProperties: false
      required: [tenant_id, snapshot_id]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        snapshot_id:
          $ref: "#/components/schemas/SnapshotId"
        cursor:
          $ref: "#/components/schemas/PageCursor"
        page_size:
          $ref: "#/components/schemas/PageSize"

    QuerySnapshotActivityListResponse:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items:
          type: array
          maxItems: 100
          items:
            $ref: "#/components/schemas/SnapshotActivityView"
        next_cursor:
          $ref: "#/components/schemas/PageCursor"

    SnapshotActivityView:
      type: object
      additionalProperties: false
      required: [activity_id, activity_type, summary, created_at_unix_ms]
      properties:
        activity_id:
          $ref: "#/components/schemas/ResourceId"
        activity_type:
          type: string
          enum: [created, status_changed, ready, failed]
        summary:
          type: string
          minLength: 1
          maxLength: 1024
        issue:
          $ref: "#/components/schemas/ResourceIssueSummary"
        created_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"

    QuerySnapshotDatasetProfileRequest:
      type: object
      additionalProperties: false
      required: [tenant_id, snapshot_id]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        snapshot_id:
          $ref: "#/components/schemas/SnapshotId"

    QuerySnapshotDatasetProfileResponse:
      type: object
      additionalProperties: false
      required: [profile]
      properties:
        profile:
          $ref: "#/components/schemas/DatasetProfileView"

    SnapshotState:
      type: string
      enum: [creating, ready, abnormal]

    SnapshotView:
      type: object
      description: |
        独立身份，固定一个 Artifact 和 Commit 的不可变逻辑 Snapshot 资源。
        `tenant_id + project_id + artifact_id + commit_id` 是不可变来源 scope；只读交付由独立的
        SnapshotDelivery 资源表达，Snapshot 本身不保存挂载或物化模式，也不拥有或改写 Artifact 数据权威。
      additionalProperties: false
      required:
        - snapshot_id
        - tenant_id
        - project_id
        - artifact_id
        - commit_id
        - data_layout
        - message
        - tag_names
        - state
        - data_health
        - integrity
        - resource_version
        - lifecycle
        - logical_file_count
        - logical_size_bytes
        - created_at_unix_ms
        - updated_at_unix_ms
      properties:
        snapshot_id:
          $ref: "#/components/schemas/SnapshotId"
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        project_id:
          $ref: "#/components/schemas/ProjectId"
        artifact_id:
          $ref: "#/components/schemas/ArtifactId"
        commit_id:
          $ref: "#/components/schemas/CommitId"
        data_layout:
          $ref: "#/components/schemas/DataLayout"
        message:
          type: string
          minLength: 1
          maxLength: 4096
        tag_names:
          type: array
          maxItems: 20
          uniqueItems: true
          items:
            $ref: "#/components/schemas/TagName"
        state:
          $ref: "#/components/schemas/SnapshotState"
        data_health:
          type: string
          description: Commit object availability resolved from the currently published PlacementSets.
          enum: [available, degraded, unavailable]
        issue:
          $ref: "#/components/schemas/ResourceIssueSummary"
        integrity:
          $ref: "#/components/schemas/SnapshotIntegritySummary"
        resource_version:
          $ref: "#/components/schemas/CanonicalU64"
        lifecycle:
          $ref: "#/components/schemas/ResourceLifecycleView"
        logical_file_count:
          $ref: "#/components/schemas/CanonicalU64"
        logical_size_bytes:
          $ref: "#/components/schemas/CanonicalU64"
        dataset_profile:
          $ref: "#/components/schemas/DatasetProfileSummary"
        created_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"
        updated_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"

    SnapshotIntegritySummary:
      type: object
      additionalProperties: false
      required: [state, files_verified, bytes_verified]
      properties:
        state:
          type: string
          enum: [pending, verified, failed]
        files_verified:
          $ref: "#/components/schemas/CanonicalU64"
        bytes_verified:
          $ref: "#/components/schemas/CanonicalU64"
        verified_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"

    ResourceLifecycleState:
      type: string
      description: 与资源健康状态独立的访问和删除生命周期。
      enum: [active, pending_delete, deleting, restoring, deleted]

    ResourceLifecycleView:
      type: object
      description: |
        四类可删除存储资源共享的生命周期 fence。`generation` 在删除、恢复和最终清理时单调增加，
        Agent assignment、S3 Ticket 和迟到报告必须携带并匹配该值。
      additionalProperties: false
      required: [state, generation]
      properties:
        state:
          $ref: "#/components/schemas/ResourceLifecycleState"
        generation:
          $ref: "#/components/schemas/PositiveCanonicalU64"
        active_deletion_id:
          $ref: "#/components/schemas/DeletionId"
        delete_requested_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"
        purge_after_unix_ms:
          $ref: "#/components/schemas/UnixMillis"
        deleted_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"

    ResourceRef:
      description: Tenant scope 由包含该引用的请求携带，不允许从资源 ID 推断或覆盖。
      oneOf:
        - $ref: "#/components/schemas/StorageVolumeResourceRef"
        - $ref: "#/components/schemas/ArtifactResourceRef"
        - $ref: "#/components/schemas/PlaygroundResourceRef"
        - $ref: "#/components/schemas/SnapshotResourceRef"
      discriminator:
        propertyName: type
        mapping:
          storage_volume: "#/components/schemas/StorageVolumeResourceRef"
          artifact: "#/components/schemas/ArtifactResourceRef"
          playground: "#/components/schemas/PlaygroundResourceRef"
          snapshot: "#/components/schemas/SnapshotResourceRef"

    StorageVolumeResourceRef:
      type: object
      additionalProperties: false
      required: [type, storage_volume_id]
      properties:
        type:
          type: string
          const: storage_volume
        storage_volume_id:
          $ref: "#/components/schemas/StorageVolumeId"

    ArtifactResourceRef:
      type: object
      additionalProperties: false
      required: [type, project_id, artifact_id]
      properties:
        type:
          type: string
          const: artifact
        project_id:
          $ref: "#/components/schemas/ProjectId"
        artifact_id:
          $ref: "#/components/schemas/ArtifactId"

    PlaygroundResourceRef:
      type: object
      additionalProperties: false
      required: [type, project_id, artifact_id, playground_id]
      properties:
        type:
          type: string
          const: playground
        project_id:
          $ref: "#/components/schemas/ProjectId"
        artifact_id:
          $ref: "#/components/schemas/ArtifactId"
        playground_id:
          $ref: "#/components/schemas/PlaygroundId"

    SnapshotResourceRef:
      type: object
      additionalProperties: false
      required: [type, snapshot_id]
      properties:
        type:
          type: string
          const: snapshot
        snapshot_id:
          $ref: "#/components/schemas/SnapshotId"

    DeletionOperationState:
      type: string
      enum:
        - requested
        - quiescing
        - quarantining
        - recoverable
        - restoring
        - purging
        - finalizing
        - completed
        - blocked
        - failed

    DeletionCompletion:
      type: string
      enum: [restored, purged]

    RetentionHoldState:
      type: string
      enum: [active, released]

    DeletionTargetView:
      type: object
      additionalProperties: false
      required:
        - resource
        - resource_version
        - lifecycle_generation
        - requires_agent_cleanup
      properties:
        resource:
          $ref: "#/components/schemas/ResourceRef"
        resource_version:
          $ref: "#/components/schemas/CanonicalU64"
        lifecycle_generation:
          $ref: "#/components/schemas/PositiveCanonicalU64"
        requires_agent_cleanup:
          type: boolean

    DeletionBlockerView:
      type: object
      additionalProperties: false
      required: [code, message]
      properties:
        code:
          $ref: "#/components/schemas/ErrorCode"
        resource:
          $ref: "#/components/schemas/ResourceRef"
        message:
          type: string
          minLength: 1
          maxLength: 4096

    DeletionImpactView:
      type: object
      description: 五分钟内不可变且与资源版本、依赖集合和删除确认选项绑定的 dry-run 结果。
      additionalProperties: false
      required:
        - tenant_id
        - root
        - cascade
        - confirm_managed_data_erase
        - targets
        - active_job_count
        - active_s3_credential_count
        - estimated_file_count
        - estimated_bytes
        - blockers
        - issued_at_unix_ms
        - expires_at_unix_ms
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        root:
          $ref: "#/components/schemas/ResourceRef"
        cascade:
          type: boolean
        confirm_managed_data_erase:
          type: boolean
        targets:
          type: array
          maxItems: 10000
          items:
            $ref: "#/components/schemas/DeletionTargetView"
        active_job_count:
          $ref: "#/components/schemas/CanonicalU64"
        active_s3_credential_count:
          $ref: "#/components/schemas/CanonicalU64"
        estimated_file_count:
          $ref: "#/components/schemas/CanonicalU64"
        estimated_bytes:
          $ref: "#/components/schemas/CanonicalU64"
        blockers:
          type: array
          maxItems: 10000
          items:
            $ref: "#/components/schemas/DeletionBlockerView"
        issued_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"
        expires_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"

    DeletionOperationView:
      type: object
      description: 固定依赖批次的持久化安全删除 Saga；进入 `purging` 后不可恢复。
      additionalProperties: false
      required:
        - deletion_id
        - tenant_id
        - root
        - state
        - resource_version
        - targets
        - request_id
        - request_digest
        - impact_digest
        - cascade
        - confirm_managed_data_erase
        - purge_after_unix_ms
        - created_at_unix_ms
        - updated_at_unix_ms
        - retry_count
      properties:
        deletion_id:
          $ref: "#/components/schemas/DeletionId"
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        root:
          $ref: "#/components/schemas/ResourceRef"
        state:
          $ref: "#/components/schemas/DeletionOperationState"
        resource_version:
          $ref: "#/components/schemas/CanonicalU64"
        targets:
          type: array
          maxItems: 10000
          items:
            $ref: "#/components/schemas/DeletionTargetView"
        request_id:
          $ref: "#/components/schemas/RequestId"
        request_digest:
          $ref: "#/components/schemas/ContentDigest"
        impact_digest:
          $ref: "#/components/schemas/ContentDigest"
        cascade:
          type: boolean
        confirm_managed_data_erase:
          type: boolean
        purge_after_unix_ms:
          $ref: "#/components/schemas/UnixMillis"
        created_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"
        updated_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"
        completion:
          $ref: "#/components/schemas/DeletionCompletion"
        last_error:
          type: string
          minLength: 1
          maxLength: 4096
        resume_state:
          allOf:
            - $ref: "#/components/schemas/DeletionOperationState"
          description: |
            `blocked` 或 `failed` 操作的持久化恢复点。重试会返回此阶段；`restoring`
            表示继续恢复，其余可恢复阶段表示继续删除。非错误状态不返回该字段。
        retry_count:
          $ref: "#/components/schemas/CanonicalU64"

    RetentionHoldView:
      type: object
      description: 只阻止任务进入物理清理阶段，不恢复或扩大资源访问权限。
      additionalProperties: false
      required:
        - retention_hold_id
        - tenant_id
        - deletion_id
        - reason
        - state
        - created_at_unix_ms
      properties:
        retention_hold_id:
          $ref: "#/components/schemas/RetentionHoldId"
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        deletion_id:
          $ref: "#/components/schemas/DeletionId"
        reason:
          type: string
          minLength: 1
          maxLength: 2048
        state:
          $ref: "#/components/schemas/RetentionHoldState"
        expires_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"
        created_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"
        released_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"

    QueryDeletionImpactRequest:
      type: object
      additionalProperties: false
      required:
        - tenant_id
        - resource
        - cascade
        - confirm_managed_data_erase
        - expected_resource_version
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        resource:
          $ref: "#/components/schemas/ResourceRef"
        cascade:
          type: boolean
        confirm_managed_data_erase:
          type: boolean
        expected_resource_version:
          $ref: "#/components/schemas/CanonicalU64"

    QueryDeletionImpactResponse:
      type: object
      additionalProperties: false
      required: [impact, impact_digest]
      properties:
        impact:
          $ref: "#/components/schemas/DeletionImpactView"
        impact_digest:
          $ref: "#/components/schemas/ContentDigest"

    CreateDeletionRequest:
      type: object
      additionalProperties: false
      required:
        - tenant_id
        - resource
        - cascade
        - confirm_managed_data_erase
        - expected_resource_version
        - impact_digest
        - request_id
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        resource:
          $ref: "#/components/schemas/ResourceRef"
        cascade:
          type: boolean
        confirm_managed_data_erase:
          type: boolean
        expected_resource_version:
          $ref: "#/components/schemas/CanonicalU64"
        impact_digest:
          $ref: "#/components/schemas/ContentDigest"
        request_id:
          $ref: "#/components/schemas/RequestId"

    QueryDeletionRequest:
      type: object
      additionalProperties: false
      required: [tenant_id, deletion_id]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        deletion_id:
          $ref: "#/components/schemas/DeletionId"

    QueryDeletionResponse:
      type: object
      additionalProperties: false
      required: [deletion, retention_holds]
      properties:
        deletion:
          $ref: "#/components/schemas/DeletionOperationView"
        retention_holds:
          type: array
          maxItems: 10000
          items:
            $ref: "#/components/schemas/RetentionHoldView"

    QueryDeletionListRequest:
      type: object
      additionalProperties: false
      required: [tenant_id]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        states:
          type: array
          maxItems: 10
          uniqueItems: true
          items:
            $ref: "#/components/schemas/DeletionOperationState"
        cursor:
          $ref: "#/components/schemas/PageCursor"
        page_size:
          $ref: "#/components/schemas/PageSize"

    QueryDeletionListResponse:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items:
          type: array
          maxItems: 100
          items:
            $ref: "#/components/schemas/DeletionOperationView"
        next_cursor:
          $ref: "#/components/schemas/PageCursor"

    UpdateDeletionRequest:
      type: object
      additionalProperties: false
      required: [tenant_id, deletion_id, request_id, expected_resource_version]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        deletion_id:
          $ref: "#/components/schemas/DeletionId"
        request_id:
          $ref: "#/components/schemas/RequestId"
        expected_resource_version:
          $ref: "#/components/schemas/CanonicalU64"

    CreateRetentionHoldRequest:
      type: object
      additionalProperties: false
      required:
        - tenant_id
        - deletion_id
        - request_id
        - expected_resource_version
        - reason
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        deletion_id:
          $ref: "#/components/schemas/DeletionId"
        request_id:
          $ref: "#/components/schemas/RequestId"
        expected_resource_version:
          $ref: "#/components/schemas/CanonicalU64"
        reason:
          type: string
          minLength: 1
          maxLength: 2048
        expires_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"

    ReleaseRetentionHoldRequest:
      type: object
      additionalProperties: false
      required:
        - tenant_id
        - deletion_id
        - retention_hold_id
        - request_id
        - expected_resource_version
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        deletion_id:
          $ref: "#/components/schemas/DeletionId"
        retention_hold_id:
          $ref: "#/components/schemas/RetentionHoldId"
        request_id:
          $ref: "#/components/schemas/RequestId"
        expected_resource_version:
          $ref: "#/components/schemas/CanonicalU64"

    DeletionMutationResponse:
      type: object
      additionalProperties: false
      required: [deletion, replayed]
      properties:
        deletion:
          $ref: "#/components/schemas/DeletionOperationView"
        replayed:
          type: boolean

    CreateRetentionHoldResponse:
      type: object
      additionalProperties: false
      required: [deletion, retention_hold, replayed]
      properties:
        deletion:
          $ref: "#/components/schemas/DeletionOperationView"
        retention_hold:
          $ref: "#/components/schemas/RetentionHoldView"
        replayed:
          type: boolean

    ReleaseRetentionHoldResponse:
      type: object
      additionalProperties: false
      required: [deletion, retention_hold, replayed]
      properties:
        deletion:
          $ref: "#/components/schemas/DeletionOperationView"
        retention_hold:
          $ref: "#/components/schemas/RetentionHoldView"
        replayed:
          type: boolean

    CreateS3AccessPointRequest:
      type: object
      description: |
        为同 Tenant 下的 Ready Snapshot 开启只读 S3 Access Point。
      additionalProperties: false
      required: [tenant_id, snapshot_id, bucket_name, request_id]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        snapshot_id:
          $ref: "#/components/schemas/SnapshotId"
        bucket_name:
          $ref: "#/components/schemas/S3BucketName"
        request_id:
          $ref: "#/components/schemas/RequestId"

    QueryS3AccessPointListRequest:
      type: object
      additionalProperties: false
      required: [tenant_id]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        cursor:
          $ref: "#/components/schemas/PageCursor"
        page_size:
          $ref: "#/components/schemas/PageSize"

    QueryS3AccessPointListResponse:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items:
          type: array
          maxItems: 1000
          items:
            $ref: "#/components/schemas/S3AccessPointView"
        next_cursor:
          $ref: "#/components/schemas/PageCursor"

    QueryS3AccessPointRequest:
      type: object
      additionalProperties: false
      required: [tenant_id, access_point_id]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        access_point_id:
          $ref: "#/components/schemas/S3AccessPointId"

    QueryS3AccessPointResponse:
      type: object
      additionalProperties: false
      required: [access_point]
      properties:
        access_point:
          $ref: "#/components/schemas/S3AccessPointView"

    UpdateS3AccessPointRequest:
      type: object
      additionalProperties: false
      required: [tenant_id, access_point_id, request_id]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        access_point_id:
          $ref: "#/components/schemas/S3AccessPointId"
        request_id:
          $ref: "#/components/schemas/RequestId"

    UpdateS3AccessPointResponse:
      type: object
      additionalProperties: false
      required: [access_point, replayed]
      properties:
        access_point:
          $ref: "#/components/schemas/S3AccessPointView"
        replayed:
          type: boolean

    CreateS3AccessPointResponse:
      type: object
      additionalProperties: false
      required:
        - access_point
        - access_key_id
        - credential_expires_at_unix_ms
        - replayed
      properties:
        access_point:
          $ref: "#/components/schemas/S3AccessPointView"
        access_key_id:
          type: string
          minLength: 1
          maxLength: 128
        secret_access_key:
          type: string
          writeOnly: true
          maxLength: 2048
        credential_expires_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"
        replayed:
          type: boolean

    CreateS3CredentialRequest:
      type: object
      additionalProperties: false
      required: [tenant_id, access_point_id, request_id]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        access_point_id:
          $ref: "#/components/schemas/S3AccessPointId"
        request_id:
          $ref: "#/components/schemas/RequestId"
        expires_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"

    CreateS3CredentialResponse:
      type: object
      additionalProperties: false
      required: [credential, replayed]
      properties:
        credential:
          $ref: "#/components/schemas/S3CredentialView"
        secret_access_key:
          type: string
          writeOnly: true
          maxLength: 2048
        replayed:
          type: boolean

    QueryS3CredentialListRequest:
      type: object
      additionalProperties: false
      required: [tenant_id, access_point_id]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        access_point_id:
          $ref: "#/components/schemas/S3AccessPointId"

    QueryS3CredentialListResponse:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/S3CredentialView"

    RevokeS3CredentialRequest:
      type: object
      additionalProperties: false
      required: [tenant_id, access_point_id, credential_id, request_id]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        access_point_id:
          $ref: "#/components/schemas/S3AccessPointId"
        credential_id:
          $ref: "#/components/schemas/S3CredentialId"
        request_id:
          $ref: "#/components/schemas/RequestId"

    QueryS3ObjectListRequest:
      type: object
      additionalProperties: false
      required: [tenant_id, access_point_id]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        access_point_id:
          $ref: "#/components/schemas/S3AccessPointId"
        prefix:
          $ref: "#/components/schemas/S3ObjectKeyPrefix"
        delimiter:
          type: string
          enum: ["", "/"]
        cursor:
          $ref: "#/components/schemas/PageCursor"
        page_size:
          $ref: "#/components/schemas/S3PageSize"

    QueryS3ObjectListResponse:
      type: object
      additionalProperties: false
      required: [items, common_prefixes]
      properties:
        items:
          type: array
          maxItems: 1000
          items:
            $ref: "#/components/schemas/S3ObjectEntryView"
        common_prefixes:
          type: array
          maxItems: 1000
          items:
            $ref: "#/components/schemas/S3ObjectKeyPrefix"
        next_cursor:
          $ref: "#/components/schemas/PageCursor"

    CreateS3DownloadUrlRequest:
      type: object
      additionalProperties: false
      required: [tenant_id, access_point_id, key]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        access_point_id:
          $ref: "#/components/schemas/S3AccessPointId"
        key:
          $ref: "#/components/schemas/S3ObjectKey"
        expires_seconds:
          type: integer
          minimum: 1
          maximum: 300

    CreateS3DownloadUrlResponse:
      type: object
      additionalProperties: false
      required: [url, expires_at_unix_ms]
      properties:
        url:
          type: string
          format: uri-reference
          minLength: 1
          maxLength: 8192
        expires_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"

    S3AccessPointView:
      type: object
      additionalProperties: false
      required:
        - access_point_id
        - tenant_id
        - project_id
        - artifact_id
        - snapshot_id
        - commit_id
        - bucket_name
        - endpoint
        - region
        - state
        - policy_generation
        - created_at_unix_ms
        - updated_at_unix_ms
      properties:
        access_point_id:
          $ref: "#/components/schemas/S3AccessPointId"
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        project_id:
          $ref: "#/components/schemas/ProjectId"
        artifact_id:
          $ref: "#/components/schemas/ArtifactId"
        snapshot_id:
          $ref: "#/components/schemas/SnapshotId"
        commit_id:
          $ref: "#/components/schemas/CommitId"
        bucket_name:
          $ref: "#/components/schemas/S3BucketName"
        endpoint:
          type: string
          format: uri-reference
          minLength: 1
          maxLength: 2048
        region:
          $ref: "#/components/schemas/RegionName"
        state:
          $ref: "#/components/schemas/S3AccessPointState"
        policy_generation:
          $ref: "#/components/schemas/PositiveCanonicalU64"
        created_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"
        updated_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"

    S3CredentialView:
      type: object
      additionalProperties: false
      required:
        - credential_id
        - access_point_id
        - access_key_id
        - state
        - expires_at_unix_ms
        - created_at_unix_ms
      properties:
        credential_id:
          $ref: "#/components/schemas/S3CredentialId"
        access_point_id:
          $ref: "#/components/schemas/S3AccessPointId"
        access_key_id:
          type: string
          minLength: 1
          maxLength: 128
        state:
          $ref: "#/components/schemas/S3CredentialState"
        expires_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"
        created_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"
        last_used_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"

    S3ObjectEntryView:
      type: object
      additionalProperties: false
      required: [key, entry_type]
      properties:
        key:
          $ref: "#/components/schemas/S3ObjectKey"
        entry_type:
          $ref: "#/components/schemas/S3ObjectEntryType"
        size_bytes:
          $ref: "#/components/schemas/CanonicalU64"
        etag:
          type: string
          minLength: 1
          maxLength: 512
        last_modified_unix_ms:
          $ref: "#/components/schemas/UnixMillis"

    CreateAddJobRequest:
      type: object
      description: |
        公开 Add operation。服务端将认证后的 PrincipalRef 注入 canonical operation 后计算
        `request_digest` 并写入内部 AddJobSpec；请求字段必须严格匹配本 schema。
      additionalProperties: false
      required:
        - tenant_id
        - project_id
        - artifact_id
        - playground_id
        - job_id
        - expected_index_version
        - deadline_unix_ms
        - paths
        - all
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        project_id:
          $ref: "#/components/schemas/ProjectId"
        artifact_id:
          $ref: "#/components/schemas/ArtifactId"
        playground_id:
          $ref: "#/components/schemas/PlaygroundId"
        job_id:
          $ref: "#/components/schemas/JobId"
        expected_index_version:
          $ref: "#/components/schemas/IndexVersion"
        deadline_unix_ms:
          $ref: "#/components/schemas/UnixMillis"
        paths:
          type: array
          maxItems: 4096
          uniqueItems: true
          description: |
            严格排序、无 portable duplicate，且不能同时包含文件路径及其后代。
            `all` 为 false 时至少包含一项。
          items:
            $ref: "#/components/schemas/LogicalPath"
        all:
          type: boolean

    CreateAddJobResponse:
      type: object
      additionalProperties: false
      required: [job, replayed]
      properties:
        job:
          $ref: "#/components/schemas/JobView"
        replayed:
          type: boolean
          description: true 表示返回的是已持久化的同一请求结果。

    QueryJobRequest:
      type: object
      additionalProperties: false
      required: [tenant_id, job_id]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        job_id:
          $ref: "#/components/schemas/JobId"

    QueryJobResponse:
      type: object
      additionalProperties: false
      required: [job]
      properties:
        job:
          $ref: "#/components/schemas/JobView"

    FinalizeAddJobRequest:
      type: object
      additionalProperties: false
      required: [tenant_id, job_id]
      properties:
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        job_id:
          $ref: "#/components/schemas/JobId"

    FinalizeAddJobResponse:
      type: object
      additionalProperties: false
      required:
        - job
        - decision
        - finalized_at_unix_ms
        - replayed
      properties:
        job:
          $ref: "#/components/schemas/JobView"
        decision:
          $ref: "#/components/schemas/PublicJobDecision"
        finalized_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"
        replayed:
          type: boolean

    JobView:
      type: object
      description: |
        面向用户的脱敏 Job。不得增加 assignment target、Agent/Volume/Mount identity、
        generation/fencing、PublicationCandidate、Manifest、IndexDelta 或 authority 物理信息。
      additionalProperties: false
      required:
        - operation
        - tenant_id
        - project_id
        - artifact_id
        - playground_id
        - job_id
        - state
        - resource_version
        - deadline_unix_ms
      properties:
        operation:
          type: string
          const: add
        tenant_id:
          $ref: "#/components/schemas/TenantId"
        project_id:
          $ref: "#/components/schemas/ProjectId"
        artifact_id:
          $ref: "#/components/schemas/ArtifactId"
        playground_id:
          $ref: "#/components/schemas/PlaygroundId"
        job_id:
          $ref: "#/components/schemas/JobId"
        state:
          $ref: "#/components/schemas/JobState"
        resource_version:
          $ref: "#/components/schemas/CanonicalU64"
        deadline_unix_ms:
          $ref: "#/components/schemas/UnixMillis"
        progress:
          $ref: "#/components/schemas/PublicJobProgress"
        decision:
          $ref: "#/components/schemas/PublicJobDecision"
        failure:
          $ref: "#/components/schemas/PublicJobFailure"
        finalized_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"

    PublicJobProgress:
      type: object
      additionalProperties: false
      required:
        - state
        - phase
        - files_completed
        - bytes_completed
      properties:
        state:
          $ref: "#/components/schemas/JobState"
        phase:
          type: string
          minLength: 1
          maxLength: 128
        files_completed:
          $ref: "#/components/schemas/CanonicalU64"
        bytes_completed:
          $ref: "#/components/schemas/CanonicalU64"
        retry_after_ms:
          $ref: "#/components/schemas/CanonicalU64"

    PublicJobDecision:
      oneOf:
        - $ref: "#/components/schemas/PublishJobDecision"
        - $ref: "#/components/schemas/ConflictJobDecision"
        - $ref: "#/components/schemas/RejectJobDecision"
      discriminator:
        propertyName: outcome
        mapping:
          publish: "#/components/schemas/PublishJobDecision"
          conflict: "#/components/schemas/ConflictJobDecision"
          reject: "#/components/schemas/RejectJobDecision"

    PublishJobDecision:
      type: object
      additionalProperties: false
      required: [outcome, final_state, published_index_version]
      properties:
        outcome:
          type: string
          const: publish
        final_state:
          type: string
          const: succeeded
        published_index_version:
          $ref: "#/components/schemas/IndexVersion"

    ConflictJobDecision:
      type: object
      additionalProperties: false
      required: [outcome, final_state, current_index_version]
      properties:
        outcome:
          type: string
          const: conflict
        final_state:
          type: string
          const: conflicted
        current_index_version:
          $ref: "#/components/schemas/IndexVersion"

    RejectJobDecision:
      type: object
      additionalProperties: false
      required: [outcome, final_state, error]
      properties:
        outcome:
          type: string
          const: reject
        final_state:
          type: string
          enum:
            - rejected
            - failed
            - cancelled
            - timed_out
            - recovery_required
        error:
          $ref: "#/components/schemas/JobError"

    PublicJobFailure:
      type: object
      additionalProperties: false
      required: [final_state, failed_at_unix_ms, stage, error]
      properties:
        final_state:
          type: string
          enum:
            - rejected
            - failed
            - cancelled
            - timed_out
            - recovery_required
        failed_at_unix_ms:
          $ref: "#/components/schemas/UnixMillis"
        stage:
          type: string
          enum:
            - execution
            - object_transfer
            - reporting
            - finalization
        error:
          $ref: "#/components/schemas/JobError"

    JobError:
      type: object
      additionalProperties: false
      required: [code, message, retryable]
      properties:
        code:
          $ref: "#/components/schemas/ErrorCode"
        message:
          type: string
          minLength: 1
          maxLength: 4096
        retryable:
          type: boolean
        retry_after_ms:
          $ref: "#/components/schemas/CanonicalU64"

    ProblemDetails:
      type: object
      description: RFC 9457 Problem Details，并携带 NeoEngram 稳定错误字段。
      additionalProperties: false
      required:
        - type
        - title
        - status
        - detail
        - instance
        - code
        - request_id
        - retryable
      properties:
        type:
          type: string
          format: uri-reference
          pattern: "^urn:neoengram:problem:[a-z0-9-]+$"
        title:
          type: string
          minLength: 1
          maxLength: 256
        status:
          type: integer
          minimum: 400
          maximum: 599
        detail:
          type: string
          minLength: 1
          maxLength: 4096
        instance:
          type: string
          format: uri-reference
        code:
          $ref: "#/components/schemas/ErrorCode"
        request_id:
          $ref: "#/components/schemas/RequestId"
        retryable:
          type: boolean
        retry_after_ms:
          $ref: "#/components/schemas/CanonicalU64"
        violations:
          type: array
          maxItems: 256
          items:
            $ref: "#/components/schemas/FieldViolation"

    FieldViolation:
      type: object
      additionalProperties: false
      required: [field, reason]
      properties:
        field:
          type: string
          minLength: 1
          maxLength: 256
        reason:
          type: string
          minLength: 1
          maxLength: 1024

    HealthResponse:
      type: object
      additionalProperties: false
      required: [status]
      properties:
        status:
          type: string
          const: ok

    TenantId:
      $ref: "#/components/schemas/ResourceId"
    StorageVolumeId:
      $ref: "#/components/schemas/ResourceId"
    StorageEnrollmentId:
      $ref: "#/components/schemas/ResourceId"
    StorageEnrollmentTokenId:
      $ref: "#/components/schemas/ResourceId"
    EdgeClusterId:
      $ref: "#/components/schemas/ResourceId"
    GatewayPoolId:
      $ref: "#/components/schemas/ResourceId"
    GatewayReplicaId:
      $ref: "#/components/schemas/ResourceId"
    ProjectId:
      $ref: "#/components/schemas/ResourceId"
    ArtifactId:
      $ref: "#/components/schemas/ResourceId"
    PlaygroundId:
      $ref: "#/components/schemas/ResourceId"
    CommitId:
      $ref: "#/components/schemas/ContentDigest"
    PreCommitId:
      $ref: "#/components/schemas/ResourceId"
    SnapshotId:
      $ref: "#/components/schemas/ResourceId"
    S3AccessPointId:
      $ref: "#/components/schemas/ResourceId"
    S3CredentialId:
      $ref: "#/components/schemas/ResourceId"
    DeletionId:
      $ref: "#/components/schemas/ResourceId"
    RetentionHoldId:
      $ref: "#/components/schemas/ResourceId"
    JobId:
      $ref: "#/components/schemas/ResourceId"
    RequestId:
      $ref: "#/components/schemas/ResourceId"

    StorageEnrollmentBootstrapToken:
      type: string
      description: 15 分钟有效且只能成功消费一次的 opaque secret；不得写入日志、审计或其他公开 DTO。
      readOnly: true
      minLength: 32
      maxLength: 2048
      pattern: "^[A-Za-z0-9._~-]+$"

    ResourceId:
      type: string
      minLength: 1
      maxLength: 128
      pattern: "^[A-Za-z0-9](?:[A-Za-z0-9._:-]{0,127})$"

    ErrorCode:
      type: string
      minLength: 1
      maxLength: 96
      pattern: "^[A-Z][A-Z0-9_]{0,95}$"

    DisplayName:
      type: string
      minLength: 1
      maxLength: 128

    Description:
      type: string
      maxLength: 2048

    RegionName:
      type: string
      minLength: 1
      maxLength: 64
      pattern: "^[a-z0-9][a-z0-9-]{0,63}$"

    StorageBackendType:
      type: string
      enum: [pvc, nfs]

    StorageAccessMode:
      type: string
      enum: [read_write_many, read_write_once, read_only_many]

    KubernetesNamespaceName:
      type: string
      description: Kubernetes DNS-1123 label；Namespace 最长 63 字符且不允许点号。
      minLength: 1
      maxLength: 63
      pattern: "^[a-z0-9](?:[-a-z0-9]{0,61}[a-z0-9])?$"

    KubernetesPvcClaimName:
      type: string
      description: Kubernetes DNS-1123 subdomain；每个 label 最长 63 字符，完整名称最长 253 字符。
      minLength: 1
      maxLength: 253
      pattern: '^[a-z0-9](?:[-a-z0-9]{0,61}[a-z0-9])?(?:\.[a-z0-9](?:[-a-z0-9]{0,61}[a-z0-9])?)*$'

    StorageVolumeState:
      type: string
      enum: [ready, degraded, unavailable]

    S3AccessPointState:
      type: string
      enum: [active, disabled]

    S3CredentialState:
      type: string
      enum: [active, revoked, expired]

    S3ObjectEntryType:
      type: string
      enum: [object, prefix]

    S3BucketName:
      type: string
      description: S3 DNS-compatible bucket name; names are globally unique across the control plane.
      minLength: 3
      maxLength: 63
      pattern: '^[a-z0-9](?:[a-z0-9-]{0,61}[a-z0-9])?(?:\.[a-z0-9](?:[a-z0-9-]{0,61}[a-z0-9])?)*$'

    S3ObjectKey:
      $ref: "#/components/schemas/LogicalPath"

    S3ObjectKeyPrefix:
      type: string
      maxLength: 1024

    PermissionName:
      type: string
      minLength: 3
      maxLength: 128
      pattern: '^[a-z][a-z0-9_]*(?:\.[a-z][a-z0-9_]*)+$'
      enum:
        - job.create
        - job.read
        - job.finalize
        - tenant.read
        - tenant.create
        - tenant.admin
        - storage.read
        - storage.create
        - storage.enrollment.create
        - storage.enrollment.read
        - storage.enrollment.review
        - artifact.read
        - artifact.create
        - artifact.commit.replicate
        - project.read
        - project.create
        - playground.read
        - playground.create
        - snapshot.read
        - snapshot.create
        - s3.access.read
        - s3.access.manage
        - resource.lifecycle.read
        - resource.lifecycle.manage
        - retention.manage
        - gateway.read
        - gateway.manage

    TagName:
      type: string
      minLength: 1
      maxLength: 128
      pattern: "^(?!refs/)[A-Za-z0-9](?:[A-Za-z0-9._/-]{0,127})$"

    PageCursor:
      type: string
      minLength: 1
      maxLength: 2048
      description: 服务端生成、与资源 scope、筛选条件和排序绑定的不透明分页 token。

    PageSize:
      type: integer
      minimum: 1
      maximum: 100
      default: 50

    S3PageSize:
      type: integer
      minimum: 1
      maximum: 1000
      default: 100

    SearchQuery:
      type: string
      minLength: 1
      maxLength: 256

    ContentDigest:
      type: string
      minLength: 64
      maxLength: 64
      pattern: "^[0-9a-f]{64}$"

    CanonicalU64:
      type: string
      minLength: 1
      maxLength: 20
      pattern: "^(0|[1-9][0-9]{0,18}|1[0-7][0-9]{18}|18[0-3][0-9]{17}|184[0-3][0-9]{16}|1844[0-5][0-9]{15}|18446[0-6][0-9]{14}|184467[0-3][0-9]{13}|1844674[0-3][0-9]{12}|184467440[0-6][0-9]{10}|1844674407[0-2][0-9]{9}|18446744073[0-6][0-9]{8}|1844674407370[0-8][0-9]{6}|18446744073709[0-4][0-9]{5}|184467440737095[0-4][0-9]{4}|18446744073709550[0-9]{3}|18446744073709551[0-5][0-9]{2}|1844674407370955160[0-9]|1844674407370955161[0-5])$"

    DecimalU64:
      allOf:
        - $ref: "#/components/schemas/CanonicalU64"
      description: 无符号整数，编码为 canonical decimal JSON string。

    PositiveCanonicalU64:
      allOf:
        - $ref: "#/components/schemas/CanonicalU64"
        - pattern: "^[1-9][0-9]{0,19}$"
      description: 大于零且不超过 u64 最大值的 canonical decimal JSON string。

    UnixMillis:
      allOf:
        - $ref: "#/components/schemas/CanonicalU64"
      description: Unix 毫秒时间戳，编码为 canonical decimal JSON string。

    LogicalPath:
      type: string
      minLength: 1
      description: |
        NFC 规范化、使用 `/` 分隔的 repository-relative portable path；不能包含绝对路径、
        空/dot component、反斜杠、NUL、平台保留字符或 NeoEngram 内部名称。

    IndexVersion:
      type: object
      additionalProperties: false
      required: [revision, digest]
      properties:
        revision:
          $ref: "#/components/schemas/CanonicalU64"
        digest:
          $ref: "#/components/schemas/ContentDigest"

    JobState:
      type: string
      enum:
        - queued
        - assigned
        - accepted
        - running
        - prepared
        - publishing
        - cancel_requested
        - succeeded
        - conflicted
        - rejected
        - failed
        - cancelled
        - timed_out
        - recovery_required
        - unknown
