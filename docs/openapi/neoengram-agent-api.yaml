openapi: 3.1.0
jsonSchemaDialect: https://json-schema.org/draft/2020-12/schema
info:
  title: NeoEngram Agent API
  version: 0.0.1
  summary: Agent-initiated action API for enrollment, sessions, Jobs, Index and Manifest pages, and metadata
  description: |
    Contract for the internal Agent action boundary carried through the NeoEngram Gateway. Every
    operation is an action-style POST and all resource identities are carried in the JSON body.
    This API is not exposed by the public Web listener. It is carried only by the mTLS Gateway
    control tunnel and is not a standalone public endpoint.

    Authentication is carried inside each JSON request. OpenAPI 3.1 Security Scheme Objects cannot
    describe a body-carried bootstrap token or Ed25519 proof, so every operation intentionally uses
    `security: []` and names its application-layer scheme through `x-neoengram-body-security`.
    This does not mean that an operation is unauthenticated.
  license:
    name: MIT OR Apache-2.0
    identifier: MIT OR Apache-2.0
servers:
  - url: https://gateway.internal.example
    description: Internal mTLS Gateway control endpoint
tags:
  - name: Enrollment
    description: One-time enrollment bootstrap and signed approval-status query operations
  - name: Session
    description: Signed HTTP/2 control channel, session lifecycle, and recovery operations
  - name: Job
    description: Signed Job report, metadata, and authoritative Index and Manifest operations; object bytes stay on the Volume
x-neoengram-body-security-schemes:
  BootstrapTokenAndEd25519:
    description: >-
      A one-time bootstrap token and installation Ed25519 proof are required in the JSON body.
  InstallationEd25519:
    description: >-
      The installation Ed25519 proof is required in the JSON body when querying enrollment status.
  ApprovedAgentEd25519:
    description: >-
      An approved Agent Ed25519 proof is required in the JSON body and binds POST, canonical path,
      Agent, installation, boot and optional session identities, request ID, timestamp, and the
      BLAKE3 digest of the unsigned RFC 8785 canonical JSON body.
  ApprovedAgentEd25519PerFrame:
    description: >-
      Every upstream NDJSON frame carries an independent approved-Agent Ed25519 proof. The proof
      binds POST, /agent/session/channel/open, Agent, installation, boot and optional session
      identities, request ID, timestamp, sequence, message ID, correlation, frame type, and the
      complete frame payload through the BLAKE3 digest of the unsigned RFC 8785 canonical JSON body.
paths:
  /agent/enrollment/bootstrap:
    post:
      operationId: bootstrapAgentEnrollment
      tags: [Enrollment]
      summary: Bootstrap Agent enrollment
      description: >-
        Consumes a one-time bootstrap token and verifies the installation Ed25519 proof before
        durably creating or replaying the pending enrollment.
      security: []
      x-neoengram-body-security: BootstrapTokenAndEd25519
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AgentBootstrapRequest'
      responses:
        '200':
          description: Bootstrap was durably accepted or replayed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentBootstrapAccepted'
        default:
          $ref: '#/components/responses/Problem'
  /agent/enrollment/status/query:
    post:
      operationId: queryAgentEnrollmentStatus
      tags: [Enrollment]
      summary: Query Agent enrollment status
      description: >-
        Verifies the installation Ed25519 proof and returns the current approval state for the
        exact enrollment request identity without consuming another bootstrap token.
      security: []
      x-neoengram-body-security: InstallationEd25519
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AgentBootstrapStatusRequest'
      responses:
        '200':
          description: Current bootstrap approval state.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentBootstrapStatusResponse'
        default:
          $ref: '#/components/responses/Problem'
  /agent/session/open:
    post:
      operationId: openAgentSession
      tags: [Session]
      summary: Open an Agent session
      description: >-
        Verifies the approved Agent proof and opens or exactly replays the durable session identity
        for the supplied boot, advancing the session generation only for a valid takeover.
      security: []
      x-neoengram-body-security: ApprovedAgentEd25519
      requestBody:
        $ref: '#/components/requestBodies/SessionOpen'
      responses:
        '200':
          description: Durable session identity and generation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentSessionOpenResponse'
        default:
          $ref: '#/components/responses/Problem'
  /agent/session/channel/open:
    post:
      operationId: openAgentSessionChannel
      tags: [Session]
      summary: Open an HTTP/2 Agent session channel
      description: >-
        Opens one Agent-initiated HTTP/2 full-duplex control channel. Both directions carry
        LF-delimited JSON frames; HTTP/2 DATA frame boundaries have no application meaning. The
        first upstream frame must be a signed channel.open and the first downstream frame is its
        correlated channel.opened response. Later upstream frames remain independently signed and
        session-fenced; assignments and decisions are delivered on the downstream stream.
      security: []
      x-neoengram-body-security: ApprovedAgentEd25519PerFrame
      x-neoengram-http2-duplex:
        transport: http2-full-duplex
        primaryControlTransport: true
        mediaType: application/x-ndjson
        h2DataFrameBoundaries: ignored
        frameDelimiter: LF
        maxFrameBytes: 1048576
        delimiterExcludedFromFrameBytes: true
        finalFrameRequiresDelimiter: true
        firstUpstreamFrameType: channel.open
        firstDownstreamFrameType: channel.opened
        upstreamAuthentication: body-carried-ed25519-proof-per-frame
      requestBody:
        required: true
        description: >-
          Streaming request body containing AgentChannelUpstreamFrame records. One logical record is
          one JSON object followed by LF; the schema describes each record, not an H2 DATA frame.
        content:
          application/x-ndjson:
            schema:
              $ref: '#/components/schemas/AgentChannelUpstreamFrame'
      responses:
        '200':
          description: >-
            Streaming response body containing AgentChannelDownstreamFrame records until either side
            closes the channel or a terminal transport/protocol error occurs.
          content:
            application/x-ndjson:
              schema:
                $ref: '#/components/schemas/AgentChannelDownstreamFrame'
        default:
          $ref: '#/components/responses/Problem'
  /agent/session/heartbeat/report:
    post:
      operationId: reportAgentSessionHeartbeat
      tags: [Session]
      summary: Report Agent session heartbeat
      description: >-
        Applies the signed heartbeat and mount observation only when the session identity and
        generation are current, updating the bound Volume health idempotently.
      security: []
      x-neoengram-body-security: ApprovedAgentEd25519
      requestBody:
        $ref: '#/components/requestBodies/HeartbeatReport'
      responses:
        '200':
          description: Heartbeat and mount observation were applied idempotently.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentHeartbeatReportResponse'
        default:
          $ref: '#/components/responses/Problem'
  /agent/job/report/create:
    post:
      operationId: createAgentJobReport
      tags: [Job]
      summary: Create a durable Agent Job report
      description: >-
        Accepts or idempotently replays an ordered Accepted, Progress, Prepared, Failed, or Finalized
        report from the currently fenced Agent session.
      security: []
      x-neoengram-body-security: ApprovedAgentEd25519
      requestBody:
        $ref: '#/components/requestBodies/JobReportCreate'
      responses:
        '200':
          $ref: '#/components/responses/ActionAccepted'
        default:
          $ref: '#/components/responses/Problem'
  /agent/job/metadata/batch/stage:
    post:
      operationId: stageAgentJobMetadataBatch
      tags: [Job]
      summary: Stage a Job metadata batch descriptor
      description: >-
        Validates Job and Tenant scope and idempotently stages one Manifest, IndexDelta, or
        ObjectReceipt MetadataBatch descriptor before its pages are accepted.
      security: []
      x-neoengram-body-security: ApprovedAgentEd25519
      requestBody:
        $ref: '#/components/requestBodies/MetadataBatchStage'
      responses:
        '200':
          $ref: '#/components/responses/MetadataStaged'
        default:
          $ref: '#/components/responses/Problem'
  /agent/job/metadata/page/stage:
    post:
      operationId: stageAgentJobMetadataPage
      tags: [Job]
      summary: Stage a Job metadata batch page
      description: >-
        Validates the descriptor-bound page number, size, digest, Job scope, and session fence before
        idempotently staging the MetadataBatch page.
      security: []
      x-neoengram-body-security: ApprovedAgentEd25519
      requestBody:
        $ref: '#/components/requestBodies/MetadataPageStage'
      responses:
        '200':
          $ref: '#/components/responses/MetadataStaged'
        default:
          $ref: '#/components/responses/Problem'
  /agent/job/index/page/query:
    post:
      operationId: queryAgentJobIndexPage
      tags: [Job]
      summary: Query an authoritative Job Index page
      description: >-
        Returns one immutable page of the exact Index version authorized by the Assignment after
        validating Agent, Job, Artifact, Workspace, and Tenant scope.
      security: []
      x-neoengram-body-security: ApprovedAgentEd25519
      requestBody:
        $ref: '#/components/requestBodies/IndexPageQuery'
      responses:
        '200':
          description: One immutable page of the requested Index version.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentIndexPageQueryResponse'
        default:
          $ref: '#/components/responses/Problem'
  /agent/job/manifest/page/query:
    post:
      operationId: queryAgentJobManifestPage
      tags: [Job]
      summary: Query an authoritative Job Manifest page
      description: >-
        Returns one immutable page of Manifest chunk references authorized by the Assignment after
        validating Agent, Job, Artifact, Manifest, and Tenant scope. The response contains metadata
        only; immutable object payload bytes remain on the user Volume.
      security: []
      x-neoengram-body-security: ApprovedAgentEd25519
      requestBody:
        $ref: '#/components/requestBodies/ManifestPageQuery'
      responses:
        '200':
          description: One immutable page of the requested Manifest.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentManifestPageQueryResponse'
        default:
          $ref: '#/components/responses/Problem'
  /agent/session/close:
    post:
      operationId: closeAgentSession
      tags: [Session]
      summary: Close an Agent session
      description: >-
        Fences the exact current session closed and advances its durable state so later requests from
        the closed generation are rejected.
      security: []
      x-neoengram-body-security: ApprovedAgentEd25519
      requestBody:
        $ref: '#/components/requestBodies/SessionClose'
      responses:
        '200':
          description: Session was fenced closed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentSessionCloseResponse'
        default:
          $ref: '#/components/responses/Problem'
components:
  requestBodies:
    SessionOpen:
      required: true
      content:
        application/json:
          schema:
            allOf:
              - $ref: '#/components/schemas/SignedAgentRequest'
              - type: object
                additionalProperties: false
                properties:
                  payload:
                    $ref: '#/components/schemas/AgentSessionOpenPayload'
            unevaluatedProperties: false
    HeartbeatReport:
      required: true
      content:
        application/json:
          schema:
            allOf:
              - $ref: '#/components/schemas/EstablishedSessionRequest'
              - type: object
                additionalProperties: false
                properties:
                  payload:
                    $ref: '#/components/schemas/AgentHeartbeatReportPayload'
            unevaluatedProperties: false
    JobReportCreate:
      required: true
      content:
        application/json:
          schema:
            allOf:
              - $ref: '#/components/schemas/EstablishedSessionRequest'
              - type: object
                additionalProperties: false
                properties:
                  payload:
                    $ref: '#/components/schemas/AgentJobReportCreatePayload'
            unevaluatedProperties: false
    MetadataBatchStage:
      required: true
      content:
        application/json:
          schema:
            allOf:
              - $ref: '#/components/schemas/EstablishedSessionRequest'
              - type: object
                additionalProperties: false
                properties:
                  payload:
                    $ref: '#/components/schemas/AgentMetadataBatchStagePayload'
            unevaluatedProperties: false
    MetadataPageStage:
      required: true
      content:
        application/json:
          schema:
            allOf:
              - $ref: '#/components/schemas/EstablishedSessionRequest'
              - type: object
                additionalProperties: false
                properties:
                  payload:
                    $ref: '#/components/schemas/AgentMetadataPageStagePayload'
            unevaluatedProperties: false
    IndexPageQuery:
      required: true
      content:
        application/json:
          schema:
            allOf:
              - $ref: '#/components/schemas/EstablishedSessionRequest'
              - type: object
                additionalProperties: false
                properties:
                  payload:
                    $ref: '#/components/schemas/AgentIndexPageQueryPayload'
            unevaluatedProperties: false
    ManifestPageQuery:
      required: true
      content:
        application/json:
          schema:
            allOf:
              - $ref: '#/components/schemas/EstablishedSessionRequest'
              - type: object
                additionalProperties: false
                properties:
                  payload:
                    $ref: '#/components/schemas/AgentManifestPageQueryPayload'
            unevaluatedProperties: false
    SessionClose:
      required: true
      content:
        application/json:
          schema:
            allOf:
              - $ref: '#/components/schemas/EstablishedSessionRequest'
              - type: object
                additionalProperties: false
                properties:
                  payload:
                    $ref: '#/components/schemas/AgentSessionClosePayload'
            unevaluatedProperties: false
  responses:
    ActionAccepted:
      description: Action was applied or replayed.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/AgentActionAcceptedResponse'
    MetadataStaged:
      description: Metadata material was staged idempotently.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/AgentMetadataStageResponse'
    Problem:
      description: Sanitized Agent API problem.
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
  schemas:
    Id:
      type: string
      minLength: 1
      maxLength: 128
      pattern: '^[A-Za-z0-9](?:[A-Za-z0-9._:-]{0,127})$'
    DecimalU64:
      $ref: '../../crates/neoengram-domain/schemas/current/control-envelope.schema.json#/$defs/DecimalU64'
    PositiveDecimalU64:
      $ref: '../../crates/neoengram-domain/schemas/current/control-envelope.schema.json#/$defs/SessionGeneration'
    Digest:
      type: string
      pattern: '^[0-9a-f]{64}$'
    AgentRequestProof:
      type: object
      additionalProperties: false
      required: [unsigned_body_digest, algorithm, public_key_spki, signature]
      properties:
        unsigned_body_digest:
          $ref: '#/components/schemas/Digest'
        algorithm:
          const: ed25519
        public_key_spki:
          $ref: '../../crates/neoengram-domain/schemas/current/agent-enrollment.schema.json#/$defs/Ed25519PublicKeySpki'
        signature:
          $ref: '../../crates/neoengram-domain/schemas/current/agent-enrollment.schema.json#/$defs/Ed25519Signature'
    SignedAgentRequest:
      type: object
      additionalProperties: false
      required:
        - wire_version
        - request_id
        - agent_id
        - installation_id
        - boot_id
        - signed_at_unix_ms
        - payload
        - proof
      properties:
        wire_version:
          const: 1
        request_id:
          $ref: '#/components/schemas/Id'
        agent_id:
          $ref: '#/components/schemas/Id'
        installation_id:
          $ref: '#/components/schemas/Id'
        boot_id:
          $ref: '#/components/schemas/Id'
        session_id:
          $ref: '#/components/schemas/Id'
        session_generation:
          $ref: '#/components/schemas/PositiveDecimalU64'
        signed_at_unix_ms:
          $ref: '#/components/schemas/DecimalU64'
        payload:
          $ref: '#/components/schemas/AgentRequestPayload'
        proof:
          $ref: '#/components/schemas/AgentRequestProof'
    EstablishedSessionRequest:
      allOf:
        - $ref: '#/components/schemas/SignedAgentRequest'
        - type: object
          additionalProperties: false
          required: [session_id, session_generation]
          properties:
            session_id:
              $ref: '#/components/schemas/Id'
            session_generation:
              $ref: '#/components/schemas/PositiveDecimalU64'
      unevaluatedProperties: false
    AgentRequestPayload:
      oneOf:
        - $ref: '#/components/schemas/AgentSessionOpenPayload'
        - $ref: '#/components/schemas/AgentHeartbeatReportPayload'
        - $ref: '#/components/schemas/AgentJobReportCreatePayload'
        - $ref: '#/components/schemas/AgentMetadataBatchStagePayload'
        - $ref: '#/components/schemas/AgentMetadataPageStagePayload'
        - $ref: '#/components/schemas/AgentIndexPageQueryPayload'
        - $ref: '#/components/schemas/AgentManifestPageQueryPayload'
        - $ref: '#/components/schemas/AgentSessionClosePayload'
        - $ref: '#/components/schemas/AgentChannelUpstreamPayloadBase'
    AgentChannelUpstreamFrame:
      description: One independently signed Agent-to-Central logical JSON record carried through Gateway, limited to 1048576 encoded bytes before its mandatory LF delimiter.
      oneOf:
        - $ref: '#/components/schemas/AgentChannelOpenFrame'
        - $ref: '#/components/schemas/AgentChannelHeartbeatFrame'
        - $ref: '#/components/schemas/AgentChannelReportFrame'
        - $ref: '#/components/schemas/AgentChannelCloseFrame'
    AgentChannelUpstreamPayloadBase:
      type: object
      additionalProperties: false
      required: [sequence, message_id, type, payload]
      properties:
        sequence:
          $ref: '#/components/schemas/PositiveDecimalU64'
        message_id:
          $ref: '#/components/schemas/Id'
        correlation_id:
          $ref: '#/components/schemas/Id'
        type:
          type: string
        payload:
          oneOf:
            - $ref: '#/components/schemas/AgentSessionOpenPayload'
            - $ref: '#/components/schemas/AgentHeartbeatReportPayload'
            - $ref: '#/components/schemas/AgentJobReportCreatePayload'
            - $ref: '#/components/schemas/AgentSessionClosePayload'
    AgentChannelOpenFrame:
      allOf:
        - $ref: '#/components/schemas/SignedAgentRequest'
        - type: object
          additionalProperties: false
          required: [payload]
          properties:
            session_id: false
            session_generation: false
            payload:
              allOf:
                - $ref: '#/components/schemas/AgentChannelUpstreamPayloadBase'
                - type: object
                  additionalProperties: false
                  properties:
                    correlation_id: false
                    sequence:
                      const: '1'
                    type:
                      const: channel.open
                    payload:
                      $ref: '#/components/schemas/AgentSessionOpenPayload'
      unevaluatedProperties: false
    AgentChannelHeartbeatFrame:
      allOf:
        - $ref: '#/components/schemas/EstablishedSessionRequest'
        - type: object
          additionalProperties: false
          required: [payload]
          properties:
            payload:
              allOf:
                - $ref: '#/components/schemas/AgentChannelUpstreamPayloadBase'
                - type: object
                  additionalProperties: false
                  properties:
                    type:
                      const: session.heartbeat
                    payload:
                      $ref: '#/components/schemas/AgentHeartbeatReportPayload'
      unevaluatedProperties: false
    AgentChannelReportFrame:
      allOf:
        - $ref: '#/components/schemas/EstablishedSessionRequest'
        - type: object
          additionalProperties: false
          required: [payload]
          properties:
            payload:
              allOf:
                - $ref: '#/components/schemas/AgentChannelUpstreamPayloadBase'
                - type: object
                  additionalProperties: false
                  properties:
                    type:
                      const: job.report
                    payload:
                      $ref: '#/components/schemas/AgentJobReportCreatePayload'
      unevaluatedProperties: false
    AgentChannelCloseFrame:
      allOf:
        - $ref: '#/components/schemas/EstablishedSessionRequest'
        - type: object
          additionalProperties: false
          required: [payload]
          properties:
            payload:
              allOf:
                - $ref: '#/components/schemas/AgentChannelUpstreamPayloadBase'
                - type: object
                  additionalProperties: false
                  properties:
                    type:
                      const: channel.close
                    payload:
                      $ref: '#/components/schemas/AgentSessionClosePayload'
      unevaluatedProperties: false
    AgentChannelDownstreamFrame:
      description: One Central-to-Agent logical JSON record carried through Gateway, limited to 1048576 encoded bytes before its mandatory LF delimiter.
      oneOf:
        - $ref: '#/components/schemas/AgentChannelOpenedFrame'
        - $ref: '#/components/schemas/AgentChannelAssignmentFrame'
        - $ref: '#/components/schemas/AgentChannelDecisionFrame'
        - $ref: '#/components/schemas/AgentChannelAckFrame'
        - $ref: '#/components/schemas/AgentChannelErrorFrame'
      discriminator:
        propertyName: type
        mapping:
          channel.opened: '#/components/schemas/AgentChannelOpenedFrame'
          job.assignment: '#/components/schemas/AgentChannelAssignmentFrame'
          job.decision: '#/components/schemas/AgentChannelDecisionFrame'
          channel.ack: '#/components/schemas/AgentChannelAckFrame'
          protocol.error: '#/components/schemas/AgentChannelErrorFrame'
    AgentChannelDownstreamBase:
      type: object
      additionalProperties: false
      required:
        - wire_version
        - sequence
        - message_id
        - session_generation
        - sent_at_unix_ms
        - type
        - payload
      properties:
        wire_version:
          const: 1
        sequence:
          $ref: '#/components/schemas/PositiveDecimalU64'
        message_id:
          $ref: '#/components/schemas/Id'
        correlation_id:
          $ref: '#/components/schemas/Id'
        session_generation:
          $ref: '#/components/schemas/PositiveDecimalU64'
        sent_at_unix_ms:
          $ref: '#/components/schemas/DecimalU64'
        type:
          type: string
        payload:
          oneOf:
            - $ref: '#/components/schemas/AgentSessionOpenResponse'
            - $ref: '#/components/schemas/AgentChannelJobAssignment'
            - $ref: '#/components/schemas/AgentChannelJobDecision'
            - $ref: '#/components/schemas/AgentChannelAck'
            - $ref: '#/components/schemas/AgentChannelControlError'
    AgentChannelOpenedFrame:
      allOf:
        - $ref: '#/components/schemas/AgentChannelDownstreamBase'
        - type: object
          additionalProperties: false
          required: [correlation_id]
          properties:
            sequence:
              const: '1'
            type:
              const: channel.opened
            payload:
              $ref: '#/components/schemas/AgentSessionOpenResponse'
      unevaluatedProperties: false
    AgentChannelAssignmentFrame:
      allOf:
        - $ref: '#/components/schemas/AgentChannelDownstreamBase'
        - type: object
          additionalProperties: false
          properties:
            correlation_id: false
            type:
              const: job.assignment
            payload:
              $ref: '#/components/schemas/AgentChannelJobAssignment'
      unevaluatedProperties: false
    AgentChannelDecisionFrame:
      allOf:
        - $ref: '#/components/schemas/AgentChannelDownstreamBase'
        - type: object
          additionalProperties: false
          properties:
            correlation_id: false
            type:
              const: job.decision
            payload:
              $ref: '#/components/schemas/AgentChannelJobDecision'
      unevaluatedProperties: false
    AgentChannelAckFrame:
      allOf:
        - $ref: '#/components/schemas/AgentChannelDownstreamBase'
        - type: object
          additionalProperties: false
          required: [correlation_id]
          properties:
            type:
              const: channel.ack
            payload:
              $ref: '#/components/schemas/AgentChannelAck'
      unevaluatedProperties: false
    AgentChannelErrorFrame:
      allOf:
        - $ref: '#/components/schemas/AgentChannelDownstreamBase'
        - type: object
          additionalProperties: false
          properties:
            type:
              const: protocol.error
            payload:
              $ref: '#/components/schemas/AgentChannelControlError'
      unevaluatedProperties: false
    AgentChannelAck:
      type: object
      additionalProperties: false
      required: [acknowledged_sequence, resource_version, replayed]
      properties:
        acknowledged_sequence:
          $ref: '#/components/schemas/PositiveDecimalU64'
        resource_version:
          $ref: '#/components/schemas/DecimalU64'
        replayed:
          type: boolean
    AgentChannelJobAssignment:
      allOf:
        - $ref: '../../crates/neoengram-domain/schemas/current/control-envelope.schema.json#/$defs/JobAssignment'
        - type: object
          additionalProperties: false
          properties:
            assignment: {}
      unevaluatedProperties: false
    AgentChannelJobDecision:
      allOf:
        - $ref: '../../crates/neoengram-domain/schemas/current/control-envelope.schema.json#/$defs/JobDecision'
        - type: object
          additionalProperties: false
          properties:
            assignment_generation: {}
            assignment_id: {}
            decision: {}
            decision_generation: {}
            final_state: {}
            job_id: {}
      unevaluatedProperties: false
    AgentChannelControlError:
      type: object
      additionalProperties: false
      required: [code, message, retryable]
      properties:
        code:
          type: string
          minLength: 1
          maxLength: 96
          pattern: '^[A-Z][A-Z0-9_]{0,95}$'
        message:
          type: string
          minLength: 1
          maxLength: 4096
        retryable:
          type: boolean
        retry_after_ms:
          $ref: '#/components/schemas/DecimalU64'
    AgentSessionOpenPayload:
      type: object
      additionalProperties: false
      required: [mount_identity_digest, expected_resource_version]
      properties:
        mount_identity_digest:
          $ref: '#/components/schemas/Digest'
        expected_resource_version:
          $ref: '#/components/schemas/DecimalU64'
        capabilities:
          type: array
          maxItems: 256
          uniqueItems: true
          items:
            type: string
            minLength: 1
            maxLength: 128
    AgentSessionOpenResponse:
      type: object
      additionalProperties: false
      required: [wire_version, request_id, agent_id, session_id, session_generation, agent_mount_id, mount_generation, owner_generation, resource_version, opened_at_unix_ms, replayed]
      properties:
        wire_version: { const: 1 }
        request_id: { $ref: '#/components/schemas/Id' }
        agent_id: { $ref: '#/components/schemas/Id' }
        session_id: { $ref: '#/components/schemas/Id' }
        session_generation: { $ref: '#/components/schemas/PositiveDecimalU64' }
        agent_mount_id: { $ref: '#/components/schemas/Id' }
        mount_generation: { $ref: '#/components/schemas/PositiveDecimalU64' }
        owner_generation: { $ref: '#/components/schemas/PositiveDecimalU64' }
        resource_version: { $ref: '#/components/schemas/DecimalU64' }
        opened_at_unix_ms: { $ref: '#/components/schemas/DecimalU64' }
        replayed: { type: boolean }
    AgentHeartbeatReportPayload:
      type: object
      additionalProperties: false
      required: [heartbeat, mount_report]
      properties:
        heartbeat:
          $ref: '#/components/schemas/AgentHeartbeat'
        mount_report:
          $ref: '#/components/schemas/AgentMountStatusReport'
    AgentHeartbeatReportResponse:
      type: object
      additionalProperties: false
      required: [wire_version, request_id, resource_version, received_at_unix_ms, replayed]
      properties:
        wire_version: { const: 1 }
        request_id: { $ref: '#/components/schemas/Id' }
        resource_version: { $ref: '#/components/schemas/DecimalU64' }
        received_at_unix_ms: { $ref: '#/components/schemas/DecimalU64' }
        replayed: { type: boolean }
    AgentJobReportCreatePayload:
      type: object
      additionalProperties: false
      required: [tenant_id, report]
      properties:
        tenant_id: { $ref: '#/components/schemas/Id' }
        report:
          allOf:
            - $ref: '../../crates/neoengram-domain/schemas/current/action-envelope.schema.json'
            - type: object
              description: >-
                Job reports are sent only on an established Agent session, so the shared Envelope
                must carry the same positive session generation as the authenticated outer request.
              required: [session_generation]
    AgentMetadataBatchStagePayload:
      type: object
      additionalProperties: false
      required: [tenant_id, job_id, descriptor]
      properties:
        tenant_id: { $ref: '#/components/schemas/Id' }
        job_id: { $ref: '#/components/schemas/Id' }
        descriptor:
          $ref: '#/components/schemas/AgentMetadataBatchDescriptor'
    AgentMetadataPageStagePayload:
      type: object
      additionalProperties: false
      required: [tenant_id, job_id, page]
      properties:
        tenant_id: { $ref: '#/components/schemas/Id' }
        job_id: { $ref: '#/components/schemas/Id' }
        page:
          $ref: '#/components/schemas/AgentMetadataBatchPage'
    AgentIndexPageQueryPayload:
      type: object
      additionalProperties: false
      required: [tenant_id, job_id, artifact_id, index_version, page_number, max_records]
      oneOf:
        - required: [workspace_id]
          properties:
            workspace_id: { $ref: '#/components/schemas/Id' }
        - required: [snapshot_id]
          properties:
            snapshot_id: { $ref: '#/components/schemas/Id' }
      properties:
        tenant_id: { $ref: '#/components/schemas/Id' }
        job_id: { $ref: '#/components/schemas/Id' }
        artifact_id: { $ref: '#/components/schemas/Id' }
        workspace_id: { $ref: '#/components/schemas/Id' }
        snapshot_id: { $ref: '#/components/schemas/Id' }
        index_version:
          $ref: '#/components/schemas/AgentWireIndexVersion'
        page_number: { type: integer, minimum: 0 }
        max_records: { type: integer, minimum: 1, maximum: 4096 }
    AgentIndexPageQueryResponse:
      type: object
      additionalProperties: false
      required: [wire_version, request_id, index_version, page_number, page_count, records]
      properties:
        wire_version: { const: 1 }
        request_id: { $ref: '#/components/schemas/Id' }
        index_version:
          $ref: '#/components/schemas/AgentWireIndexVersion'
        page_number: { type: integer, minimum: 0 }
        page_count: { type: integer, minimum: 0 }
        records:
          type: array
          maxItems: 4096
          items:
            $ref: '#/components/schemas/AgentIndexRecord'
    AgentIndexRecord:
      type: object
      additionalProperties: false
      required: [path, manifest_id, total_size, chunk_count]
      properties:
        path:
          type: string
          minLength: 1
          maxLength: 4096
        manifest_id:
          $ref: '#/components/schemas/Digest'
        total_size:
          $ref: '#/components/schemas/DecimalU64'
        chunk_count:
          $ref: '#/components/schemas/DecimalU64'
    WireChunkingStrategy:
      $ref: '../../crates/neoengram-domain/schemas/current/metadata-batch.schema.json#/$defs/WireChunkingStrategy'
    WireChunkRef:
      $ref: '#/components/schemas/AgentWireChunkRef'
    AgentManifestPageQueryPayload:
      type: object
      additionalProperties: false
      required: [tenant_id, job_id, artifact_id, manifest_id, page_number, max_chunks]
      properties:
        tenant_id: { $ref: '#/components/schemas/Id' }
        job_id: { $ref: '#/components/schemas/Id' }
        artifact_id: { $ref: '#/components/schemas/Id' }
        manifest_id: { $ref: '#/components/schemas/Digest' }
        page_number: { type: integer, minimum: 0 }
        max_chunks: { type: integer, minimum: 1, maximum: 4096 }
    AgentManifestPageQueryResponse:
      type: object
      additionalProperties: false
      required: [wire_version, request_id, manifest_id, total_size, chunking, page_number, page_count, chunks]
      properties:
        wire_version: { const: 1 }
        request_id: { $ref: '#/components/schemas/Id' }
        manifest_id: { $ref: '#/components/schemas/Digest' }
        total_size: { $ref: '#/components/schemas/DecimalU64' }
        chunking: { $ref: '#/components/schemas/WireChunkingStrategy' }
        page_number: { type: integer, minimum: 0 }
        page_count: { type: integer, minimum: 1 }
        chunks: { type: array, maxItems: 4096, items: { $ref: '#/components/schemas/WireChunkRef' } }
    AgentSessionClosePayload:
      type: object
      additionalProperties: false
      required: [expected_resource_version]
      properties:
        expected_resource_version: { $ref: '#/components/schemas/DecimalU64' }
    AgentSessionCloseResponse:
      type: object
      additionalProperties: false
      required: [wire_version, request_id, resource_version, closed_at_unix_ms]
      properties:
        wire_version: { const: 1 }
        request_id: { $ref: '#/components/schemas/Id' }
        resource_version: { $ref: '#/components/schemas/DecimalU64' }
        closed_at_unix_ms: { $ref: '#/components/schemas/DecimalU64' }
    AgentActionAcceptedResponse:
      type: object
      additionalProperties: false
      required: [wire_version, request_id, resource_version, replayed]
      properties:
        wire_version: { const: 1 }
        request_id: { $ref: '#/components/schemas/Id' }
        resource_version: { $ref: '#/components/schemas/DecimalU64' }
        replayed: { type: boolean }
    AgentMetadataStageResponse:
      type: object
      additionalProperties: false
      required: [wire_version, request_id, batch_id, complete, replayed]
      properties:
        wire_version: { const: 1 }
        request_id: { $ref: '#/components/schemas/Id' }
        batch_id: { $ref: '#/components/schemas/Id' }
        complete: { type: boolean }
        replayed: { type: boolean }
    AgentHeartbeat:
      allOf:
        - $ref: '../../crates/neoengram-domain/schemas/current/control-envelope.schema.json#/$defs/AgentHeartbeat'
        - type: object
          additionalProperties: false
          properties:
            acknowledged_resource_version: {}
            agent_id: {}
            mount_observations: {}
            observed_at_unix_ms: {}
            running_jobs: {}
            sequence: {}
      unevaluatedProperties: false
    AgentMountStatusReport:
      allOf:
        - $ref: '../../crates/neoengram-domain/schemas/current/agent-enrollment.schema.json#/$defs/AgentMountStatusReport'
        - type: object
          additionalProperties: false
          properties:
            access_mode: {}
            agent_id: {}
            agent_mount_id: {}
            boot_id: {}
            health: {}
            installation_id: {}
            mount_generation: {}
            mount_identity_digest: {}
            observed_at_unix_ms: {}
            observed_volume_marker: {}
            owner_generation: {}
            sequence: {}
            session_generation: {}
            storage_volume_id: {}
      unevaluatedProperties: false
    AgentWireChunkRef:
      allOf:
        - $ref: '../../crates/neoengram-domain/schemas/current/metadata-batch.schema.json#/$defs/WireChunkRef'
        - type: object
          additionalProperties: false
          properties:
            object_id: {}
            offset: {}
            size: {}
      unevaluatedProperties: false
    AgentMetadataBatchDescriptor:
      allOf:
        - $ref: '../../crates/neoengram-domain/schemas/current/control-envelope.schema.json#/$defs/MetadataBatchDescriptor'
        - type: object
          additionalProperties: false
          properties:
            batch_digest: {}
            batch_id: {}
            encoded_bytes: {}
            kind: {}
            page_count: {}
            pages: {}
            record_count: {}
            schema_version: {}
            scope: {}
      unevaluatedProperties: false
    AgentMetadataBatchPage:
      allOf:
        - $ref: '../../crates/neoengram-domain/schemas/current/metadata-batch.schema.json#/$defs/MetadataBatchPage'
        - type: object
          additionalProperties: false
          properties:
            batch_id: {}
            batch_digest: {}
            kind: {}
            page_count: {}
            page_digest: {}
            page_number: {}
            records: {}
            record_count: {}
            schema_version: {}
            scope: {}
      unevaluatedProperties: false
    AgentWireIndexVersion:
      allOf:
        - $ref: '../../crates/neoengram-domain/schemas/current/metadata-batch.schema.json#/$defs/WireIndexVersion'
        - type: object
          additionalProperties: false
          properties:
            digest: {}
            revision: {}
      unevaluatedProperties: false
    AgentBootstrapRequest:
      allOf:
        - $ref: '../../crates/neoengram-domain/schemas/current/agent-enrollment.schema.json#/$defs/AgentBootstrapRequest'
        - type: object
          additionalProperties: false
          properties:
            agent_version: {}
            bootstrap_request_id: {}
            bootstrap_token: {}
            capabilities: {}
            edge_cluster_id: {}
            installation_id: {}
            probe:
              $ref: '#/components/schemas/AgentBootstrapProbe'
            proof:
              $ref: '#/components/schemas/AgentBootstrapProof'
            public_key_fingerprint: {}
            storage_volume_id: {}
            tenant_id: {}
            volume_descriptor_digest: {}
            wire_version: {}
      unevaluatedProperties: false
    AgentBootstrapStatusRequest:
      allOf:
        - $ref: '../../crates/neoengram-domain/schemas/current/agent-enrollment.schema.json#/$defs/AgentBootstrapStatusRequest'
        - type: object
          additionalProperties: false
          properties:
            bootstrap_request_id: {}
            installation_id: {}
            proof: {}
            signed_at_unix_ms: {}
            tenant_id: {}
            wire_version: {}
      unevaluatedProperties: false
    AgentBootstrapStatusResponse:
      allOf:
        - $ref: '../../crates/neoengram-domain/schemas/current/agent-enrollment.schema.json#/$defs/AgentBootstrapStatusResponse'
        - type: object
          additionalProperties: false
          properties:
            agent_id: {}
            bootstrap_request_id: {}
            certificate: {}
            enrollment_id: {}
            installation_id: {}
            resource_version: {}
            state: {}
            updated_at_unix_ms: {}
            wire_version: {}
      unevaluatedProperties: false
    AgentBootstrapAccepted:
      allOf:
        - $ref: '../../crates/neoengram-domain/schemas/current/agent-enrollment.schema.json#/$defs/AgentBootstrapAccepted'
        - type: object
          additionalProperties: false
          properties:
            agent_id: {}
            agent_mount_id: {}
            bootstrap_request_id: {}
            enrollment_id: {}
            mount_generation: {}
            replayed: {}
            resource_version: {}
            review_expires_at_unix_ms: {}
            state: {}
      unevaluatedProperties: false
    AgentBootstrapProbe:
      allOf:
        - $ref: '../../crates/neoengram-domain/schemas/current/agent-enrollment.schema.json#/$defs/AgentBootstrapProbe'
        - type: object
          additionalProperties: false
          properties:
            access_mode: {}
            fsync_supported: {}
            health: {}
            marker_matches: {}
            mount_boundary_detected: {}
            mount_identity_digest: {}
            observed_at_unix_ms: {}
            observed_volume_marker: {}
            rename_supported: {}
      unevaluatedProperties: false
    AgentBootstrapProof:
      allOf:
        - $ref: '../../crates/neoengram-domain/schemas/current/agent-enrollment.schema.json#/$defs/AgentBootstrapProof'
        - type: object
          additionalProperties: false
          properties:
            algorithm: {}
            public_key_spki: {}
            signature: {}
      unevaluatedProperties: false
    Problem:
      type: object
      additionalProperties: false
      required: [type, title, status, detail, instance, code, request_id, retryable]
      properties:
        type: { type: string }
        title: { type: string }
        status: { type: integer, minimum: 400, maximum: 599 }
        detail: { type: string }
        instance: { type: string }
        code: { type: string }
        request_id: { type: string }
        retryable: { type: boolean }
        retry_after_ms: { $ref: '#/components/schemas/DecimalU64' }
